<!-- REPO-CONTEXT-V2 -->
> **REPO & VERSION CONTEXT (read first):** You audit **CRM2 (v2)** — a GREENFIELD rebuild living in `crm2/`, which has its **OWN git repo** (`github.com/acsdeveloper2025/acs-crm-2`) even though it sits INSIDE the v1 monorepo directory `CRM-APP-MONOREPO-PROD/` (v1 git-ignores it). **THREE separate git repos share that one folder:**
> 1. **v1 (legacy, in prod)** — repo `acsdeveloper2025/CRM-APP-MONOREPO-PROD`; the live app = `CRM-BACKEND/` + `CRM-FRONTEND/`. ⚠️ `acs_db_final_version.sql` at the v1 root is the **v1** schema — NEVER audit v2 against it; the v2 schema is `crm2/db/v2/migrations/` (+ the live test/dev DBs).
> 2. **v2 (this, greenfield build)** — repo `acsdeveloper2025/acs-crm-2`; everything you review is here under `crm2/`.
> 3. **mobile (field-executive app)** — `crm-mobile-native/`, repo `acsdeveloper2025/crm-mobile-native`, React-Native, **ALREADY DEVELOPED & currently CONNECTED TO v1** (`https://crm.allcheckservices.com/api/mobile`). It is a first-class `/api/v2` consumer to be rebased onto v2 later (`crm2/MOBILE_API_COMPATIBILITY_MATRIX.md`) — **never break the mobile contract** (ADR-0012).

# Ledger — Database & Data-Integrity Auditor

Charter: AGENT_ORG.md row 3. Migrations · schema/constraints/indexes · transactions · OCC · query
correctness · N+1 · append-only audit_log · live-DB triple-write. SoT: DATABASE_CHANGE_PROCESS ·
db/v2/migrations · ADR-0019 (OCC) · CONCURRENCY_AND_EDITING_STANDARD · feedback_sql_live_db_apply.

## Standing invariants
- DDL only via migrations (forward-only, idempotent: `ADD COLUMN IF NOT EXISTS`, DO-block CHECKs).
- **Triple-write:** a new migration must be applied to BOTH dev `:54329` (psql -f by hand) AND test `:5433`
  (harness) — source-only migrations leave the dev API 500ing (`relation does not exist`).
- OCC: `version` col on every editable table; guarded update; `audit_log` is **append-only** (DB trigger
  blocks UPDATE/DELETE). Effective-dated domains (rates) keep their own `rate_history` — don't double-audit.
- Every list query: `count(*)::int` for totalCount + a separate paginated SELECT.

## Migration state
- Chain `0001`→`0019` clean on a fresh DB. 0017 audit_log+version(5 tables) · 0018 CPV version · 0019 rates version.
- **No migration in the DataGrid/pagination slice** (code-only). ⚠️ **OPEN PERF/DB item:** the paginated
  `ORDER BY <sortColumn>` on clients/products is currently unindexed beyond PK; sortable columns
  (name/code/effective_from/created_at/updated_at) should get indexes as lists grow — esp. **locations (157k)**
  when it's retrofitted. Flag missing indexes on any sort/filter column during the rollout.

## Log
- **2026-06-06 · DataGrid + pagination (`4e7a8fd`)** — clients/products repo.list = COUNT + paginated
  SELECT with `ORDER BY ${sortColumn} ${sortOrder}, id ${sortOrder}` (stable tiebreaker). sortColumn is
  whitelisted (safe). No new migration. OPEN: add indexes for sort/filter columns before GA, esp. locations.

- **2026-06-06 · DataGrid Slice 1 — users / verification_units / report_templates (uncommitted)** —
  VERDICT **PASS**. repo.list on all three → `count(*)::int` + paginated SELECT.
  - (a) INJECTION clean. `ORDER BY ${o.sortColumn} ${o.sortOrder}` interpolates only whitelisted literals:
    `sortColumn` is resolved in `platform/pagination.ts:51-55` — `sortBy` accepted ONLY if `hasOwnProperty`
    on `spec.sortMap` else `defaultSort`, then `sortColumn = spec.sortMap[sortBy]` (a fixed string literal,
    never user input). `sortOrder` hard-coerced to `'asc'`/`'desc'` (`pagination.ts:58-59`). Controllers now
    pass `req.query` straight through (users/controller.ts:19, vu:17, templates:18) — all coercion in
    service+resolvePage. The dead `?? spec.defaultSort` fallback (pagination.ts:55) is harmless (literal).
  - (a') users reportsTo → `m.name` (the LEFT JOIN alias): VALID — `m.name` appears ONLY in the items
    ORDER BY (users/repository.ts:75-79, which has `LEFT JOIN users m`). COUNT query
    (users/repository.ts:60 `FROM users u ${clause}`) has NO join and its WHERE only ever references `u.*`
    (`u.role`/`u.is_active`/`u.username`/`u.name`, repository.ts:42-52) — never `m.*`. No broken COUNT. Safe.
  - (b) COUNT correctness: same `${clause}`/`params` as items, `::int` cast, all three. (c) every value via `$n`
    (search via `%..%` bind). (d) no N+1 (reportsTo via the existing single LEFT JOIN, not per-row). (e) param
    numbering correct: filters fill `$1..$N`, then `LIMIT $${N+1} OFFSET $${N+2}` with `[...params, limit, offset]`
    (users/repo:80, vu/repo:66, templates/repo:62); deterministic id tiebreak (`u.id`/`id`) shares the sortOrder.
  - (f) NO migration — confirmed none needed (no schema change; sort/filter cols pre-exist).
  - SHOULD-FIX / OPEN (perf, carried): newly-sortable cols largely unindexed —
    users(name/username/effective_from/created_at/updated_at + reportsTo `m.name`) and default sorts
    `verification_units.sort_order` & `report_templates.name` have NO index (existing idx cover only
    role/reports_to/is_active, kind/category/code, type/active/code). Tables small-to-moderate now
    (users tens · VU hundreds · templates ~180) → seq-scan + in-mem sort acceptable; add indexes before GA
    and BEFORE the locations(157k)/cases retrofit. reportsTo-sort (ORDER BY on JOINed self-table) is the
    slowest path to watch.

- **2026-06-06 · DataGrid Slice 3 — cases list server-pagination (uncommitted)** —
  VERDICT **PASS** (1 perf RATCHET, non-blocking). `cases/repository.ts` `list()` → COUNT + paginated
  SELECT; `cases/service.ts` `list()` now resolves via `CASE_PAGE_SPEC`/`resolvePage`/`buildPage`,
  returns `Paginated<CaseView>`. No migration (code-only — confirmed `git diff --stat db/v2/migrations` empty).
  - (a) ✅ COUNT CORRECTNESS — `count(*)::int FROM cases cs LEFT JOIN case_applicants pa ON pa.case_id=cs.id
    AND pa.is_primary ${clause}` (repository.ts:217-221) == #cases matching filter. Proof: partial-unique
    `uq_case_one_primary ON case_applicants(case_id) WHERE is_primary` (0010_cases.sql:47) ⇒ ≤1 primary row
    per case; LEFT JOIN emits exactly 1 row per case (the primary, or a NULL-padded row when none) ⇒
    no row multiplication, `count(*)` == count of `cases` rows. Predicate satisfiability on the cs+pa join
    ONLY: `cs.status` (col on cases), `cs.client_id` (col on cases), search `(pa.name ILIKE OR cs.case_number
    ILIKE)` — `pa.name` is the joined primary-applicant alias, `cs.case_number` on cases. NONE reference
    `clients`/`products` ⇒ dropping the `JOIN clients`/`JOIN products` from the items SELECT in the COUNT is
    SAFE and does not change the row set (inner joins to clients/products are FK-NOT-NULL one-to-one — `cases.client_id`
    NOT NULL REFERENCES clients, `cases.product_id` NOT NULL REFERENCES products (0010:12-13) — so they neither
    drop nor multiply case rows; their absence in COUNT is correct AND a cheaper plan). COUNT WHERE == items WHERE
    (same `clause`/`params` built once at repository.ts:205-216). ✅
  - (b) ✅ ORDER BY — `ORDER BY ${o.sortColumn} ${o.sortOrder}, cs.id ${o.sortOrder}` (repository.ts:233).
    `sortColumn` is whitelisted: `CASE_PAGE_SPEC.sortMap` (service.ts:14-22) maps apiField→fixed SQL literal
    {caseNumber→cs.case_number, status→cs.status, clientName→cl.name, productName→p.name, primaryName→pa.name,
    createdAt→cs.created_at, updatedAt→cs.updated_at}; `resolvePage` (pagination.ts:51-55) accepts `sortBy`
    ONLY via `hasOwnProperty(sortMap)` else `defaultSort`, then `sortColumn = sortMap[sortBy]` (literal, never
    user input); `sortOrder` hard-coerced asc/desc (pagination.ts:57-59). Every sort target column is present
    in the items SELECT's JOINs: `cl.name`/`p.name` via the inner `JOIN clients cl`/`JOIN products p`
    (repository.ts:46-47), `pa.name` via `LEFT JOIN case_applicants pa ... is_primary` (repository.ts:48),
    `cs.*` on the base table. The `cs.id` uuid tiebreak is the PK (0010:10) → strictly deterministic ordering
    (no dup-page/skipped-row across OFFSET windows). NOTE: tiebreak inherits `o.sortOrder` (matches the
    established clients/products/users/locations pattern — fine, deterministic either direction).
  - (c) ✅ PARAMS/LIMIT/OFFSET — filters fill `$1..$N` in order (status, clientId, search), then
    `LIMIT $${params.length+1} OFFSET $${params.length+2}` with `[...params, o.limit, o.offset]`
    (repository.ts:230-232,234). COUNT reuses the same `params` array WITHOUT the limit/offset tail
    (repository.ts:218-221) — correct (count takes no LIMIT). `limit` clamped ≤500 / ≥1 in resolvePage
    (pagination.ts:41-42). clientId server-validated `Number.isInteger && >0` (service.ts), status validated
    against `CASE_STATUSES` allowlist (service.ts) before reaching SQL — both also bound via `$n` regardless.
  - (d) N+1 — items SELECT carries 2 correlated subqueries per row: `applicant_count`
    (`count(*) FROM case_applicants a WHERE a.case_id=cs.id`, repository.ts:43) + `task_count`
    (`count(*) FROM case_tasks ct WHERE ct.case_id=cs.id`, repository.ts:44). This is bounded N+1 within ONE
    SQL statement (NOT app-level round-trips) and is now CAPPED by the page LIMIT (≤500, default likely 50) —
    previously ran over up-to-200 rows unbounded; pagination strictly improves it. Both subqueries are
    index-backed: `idx_applicants_case ON case_applicants(case_id)` (0010:48) and `idx_case_tasks_case ON
    case_tasks(case_id)` (0010:70). ACCEPTABLE. (Could fold to LATERAL/GROUP-BY aggregate later if it ever
    shows in EXPLAIN — not warranted at current row counts.)
  - (e) ✅ NO MIGRATION NEEDED — all cases tables pre-exist (0010); no schema change. Index posture for the
    new pagination paths:
    · default `ORDER BY cs.created_at DESC, cs.id` — **NO index on cases.created_at** (0010 indexes only
      client_id + status; cases.id is PK). At today's tiny `cases` row count → seq-scan + in-mem sort is fine.
    · search `pa.name ILIKE '%q%'` — `idx_applicants_name ON case_applicants(lower(name))` (0010:49) does NOT
      serve a leading-wildcard ILIKE (btree on a functional expr can't, and ILIKE bypasses lower()-index) →
      seq-scan on case_applicants for search. Tiny now; same trgm story as locations if it grows.
    · subquery FK joins already covered (idx_applicants_case, idx_case_tasks_case).
  - RATCHET (carried, non-blocking): **before `cases` grows operationally**, add `idx_cases_created_at ON
    cases(created_at DESC)` (or composite `(created_at DESC, id)` for an index-ordered LIMIT like the locations
    `(pincode,id)` pattern) to back the default sort, and consider `gin(name gin_trgm_ops)` on case_applicants
    for the `pa.name` search path (mirrors 0020 locations). `cs.client_id`/`cs.status` filters already indexed
    (0010:29-30). Cases is small today → seq-scan acceptable; raise this the moment cases volume climbs or a
    sort/search-heavy cases workspace lands.
  - DEFER NOTE: non-default sort columns on `cases` (case_number/status/client name via cl.name/product via
    p.name/primaryName via pa.name/updated_at) are unindexed for ordering — same low-priority posture as the
    locations non-default-sort note; revisit only if a sort becomes hot.

- **2026-06-06 · DataGrid Slice 2 — locations[157k] + migration 0020 (uncommitted)** —
  VERDICT **PASS**. The locations list endpoint converted to `Paginated<Location>` (COUNT + LIMIT/OFFSET,
  whitelisted ORDER BY) PLUS new index migration `db/v2/migrations/0020_locations_search_indexes.sql`.
  This **RESOLVES my carried Slice-1 ratchet** ("must be indexed before locations[157k]").
  - (a) MIGRATION forward-only + idempotent: `CREATE EXTENSION IF NOT EXISTS pg_trgm` (0020:10),
    `CREATE INDEX IF NOT EXISTS` ×5 (0020:12-15,19). No `BEGIN/COMMIT` wrapper (unlike 0004) — fine, each
    stmt autocommits; note **non-CONCURRENTLY** so it takes a SHARE lock building 4 GIN on 157k rows
    (acceptable: dev tool, applied offline; flag only if ever run on a live-write prod). Re-runnable.
  - (b) TRGM indexes back the global search: `gin (<col> gin_trgm_ops)` on all four
    pincode/area/city/state (0020:12-15) — exactly the cols in the repo's 4-col OR
    (`repository.ts:36-38`: `pincode/area/city/state ILIKE $n`). EXPLAIN on the **live 157072-row dev DB**
    proves COUNT(search='karnataka') → `BitmapOr` across all 4 `*_trgm` Bitmap Index Scans (intended plan,
    no seq scan). `gin_trgm_ops` is the correct opclass for `ILIKE '%term%'` (leading wildcard).
  - (c) btree `(pincode, id)` (`idx_locations_pincode_sort`, 0020:19) backs the default
    `ORDER BY pincode, id` (repo `o.sortColumn`=`pincode`, tiebreak `id`): EXPLAIN → **Index Only Scan**,
    LIMIT 50 OFFSET 0 cost 0.42..1.95. Composite (pincode,id) matches both sort keys → no separate sort node.
  - (d) NO bloat/redundancy vs pre-existing (idx_locations_pincode partial WHERE is_active · idx_locations_state_city
    partial · idx_locations_effective_from · uq_locations(pincode,area) · pkey): the new sort idx is
    **un-partial** on purpose — the admin list has NO is_active filter so the partial idx_locations_pincode
    can't cover it (comment 0020:17-18 is correct). Trgm GIN is a distinct access method, no overlap. 10 total
    indexes on locations now, all justified by a query path.
  - (e) REPO correctness (`repository.ts:23-57`): COUNT WHERE == items WHERE (one `clause`/`params` built
    once at :41-43, reused for both COUNT :45-47 and items :51-56). `count(*)::int` cast (:45). LIMIT/OFFSET
    param numbering correct: filters fill `$1..$N`, then `LIMIT $${params.length+1} OFFSET $${params.length+2}`
    with `[...params, o.limit, o.offset]` (:53-54). Deterministic `id` tiebreak (:52). **sortColumn is ONLY ever
    the whitelisted literal**: `LOCATION_PAGE_SPEC.sortMap` (service.ts:8-21) → `resolvePage` accepts sortBy
    only via `hasOwnProperty(sortMap)` else defaultSort, `sortColumn = sortMap[sortBy]` literal
    (pagination.ts:51-55); sortOrder hard-coerced asc/desc (:57-59). Injection test
    `sortBy=pincode;DROP TABLE locations` → falls back to `pincode` (test :131-138). Active/pincode parsed
    in service (service.ts:30-31), echoed into `filters` (:42-46).
  - (f) ✅ TRIPLE-WRITE CONFIRMED: file is the 3rd leg + applied to BOTH DBs (CTO pre-applied). Verified live —
    dev :54329/crm2_dev: pg_trgm + all 5 new indexes present, 157072 rows; test :5433/crm2_test: pg_trgm +
    all 5 present (1 row). (Note: dev DB is named `crm2_dev`, test-harness DB is `crm2_test` — both have it.)
  - DEEP-OFFSET reasoning: OFFSET 100000 still **Index Only Scan** cost ~3052 (sub-ms) — acceptable for admin
    browsing; search-first is the norm and the search path is index-backed too. No keyset pagination needed at this scale.
  - SHOULD-FIX / NOTE (carried, non-blocking): non-default sorts (area/city/state/country/status/effectiveFrom/
    createdAt/updatedAt) have NO covering index → a 157k sort falls back to seq-scan + sort when the user picks a
    non-pincode column. idx_locations_state_city (partial WHERE is_active) only partly helps state/city in the
    no-filter admin list. Acceptable now (sort-by-pincode is the default/dominant path; others are rare admin
    actions); add per-column sort indexes only if a non-pincode sort becomes hot. Slice-1 ratchet for OTHER tables
    (users/VU/templates sortable cols) still **OPEN** — those tables are small, GA-deadline only.
  - RATCHET RESOLVED: locations(157k) index ratchet from Slice-1 → **CLOSED** (0020 + EXPLAIN-proven on live 157k).

- **2026-06-06 · DataGrid Slice 4 (FINAL) — rates list server-pagination + global search (uncommitted)** —
  VERDICT **PASS** (1 proportionate perf RATCHET, non-blocking). `rates/repository.ts` `list()` → COUNT +
  paginated SELECT over a shared `RATE_FROM` const; `rates/service.ts` `list()` now resolves via
  `RATE_PAGE_SPEC`/`resolvePage`/`buildPage`, returns `Paginated<RateView>`; controller collapsed to pass
  `req.query` straight through (controller.ts: old `parsePositiveInt` block deleted, coercion moved to service
  via `toPosInt`). No migration (code-only — `git diff --stat db/v2/migrations` empty; rates schema 0003/0012/
  0013/0015/0019 unchanged).
  - (a) ✅ COUNT CORRECTNESS — `RATE_FROM` (repository.ts:16-20) is shared VERBATIM by COUNT (`SELECT count(*)::int
    ${RATE_FROM} ${clause}`, :67) and items (:80), with the SAME `clause`/`params` built once (:46-65). The three
    INNER joins are `NOT NULL REFERENCES` FKs — `client_id`/`product_id`/`verification_unit_id NOT NULL REFERENCES`
    (0003_rates.sql:11-13) ⇒ strictly 1:1, no row multiplication; `LEFT JOIN locations l ON l.id=r.location_id`
    matches ≤1 row on the locations PK ⇒ no multiplication either. So `count(*)` == #rate rows matching the filter.
    KEY POINT: the search predicate references JOINED columns (`c.name/c.code/p.name/p.code/vu.name/l.pincode/
    l.area`, :60) — these resolve in COUNT precisely BECAUSE COUNT carries the full join set (not the bare `FROM
    rates`). Joins-in-COUNT is mandatory here and is present. ✅
  - (b) ✅ INJECTION clean. ORDER BY interpolates ONLY `${o.sortColumn} ${o.sortOrder}` (:82). `sortColumn` comes
    from `RATE_PAGE_SPEC.sortMap` (service.ts) — apiField→fixed qualified literal {client→c.name, product→p.name,
    kind→vu.kind, unit→vu.name, pincode→l.pincode, area→l.area, rateType→r.rate_type, amount→r.amount,
    effectiveFrom→r.effective_from, status→r.is_active, createdAt→r.created_at, updatedAt→r.updated_at} — resolved
    via `resolvePage` hasOwnProperty-whitelist else `defaultSort` (pagination.ts:51-55, never user input);
    `sortOrder` hard-coerced asc/desc (:57-59). Test `sortBy=r.amount;DROP TABLE rates` → 200, `sort.sortBy='client'`
    (default, not the injection string). SEARCH parameterized: ONE `%${o.search}%` pushed once at `n=params.length`
    (:57-58) then reused across all 8 ILIKE clauses via `$${n}` (:60) — bound value, single param, no concat. ✅
  - (c) ✅ AMOUNT SORT — `amount → r.amount` (RAW numeric col) in sortMap; the SELECT aliases `r.amount::float8 AS
    amount` (:74) but ORDER BY targets the raw `r.amount` → numeric (not text) ordering. Test asserts amount asc →
    `[10,20]` page1, `[30]` page2. ✅
  - (d) ✅ EFFECTIVE-DATED preserved — `if (!o.history) where.push((r.effective_to IS NULL OR r.effective_to >
    now()))` (:64) byte-identical to prior; `history=true` includes superseded (test: current view=1 row, history
    view=2 rows). GiST `rates_no_overlap` exclusion + `revise` (end-date-then-insert, :128-176) + `updateAmount`
    (OCC :179-191) + `setActive` (:194-215) + `rate_history` writes + `history()` (:218-234) ALL UNTOUCHED — diff
    has zero edits below `list()`. ✅
  - (e) ✅ PARAM NUMBERING — `add()` helper fills filters `$1..$N` (client/product/vu/active, :52-55, still numbers
    via `params.push` then `$${params.length}`), THEN search at `$${params.length}` (:57-60), THEN
    `LIMIT $${params.length+1} OFFSET $${params.length+2}` with `[...params, o.limit, o.offset]` (:82-84).
    `params.length` is read AFTER search is conditionally pushed, so LIMIT/OFFSET indices are correct WITH OR
    WITHOUT search present (the off-by-one trap is avoided). COUNT reuses `params` WITHOUT the limit/offset tail
    (:68) — correct (count takes no LIMIT). `limit` clamped ≤500/≥1 in resolvePage (test: `limit=501`→400). `r.id`
    tiebreak (integer IDENTITY PK, 0003:10) inheriting `o.sortOrder` → strictly deterministic, no dup/skip across
    OFFSET windows. ✅
  - (f) INDEX POSTURE — default `ORDER BY c.name ASC, r.id` and most sort columns (c.name/p.name/vu.*/l.*/
    rate_type/amount/effective_from/is_active/created_at/updated_at) are UNINDEXED for ordering (rates has only
    uq_rates(client,product,vu) + idx_rates_client_product partial + idx_rates_resolve partial). The 8-col
    leading-wildcard `ILIKE '%term%'` global search (c/p/vu/l/rate_type) → seq-scan (no trgm). RATCHET
    (PROPORTIONATE, non-blocking): rates is a SMALL config table (one row per client×product×unit, plus
    superseded versions) — NOT locations(157k). Seq-scan + in-mem sort is fine at this scale; do NOT add trgm/
    sort indexes prophylactically. Re-raise ONLY if rates volume climbs materially or a sort/search-heavy rate
    workspace lands. Deliberately NOT mirroring the 0020 locations trgm decision (size-disproportionate).
  - OPEN (carried): Slice-1 ratchet for users/VU/templates sortable cols still open (small tables, GA-deadline
    only). With Slice 4 the DataGrid/server-pagination rollout is COMPLETE across clients/products/users/VU/
    templates/locations/cases/rates; no list endpoint left on the legacy array shape on the DB side.

- **2026-06-06 · Slice 1A — cases-growth index ratchet + migration 0021 (migration-only, no code)** —
  VERDICT **PASS**. New `db/v2/migrations/0021_cases_growth_indexes.sql` adds 4 indexes for the cases
  DataGrid list access paths. This **RESOLVES my carried cases ratchet** from Slice 3 (lines 101-106:
  "before `cases` grows operationally, add idx_cases_created_at … and consider gin trgm on case_applicants").
  Mirrors the 0020 locations pattern, size-appropriately scoped to the actually-searched/sorted columns.
  - (a) ✅ MIGRATION SHAPE — forward-only + idempotent: `CREATE EXTENSION IF NOT EXISTS pg_trgm` (0021:21),
    `CREATE INDEX IF NOT EXISTS` ×4 (0021:13,17,22,23). No BEGIN/COMMIT wrapper (each stmt autocommits —
    matches 0020). Non-CONCURRENTLY ⇒ SHARE lock while building (acceptable: dev tool, applied offline;
    flag only if ever run against a live-write prod cases table, which by then will be large). Re-runnable
    (verified triple-write: idempotent re-run skips).
  - (b) ✅ DEFAULT-SORT INDEX matches the query EXACTLY. Repo default sort is `ORDER BY cs.created_at DESC,
    cs.id DESC` — derived from `CASE_PAGE_SPEC.defaultSort='createdAt'`/`defaultOrder='desc'` (service.ts:28,
    31-32) → `o.sortColumn='cs.created_at'`, `o.sortOrder='desc'`, and the tiebreak `cs.id` inherits
    `o.sortOrder` (repository.ts:233). The new `idx_cases_created_at ON cases (created_at DESC, id DESC)`
    (0021:13; live def confirmed `btree (created_at DESC, id DESC)`) is a composite whose BOTH keys + BOTH
    directions match the ORDER BY ⇒ planner reads the first page index-ordered, no sort node, and the
    `id DESC` 2nd key makes the keyset strictly deterministic across OFFSET windows. `cases.id` is uuid PK
    (0010:10) so `(created_at DESC, id DESC)` is a total order. Correct construction.
  - (c) ✅ PRODUCT FK INDEX — items SELECT does `JOIN products p ON p.id = cs.product_id` (repository.ts:47).
    `cases.product_id` is `NOT NULL REFERENCES products` (0010:13) — was UNINDEXED on the cases side
    (0010 only indexed client_id + status). `idx_cases_product ON cases (product_id)` (0021:17) backs the
    nested-loop/hash-join lookup as cases grows. Mirrors the pre-existing `idx_cases_client`. Correct.
    NOTE: this FK index also serves the (currently absent) `productId` list filter if one is ever added —
    no filter on product today, but the JOIN alone justifies it.
  - (d) ✅ SEARCH TRGM INDEXES — global search is `(pa.name ILIKE $n OR cs.case_number ILIKE $n)` with
    `$n = '%'||search||'%'` (repository.ts:217-219). Leading-wildcard ILIKE ⇒ btree useless; `gin_trgm_ops`
    is the correct opclass. `idx_cases_case_number_trgm` on `cases(case_number gin_trgm_ops)` (0021:22) +
    `idx_applicants_name_trgm` on `case_applicants(name gin_trgm_ops)` (0021:23) — exactly the two searched
    columns. One per column ⇒ planner can BitmapOr (search hits BOTH the cases base and the LEFT-JOINed
    primary applicant). Live: both confirmed `gin (… gin_trgm_ops)` on `character varying` cols (trgm
    supports text/varchar). The pre-existing `idx_applicants_name` is btree on `lower(name)` (0010:49,
    equality/dedupe) — DISTINCT access method, NO redundancy with the new GIN. Correct, and the dedupe
    path's `upper(a.name) =` equality (repository.ts:87) is unaffected.
  - (e) ✅ NO REDUNDANCY / BLOAT — full live index inventory: cases now {pkey, uq_cases_number,
    idx_cases_client, idx_cases_status, +idx_cases_created_at, +idx_cases_product, +idx_cases_case_number_trgm};
    case_applicants {pkey, uq_case_one_primary, idx_applicants_case, idx_applicants_mobile, idx_applicants_name,
    idx_applicants_pan, +idx_applicants_name_trgm}. Every new index maps 1:1 to a distinct access path
    (default sort / product JOIN / case_number search / name search). No overlap with any existing index.
  - (f) ✅ TRIPLE-WRITE CONFIRMED — file (leg 1) + dev `crm2_dev@:54329` + test `crm2_test@:5433` (leg 2+3):
    all 4 indexes present in BOTH DBs (re-verified live this session, not trusting the handoff). Idempotent
    re-run skips. Chain: 0021 is sequenced after 0020, NO duplicate migration number (0005 gap is pre-existing
    historical, not introduced here; harmless for the file-based forward-only runner).
  - (g) CTO DECISIONS — ALL 3 CONFIRMED, agree:
    · `count(*) OVER()` WONTFIX → AGREE. The list deliberately runs a LEAN separate COUNT
      (`count(*)::int FROM cases cs LEFT JOIN case_applicants pa … is_primary ${clause}`, repository.ts:224-226)
      that carries ONLY the cs+primary-applicant join (needed for the `pa.name` search predicate) and OMITS
      the JOIN clients/products + the two correlated subqueries (applicant_count/task_count) of the items
      SELECT. `count(*) OVER()` would glue the count to the heavy per-row item plan, forcing
      clients/products joins AND both subqueries to execute over the full filtered set just to compute a
      total. The lean COUNT is the correct, cheaper choice. (Row-equivalence of the lean COUNT vs items was
      proven in my Slice-3 entry (a): partial-unique uq_case_one_primary ⇒ LEFT JOIN ≤1 row/case ⇒ no
      multiplication; dropped FK joins are 1:1 NOT-NULL so they neither drop nor multiply.) ✅
    · LATERAL/grouped counts WONTFIX → AGREE. applicant_count/task_count are correlated `count(*)` subqueries
      (repository.ts:43-44) index-backed by `idx_applicants_case` + `idx_case_tasks_case` (0010:48,70). They
      now run only over the page (LIMIT ≤500, default 50) — bounded within ONE SQL statement, sub-ms per row
      on an index lookup. Rewriting to LATERAL/GROUP-BY is premature; warranted only if EXPLAIN ever shows
      these dominate. Correct call. ✅
    · Small-table sort indexes (users/VU/templates) defer-by-design → AGREE this is the RIGHT call, NOT a
      gap. Those are tiny reference tables (users tens · VU hundreds · templates ~180); the planner picks
      seq-scan + in-memory sort over an index at that cardinality, so a sort index would be pure write/
      maintenance overhead with zero read benefit. Indexing the UNBOUNDED operational table (cases) while
      leaving bounded reference tables un-indexed is exactly the size-proportionate posture (same logic I
      applied declining trgm on the small `rates` table in Slice 4). ✅ (My standing Slice-1 ratchet for
      those cols remains OPEN as a GA-deadline tidy-item, not a perf necessity — consistent with this.)
  - EXPLAIN caveat: dev/test DBs hold 3 cases / 3 applicants, so the planner won't choose the new indexes
    there (seq-scan wins at n=3) — index SELECTION can't be EXPLAIN-proven at this size, but the index
    DEFINITIONS provably match the access paths (verified live above) and will be chosen once cases volume
    climbs. Unlike Slice 2 (locations had 157k rows to prove the plan), cases is genuinely small today — this
    is a PROACTIVE ratchet ahead of growth, which is the correct time to add growth indexes (before, not
    after, the table is big enough to need a CONCURRENTLY rebuild).
  - RATCHET RESOLVED: the cases-growth index ratchet I carried from Slice 3 (created_at sort + applicant
    name trgm) is now **CLOSED** by 0021, plus the product-FK join index. DEFER NOTE still stands: non-default
    cases sorts (case_number/status/clientName via cl.name/productName via p.name/primaryName via pa.name/
    updatedAt) remain unindexed for ORDERING — low priority, add only if a non-default sort becomes hot
    (same posture as the locations non-default-sort note). DISPOSITION: APPROVED, merge-ready.
  - OPEN (carried): Slice-1 users/VU/templates sortable-col indexes — confirmed defer-by-design above
    (no longer a perf concern; GA cosmetic tidy only).

- **2026-06-06 · Slice — Server-side column-filter contract (`resolveFilters` + clients/products `columnFilters`)** — **VERDICT: PASS.**
  - (a) ✅ **COUNT/items NO DIVERGENCE — filters applied to BOTH.** The repo builds `where[]`/`params[]` ONCE (clients repository.ts:26-43, products :29-43), appends the column-filter predicates BEFORE forming `clause`, then runs the lean COUNT (`SELECT count(*)::int … ${clause}`, clients:45-48) and the items SELECT (`… ${clause} ORDER BY … LIMIT/OFFSET`, clients:51-57) over the SAME `clause` + SAME `params` array. Items only APPENDS `limit`/`offset` as `$N+1`/`$N+2` after the shared params (`[...params, o.limit, o.offset]`). So every column filter constrains COUNT and items identically — totalCount is honest for the filtered set, no count/items skew.
  - (b) ✅ **AND-combined, parameterized.** Filters `where.push` with `' AND '`-join (clause builder unchanged) — each is `${f.column} ILIKE|= $N` with the value `params.push`ed (`%v%` for ilike, raw for eq) → bound, never interpolated. Combines correctly with the pre-existing `active`/`effective_from`/`search` predicates (same `where[]`).
  - (c) ✅ **No N+1 / no extra round-trip.** Still exactly 2 queries per list call (COUNT + items) — column filters add WHERE predicates to the existing pair, zero new queries, no per-row work. `f.column` = whitelisted literal (service filterMap), no dynamic table/join introduced.
  - (d) ✅ **Filterable columns exist + are sane.** clients/products filter on `code`,`name` — both real NOT-NULL varchar columns in those tables (0002), already projected. enum kind (`is_active`-style) eq against a value set — bound, no cast hazard (the contract's only live enum is in the unit-test spec; clients/products ship text-only filters).
  - (e) ✅ **No migration, no schema change** — pure query-builder addition. Triple-write invariant N/A (no DDL).
  - INDEX/perf note → see Performance ledger: leading-wildcard ILIKE on clients/products `code`/`name` is a sub-ms seq-scan at this cardinality (dozens of rows); no index needed now. RATCHET when this contract rolls to large tables (cases/locations) — those filterable cols need trigram GIN (precedent 0020/0021).
  - OPEN (carried): when column-filtering reaches cases/locations, ensure the filter predicates are added to BOTH the items and COUNT query (the shared-`where` pattern here makes that automatic — preserve it) AND that any user/territory row-scope WHERE is applied to both, else filtered totalCount leaks out-of-scope existence.

- **2026-06-06 · B-4 Excel-style header multi-select (§7) — `filterClauses` centralization + enum `IN`** — **VERDICT: PASS.**
  - (a) **`= ANY($n)` is the correct/idiomatic IN over the `kind` text column** — node-postgres binds the JS string[] as a typed text[] array param; planner evaluates `kind = ANY($1)` equivalently to `kind IN (...)`. No `::array` cast needed in the SQL (the comment's `::array` is illustrative only; actual code emits bare `= ANY($n)` which is correct — verified `pagination.ts:90`).
  - (b) **Filters applied to BOTH count + items** — `filterClauses(o.columnFilters, params)` pushes onto the SAME `params` array and its fragments join the SAME `where`, which feeds BOTH the `count(*)` query and the items query in all three repos (clients `repository.ts`, products `repository.ts`, VU `repository.ts:55-73`). Items query appends limit/offset at `$params.length+1/+2` AFTER filter binds — **param-index correctness preserved**; the shared-array push pattern makes count/items binding automatically consistent.
  - (c) **Refactor is behavior-preserving** — old inline loop (`f.op==='ilike' ? '%v%' : v` + `ILIKE/=`) is exactly reproduced by `filterClauses` for the ilike/eq paths; the only NEW behavior is the `in` branch (multi-value). clients/products gained NO enum filterMap, so their text-ILIKE behavior is byte-identical, just routed through the helper.
  - (d) ✅ **No migration, no schema change** — pure query-builder refactor + one `filterMap` literal added to VU service. Triple-write invariant N/A (no DDL).
  - OPEN (carried, unchanged): when column-filtering reaches cases/locations, preserve shared-`where` so filter predicates AND row-scope WHERE hit both items+count (filtered-totalCount existence leak); text filter cols there need trigram GIN (see Performance).

- **2026-06-06 · Slice — Column-filter rollout to the 5 remaining lists (users · report-templates · locations · rates · cases)** — **VERDICT: PASS.**
  - **THE CRITICAL AUDIT — count/items FROM-parity confirmed PER MODULE (every filterMap column resolves in the COUNT FROM, no count/items divergence):**
    · **users** — COUNT `FROM users u` (repository.ts:75, no manager join). filterMap = `u.username`/`u.name`/`u.role` (service.ts:32-35) — ALL on `u`. reportsTo (`m.name`) correctly NOT filterable (the `users m` self-join exists only in the items SELECT). ✅
    · **report-templates** — COUNT `FROM report_templates` (repository.ts:63). filterMap = `code`/`name`/`template_type` (service.ts:26-29) — single table. ✅
    · **locations** — COUNT `FROM locations` (repository.ts:51). filterMap = `pincode`/`area`/`city`/`state` (service.ts:21-25) — single table. ✅
    · **rates** — COUNT `${RATE_FROM}` (repository.ts:72) where `RATE_FROM` (repository.ts:17-21) carries `JOIN verification_units vu` + `LEFT JOIN locations l`. filterMap = `vu.kind`/`vu.name`/`l.pincode`/`l.area`/`r.rate_type` (service.ts:33-38) — ALL resolve in RATE_FROM (count and items share the identical const). The `LEFT JOIN l` is the only nullable join; an `l.pincode/l.area` filter naturally excludes location-null rows in BOTH count and items identically → no divergence. ✅
    · **cases** — COUNT `FROM cases cs LEFT JOIN case_applicants pa … AND pa.is_primary` (repository.ts:230-231). filterMap = `cs.case_number`/`pa.name`/`cs.status` (service.ts:33-37) — ALL on cs/pa. `clientName`(`cl.name`)/`productName`(`p.name`) correctly NOT filterable (those joins live only in the heavy items SELECT, not the lean COUNT). One-primary partial-unique ⇒ LEFT JOIN pa never multiplies ⇒ `pa.name` filter is row-honest in COUNT. ✅ (test asserts `f_primaryName=alpha → totalCount=1`, cases.api.test.ts:132-135.)
  - (a) ✅ **Filters applied to BOTH count + items in all 5.** Each repo builds `where[]`/`params[]` ONCE, then `where.push(...filterClauses(o.columnFilters ?? [], params))` BEFORE forming `clause` (cases:225, locations:47, rates:66, templates:59, users:71). The same `clause`+`params` feed the COUNT and the items query; items only appends `limit`/`offset` as trailing `$N+1`/`$N+2`. The shared-`where` pattern (carried-OPEN from prior slices) is PRESERVED — count/items binding is automatically consistent. No new query, still exactly 2 per list call.
  - (b) ✅ **AND-combined, parameterized, contract reused unchanged.** `filterClauses`/`resolveFilters`/`AppliedFilter` (pagination.ts:55-96) are the EXACT helpers audited in the clients/products + B-4 slices (no edit to pagination.ts in this diff — confirmed name-only). enum→`= ANY($n)` (typed array param), text→`ILIKE %v%` bound. Combines with existing `search`/`status`/`active`/`clientId`/`history`/`pincode` predicates via the same `where[]`.
  - (c) ✅ **Enum domains match schema.** CASE_STATUSES (cases.ts:9), USER_ROLES (users.ts:11), REPORT_TEMPLATE_TYPES (verificationUnit.ts:13 = `['FIELD_NARRATIVE','KYC_DOCUMENT']` — matches DB `chk_report_template_type`, 0008:22), KINDS (verificationUnit.ts:8) — all imported, all `readonly` literal tuples; out-of-set values dropped by `resolveFilters` before SQL.
  - (d) ✅ **No migration, no schema change** — pure query-builder additions (one `filterMap` literal + one `resolveFilters` call + one `columnFilters` field + one echo loop per module). Triple-write invariant N/A (no DDL). Index adequacy → Performance ledger.
  - **RATCHET CLOSED (the one I gated in the clients/products + B-4 slices):** I required that before any cases/locations filterMap merges, every leading-wildcard text-filter column be paired with a trgm GIN index. **locations** pincode/area/city/state are ALL trgm-GIN-backed (0020:12-15) ✅; **cases** case_number trgm (0021:22) + applicant name trgm (0021:23) back `cs.case_number`/`pa.name` ✅. rates joined text cols (`vu.name`/`l.pincode`/`l.area`/`r.rate_type`) are on small tables (VU dozens, rates small) — index-free by design. The gate is SATISFIED; ratchet RESOLVED.
  - OPEN (carried, unchanged): cases LIST still has NO row-level scope (pre-existing, sibling of B-20) — when row-scope lands it must hit BOTH count+items (shared-`where` makes this automatic; preserve) else filtered totalCount leaks out-of-scope existence. The new column filters do NOT widen this (they only narrow). DISPOSITION: APPROVED, merge-ready.

- **2026-06-06 · CPV effective-from reschedule edit — `updateEffectiveFrom` on both CPV sub-repos** — **VERDICT: PASS.**
  - (a) ✅ **OCC guard correct — mirrors the proven clients/users/setActive pattern (ADR-0019).** Inside `withTransaction(q)`: pre-read `before` (`SELECT ${CP_COLS|CPV_COLS} … WHERE id=$1`) → if absent `AppError.notFound('CLIENT_PRODUCT_NOT_FOUND'|'CPV_UNIT_NOT_FOUND')` (404). Then `UPDATE … SET effective_from=$2, version=version+1, updated_at=now() WHERE id=$1 AND version=$3 RETURNING …` → if 0 rows `AppError.stale(before)` (409 STALE_UPDATE). The pre-read `before` is what disambiguates 404 (row gone) vs 409 (row exists but version moved) — identical shape to `setActive` (repository.ts cp:137-141 / cpv) and the clients/users OCC. version+1 monotonic bump, WHERE-on-version is the compare-and-set. Confirmed.
  - (b) ✅ **Audit append in the SAME tx, action 'UPDATE'.** `appendAudit({entityType:'client_products'|'client_product_verification_units', entityId:id, action:'UPDATE', actorId, before, after:row, versionAfter:row.version}, q)` is called with the transaction's `q` (audit.ts:24 `appendAudit(entry, q)`) so the audit row commits atomically with the UPDATE. `'UPDATE'` ∈ `AuditAction` (`'CREATE'|'UPDATE'|'DEACTIVATE'|'ACTIVATE'`, audit.ts:2) which mirrors the audit_log CHECK (0017). before/after captured for the field-level diff. Append-only (DB rejects UPDATE/DELETE on audit_log).
  - (c) ✅ **No DDL / no migration / no schema change** — pure repo method addition on existing columns (`effective_from`/`version`/`updated_at` all pre-exist in both tables). Triple-write invariant N/A (no DDL).
  - (d) ✅ **No key column mutated.** SET touches only effective_from/version/updated_at — client_id/product_id (CP) and client_product_id/verification_unit_id (CPV) untouched, so no unique-key/FK churn, no constraint risk. The reschedule cannot create a duplicate (keys fixed). USABLE semantics (ADR-0017 `effective_from<=now()`) honored downstream — test confirms future-dating excludes from `?active=true`.
  - DISPOSITION: APPROVED, merge-ready. No new index needed (PK-keyed single-row UPDATE).

- **2026-06-06 · ADR-0020 correctable identity keys (clients + products `code`) — VERDICT: PASS.**
  - (a) ✅ **`hasDependents` covers ALL live referencing tables — no integrity hole.** Exhaustive FK scan of `db/v2/migrations` (`grep -rniE 'REFERENCES (public.)?(clients|products)\('`): the ONLY tables with a `client_id`/`product_id` FK to clients/products are **`client_products`** (0002:28-29), **`rates`** (0003:11-12), **`cases`** (0010:12-13), plus `rate_type_eligibility` (0012:29-30) and `service_zone_rules` (0012:46-47) — and BOTH of the latter were `DROP TABLE … CASCADE`d in 0013_rate_management_flatten.sql:39-41. `rate_history` (0012:87) references `rates(id)`, NOT clients/products, so it is covered transitively through `rates`. Therefore the live referencing set = exactly {client_products, rates, cases}, which is precisely what `hasDependents` checks (repository.ts clients:104-110 by client_id; products:104-110 by product_id). No missed table → a referenced code can never be silently changed.
  - (b) ✅ **OCC guard intact on the rekey path.** `updateRow` keeps the proven pattern: inside `withTransaction(q)`, `UPDATE … WHERE id=$1 AND version=$6` → 0 rows → re-SELECT → 404 CLIENT/PRODUCT_NOT_FOUND or `AppError.stale(current)` (409). version `+1` compare-and-set unchanged from `updateName`.
  - (c) ✅ **Audit append in the SAME tx.** `appendAudit({entityType:'clients'|'products', action:'UPDATE', before, after:row, versionAfter}, q)` called with the tx `q` → commits atomically. A code correction is captured before/after in the audit chain (append-only).
  - (d) ✅ **Unique-violation mapped, no DDL, no migration.** The rekey can collide on the `code` unique index → `isUniqueViolation` (SQLSTATE 23505) → `409 CLIENT_CODE_EXISTS`/`PRODUCT_CODE_EXISTS` (repository.ts clients:155 / products:96). Pure repo-method change on the existing `code` column (already UNIQUE, NOT NULL); no schema change → triple-write invariant N/A. COALESCE keeps NOT NULL satisfied (never writes NULL code).
  - DISPOSITION: APPROVED, merge-ready. No new index needed (PK-keyed single-row UPDATE; the three EXISTS probes hit the existing FK-side indexes on client_id/product_id).

- **2026-06-06 · ADR-0020 rollout to report_templates · locations · users (`code`/`pincode`/`username` correctable) — VERDICT: PASS.**
  - **THE CRITICAL AUDIT — renumbered UPDATE `$N` ↔ params array traced for ALL 3, EXACT match:**
    · **locations** (repository.ts:163-177) — SQL `SET pincode=COALESCE($2,pincode), area=$3, city=$4, state=$5, country=$6, effective_from=COALESCE($7::timestamptz,…), version=version+1, updated_by=$8, … WHERE id=$1 AND version=$9`. Params array order: `id`($1), `input.pincode ?? null`($2), `area`($3), `city`($4), `state`($5), `country`($6), `effectiveFrom ?? null`($7), `userId`($8), `expectedVersion`($9). **9/9 aligned.** ✅
    · **report_templates** (repository.ts:128-141) — SQL `SET code=COALESCE($2,code), name=$3, template_type=$4, content=$5, effective_from=COALESCE($6::timestamptz,…), version=version+1, updated_by=$7, … WHERE id=$1 AND version=$8`. Params: `id`($1), `input.code ?? null`($2), `name`($3), `templateType`($4), `content`($5), `effectiveFrom ?? null`($6), `userId`($7), `expectedVersion`($8). **8/8 aligned.** ✅
    · **users** (repository.ts:156-170) — SQL `SET username=COALESCE($2,username), name=$3, email=$4, role=$5, reports_to=$6, effective_from=COALESCE($7::timestamptz,…), version=version+1, updated_by=$8, … WHERE id=$1 AND version=$9`. Params: `id`($1, uuid str), `input.username ?? null`($2), `name`($3), `email`($4), `role`($5), `reportsTo`($6), `effectiveFrom ?? null`($7), `userId`($8), `expectedVersion`($9). **9/9 aligned.** ✅ No off-by-one, no shared-bind reuse across SET-assign + literal-compare (the SEV-1 class from the backend-review hotfix is absent — each col binds a distinct `$N`).
  - **COALESCE($2, key) is wipe-safe in all 3.** Service computes `keyChanged` and passes the key ONLY when changed; otherwise the key field is `undefined` → repo `key ?? null` → `$2=NULL` → `COALESCE(NULL, key)` keeps the stored value. An omitted key cannot null/wipe the column. (locations/service.ts:73 spreads `{pincode: undefined}` on no-change; templates/service.ts:89 omits the field; users/service.ts:101 omits.) Confirmed no accidental wipe.
  - **OCC guard + in-tx audit intact in all 3.** Each keeps `withTransaction(q)` → UPDATE `WHERE id=$1 AND version=$N` (version+1 compare-and-set) → 0 rows → re-SELECT → 404 `*_NOT_FOUND` or `AppError.stale(current)` (409) → `appendAudit({entityType, action:'UPDATE', before, after:row, versionAfter:row.version}, q)` committed atomically (append-only). Byte-identical to the proven clients/products pattern.
  - **`hasDependents` for locations covers ALL live referrers — no integrity hole.** FK scan of `db/v2/migrations` (NOT the v1 acs_db_final_version.sql): only two tables FK `locations(id)` — `service_zone_rules` (0012:49) and `rates` (0013:20). `service_zone_rules` was `DROP TABLE … CASCADE`d in 0013:39, so the LIVE referencing set = **{rates} only**. `locationRepository.hasDependents` = `EXISTS(SELECT 1 FROM rates WHERE location_id=$1)` (repository.ts:139-145) — exactly the live set. Complete. ✅ (`rate_history` references `rates(id)`, transitively covered.)
  - **report_templates `hasDependents`→`false` is CORRECT today.** `grep -rniE 'REFERENCES (public.)?report_templates' db/v2/migrations` = ZERO referrers → the lock never fires; the JSDoc correctly instructs adding referrers when reporting/CPV lands. No integrity hole (nothing can be orphaned by a code change). **DB-RATCHET (carried):** before any reporting table FKs `report_templates(id)`, the `false` stub MUST be replaced with the real `EXISTS` probe — recorded so it isn't forgotten.
  - **users — NO dependents gate is CORRECT.** `username` has no FK; every reference to a user is by `uuid id` (cases.assigned_to 0010:60, sessions 0009:13, reports_to 0007:15 — all `REFERENCES users(id)`). A login rename touches no FK edge → no lock needed; uniqueness on the `username` unique index is enforced via `mapWriteError` → 409 USER_EXISTS (repository.ts:191-192).
  - **Unique-violation mapped per entity.** locations `isUniqueViolation`→`LOCATION_EXISTS` (repository.ts:198-200, pre-existing pincode+area uniq); templates NEW try/catch `pgCode===23505`→`REPORT_TEMPLATE_EXISTS` (repository.ts:166-169, mirrors create-path :106); users `mapWriteError`→`USER_EXISTS`. No double-wrap of AppError.
  - (d) ✅ No DDL / no migration / no schema change — pure repo-method changes on pre-existing columns (`pincode`/`code`/`username` all already exist + are UNIQUE/NOT-NULL as appropriate). Triple-write invariant N/A. No new index needed (PK/uuid-keyed single-row UPDATE; EXISTS probe hits the existing `rates(location_id)` index `idx_rates_resolve` 0013:35).
  - DISPOSITION: APPROVED, merge-ready. The renumbered param indices are exact in all 3 repos; locations lock predicate is complete vs the live FK graph; templates/users correctly have no lock (no/uuid-only referrers). Accept-gate: pnpm verify green, api 220→223, sdk 63, Playwright 64.

- **2026-06-06 · ADR-0020 final entity: verification_units `code` correctable (the $23 trailing-param one) — VERDICT: PASS.**
  - **THE CRITICAL AUDIT — $23 trailing param traced index-by-index, EXACT (repository.ts:166-208).** SQL `SET code = COALESCE($23, code), name=$2, description=$3, category=$4, kind=$5, worker_role=$6, assignment_method=$7, required_form_code=$8, required_photos=$9, required_gps=$10, required_attachments=$11::jsonb, result_set=$12::text[], review_required=$13, billing_profile=$14, commission_profile=$15, report_template_type=$16, reverification_rule=$17, pii_sensitive=$18, sort_order=$19, effective_from=COALESCE($21::timestamptz,…), version=version+1, updated_by=$20, updated_at=now() WHERE id=$1 AND version=$22`. Params array (1-indexed, 23 elements): id($1), name($2), description($3), category($4), kind($5), workerRole($6), assignmentMethod($7), requiredFormCode($8), requiredPhotos($9), requiredGps($10), requiredAttachments-json($11), resultSet($12), reviewRequired($13), billingProfile($14), commissionProfile($15), reportTemplateType($16), reverificationRule($17), piiSensitive($18), sortOrder($19), userId($20), effectiveFrom($21), expectedVersion($22), **`input.code ?? null`($23)**. **23/23 aligned; $1..$22 BYTE-UNCHANGED from the prior dense list (verified against the pre-diff version); index 23 = `input.code ?? null` exactly.** No off-by-one, no shared-bind reuse across SET-assign + literal-compare (the backend-review SEV-1 class is absent). The "append a fresh trailing param to avoid renumbering" strategy is sound and correctly executed.
  - **COALESCE($23, code) is wipe-safe.** When the service omits a code change, `merged={...existing,...patch}` still carries `existing.code` → `validated.code=existing.code` → `input.code=existing.code` (NOT undefined) → $23=existing.code → COALESCE keeps the same value (no-op). The `?? null` defensive guard never fires via the service path; even if it did, COALESCE(NULL, code) preserves the stored NOT-NULL code. A code can never be nulled/wiped. Confirmed.
  - **`hasDependents` covers ALL live referrers — no integrity hole (re-derived from v2 migrations, NOT the v1 dump).** `grep REFERENCES verification_units db/v2/migrations` = 5 tables: `client_product_verification_units` (0001:80), `rates` (0003:13), `case_tasks` (0010:58), `rate_type_eligibility` (0012:31), `service_zone_rules` (0012:48). The latter TWO are `DROP TABLE … CASCADE`d in 0013:39-40. So the LIVE referring set = **{client_product_verification_units, rates, case_tasks}** = EXACTLY the 3 EXISTS probes in `hasDependents` (repository.ts:150-156). Complete. CODE_LOCKED (409) thrown PRE-mutation in the service (service.ts:103-105), before `repo.update` runs — no SQL UPDATE of `code` executes for an in-use row. `hasDependents` is a single read-only `SELECT EXISTS(...OR...OR...)` (one round-trip, no side effects).
  - **OCC guard + in-tx audit intact.** `requireVersion(patch)` first (400 VERSION_REQUIRED) → `withTransaction(q)` → UPDATE `WHERE id=$1 AND version=$22` (version+1 compare-and-set) → 0 rows → re-SELECT → 404 UNIT_NOT_FOUND or `AppError.stale(current)` (409) → `appendAudit({entityType:'verification_units', action:'UPDATE', before, after:row, versionAfter:row.version}, q)` committed atomically (append-only). Unchanged from the proven pattern.
  - **Unique-violation mapped, no DDL.** The whole tx is wrapped in try/catch → `isUniqueViolation` (local const, SQLSTATE 23505, repository.ts:19 — already used by the create path :231) → `409 UNIT_CODE_EXISTS`. `code` is BOUND ($23), never interpolated; the error body `code already exists: ${input.code}` is a server-side AppError string, not SQL. Pure repo-method change on the pre-existing UNIQUE NOT-NULL `code` column; no schema change → triple-write invariant N/A. No new index needed (PK-keyed single-row UPDATE; the 3 EXISTS probes hit existing FK-side indexes on `verification_unit_id`).
  - COSMETIC (note-only, as flagged): the pre-existing `/** OCC-guarded full-row update + version bump (ADR-0019)… */` JSDoc (repository.ts:148) is now orphaned above the inserted `hasDependents` and no longer sits above `update`. No behavior/type impact.
  - DISPOSITION: APPROVED, merge-ready. The complex $23 trailing-param is exact; merge-revalidate cannot leak an invalid code (CreateVerificationUnitSchema.parse validates the merged row); the lock is complete vs the live v2 FK graph. Accept-gate: sdk 63 (VU 13 green), api typecheck clean, api 223 recorded.

- **2026-06-07 · B-13 DataGrid Export — synchronous CSV/XLSX (clients/products) (uncommitted)** — **VERDICT: PASS. NO DDL / NO MIGRATION — triple-write N/A.** Pure query-reuse: `exportData` (clients/service.ts:60-75, products/service.ts:61-75) re-runs the SAME `repo.list` COUNT+paginated-SELECT pair already audited PASS in the DataGrid slices — no new query, no new join, no N+1, no `SELECT *`. mode=`all` swaps the page LIMIT for `EXPORT_JOB_THRESHOLD` (10000, packages/config/src/index.ts:19) at offset 0; the COUNT is unchanged and remains the row-count source of truth. Confirmed git-diff touches no `db/v2/migrations`.
  - **COUNT/items parity preserved** — `exportData` reuses the identical `where[]`/`params[]` + `clause` builder, so the export reads exactly the rows the COUNT counted (no count/items skew, no truncated-"all"). The 413 boundary guard (`totalCount >= 10000`) reads the honest COUNT, not the LIMIT-capped fetch → see Performance ledger (b-13 entry, boundary trace) for the `≥`-correctness proof.
  - **Index posture deferred to Performance ledger** (no schema change here). RATCHET recorded there: when `mode=all` export rolls to locations[157k]/cases, non-default export sorts hit unindexed ORDER BY → restrict export sort to indexed cols or add `(col,id)` indexes (triple-write at that point); the default-sort + trgm search paths are already index-backed (0020/0021).
  - **DEFERRED (correct):** the ≥10000 background-job tier (report-worker) is not built — `assertExportable` throws 413. No DB object (no job/export table) introduced; when the job tier lands it will need its own migration (export_jobs table + triple-write) and streaming writers — flagged in Performance ledger.

- **2026-06-09 · B-13 cont. — Export `mode:'selected'` (clients/products `repo.list` `ids?: number[]`) (uncommitted)** — **VERDICT: PASS. NO DDL / NO MIGRATION / NO SCHEMA CHANGE — triple-write N/A** (confirmed `git diff` touches no `db/v2/migrations`; pure query-builder + service addition on existing columns).
  - (a) ✅ **REUSE, NOT BESPOKE — the DON'T-REGRESS rule holds.** The selected path adds `id = ANY($n)` to the SAME `where[]`/`params[]` builder that list/count/items already share (clients repository.ts:43-46, products repository.ts identical). The clause is pushed AFTER scope/active/search/`filterClauses`, BEFORE `const clause = ...` (clients:47) → it feeds BOTH the lean COUNT (`count(*)::int FROM clients ${clause}`, :48-51) and the items SELECT (`... ${clause} ORDER BY ... LIMIT/OFFSET`, :54-58) over the identical `clause`+`params`. No separate/bespoke export query; `service.exportData` re-runs the same `repo.list` already audited PASS in the B-13 and DataGrid slices. ✅
  - (b) ✅ **`id = ANY($n)` CORRECTNESS — index-usable, no cast needed.** `clients.id`/`products.id` are `integer GENERATED ALWAYS AS IDENTITY PRIMARY KEY` (0002:7,17). The bound param is a JS `number[]` (`ex.ids.map(Number).filter(Number.isInteger)`, clients/service.ts; products/service.ts) — node-postgres binds a JS number[] as a typed `int[]`/`int8[]` array param, so `id = ANY($n::int[])` is the planner-equivalent of `id IN (...)` and is served by the PK index (`= ANY(array)` → index scan / BitmapOr over the PK, no seq-scan, no per-element type coercion). No `::int[]` cast is required in the SQL (the driver types the array); the int PK + int[] param are domain-matched. Same `= ANY($n)` idiom already audited correct for the enum `IN` path (B-4 entry (a), `kind = ANY($n)`). ✅
  - (c) ✅ **COUNT/items COHERENCE.** Because `id = ANY` lives in the shared `where[]`, COUNT and items narrow IDENTICALLY — count is honest for the ticked set were it consumed. For `mode:'selected'` the service skips `assertExportable` (only `mode:'all'` calls it, clients/service.ts:`if (ex.mode === 'all') assertExportable(totalCount)`), so the COUNT is computed-but-unused on this path — harmless (one extra cheap PK-filtered `count(*)`), and coherent if it were ever read. No count/items skew possible (single shared clause). ✅
  - (d) ✅ **INDEX/SCALE.** `id = ANY(ids)` on the integer PK is index-served at any cardinality; the `ids` array is bounded by what the user ticked across pages (client-side selection set), so the IN-list is small in practice. `limit = exportThreshold()` (10k) on the selected path is a pure BACKSTOP (the WHERE already constrains to ≤|ids| rows; the LIMIT only caps a pathological tick-set) — correct, no concern. ORDER BY is the unchanged whitelisted `${sortColumn} ${sortOrder}, id ${sortOrder}` (clients:56) — no new sort/index surface. **No RATCHET** (PK-keyed filter needs no new index).
  - (e) ✅ **NO N+1 / NO `SELECT *` — unchanged from the audited list path.** Still exactly 2 queries per export call (COUNT + items); `id = ANY` adds one WHERE predicate, zero new round-trips, zero per-row work. SELECT projects the fixed `SELECT_COLS` literal (clients repository.ts:7), never `*`. The empty/invalid-ids guard short-circuits to `{ rows: [], columns }` in the service BEFORE any SQL (clients/products service.ts) → a `mode:'selected'` with no ids issues no query and exports a header-only file (test: "exports nothing… never falls through to all") — never degrades to a full-table scan. ✅
  - **OCC / audit_log:** N/A — export is read-only, no UPDATE/version/audit surface touched.
  - DISPOSITION: APPROVED, merge-ready. The selected export reuses the shared list WHERE builder (no bespoke SQL), `id = ANY($n)` binds a JS int[] against the integer PK (index-served, no cast), count/items stay coherent via the single shared clause, and the empty-ids guard prevents any fall-through to "all". No migration, no index, no RATCHET. OPEN items carried forward unchanged (Slice-1 users/VU/templates sort-col indexes = GA-cosmetic; cases list row-scope must hit both count+items when it lands).

- **2026-06-09 · Bulk activate/deactivate (clients + products) — `platform/bulk.ts applyBulkOcc` + `service.bulkSetActive` reusing `repo.setActive` (uncommitted)** — **VERDICT: PASS. NO DDL / NO MIGRATION / NO SCHEMA CHANGE — triple-write N/A** (confirmed `git diff --stat` touches no `db/v2/migrations`; pure platform helper + service/controller/route wiring + FE DataGrid selection refactor).
  - (a) ✅ **PER-ROW OCC INTEGRITY — each row is its own atomic read-modify-write, version bumps EXACTLY 1, no lost update.** `bulkSetActive` (clients/service.ts:128-131, products/service.ts:124-128) calls `applyBulkOcc(items, (id,version)=>repo.setActive(Number(id), isActive, userId, version))`. Each `setActive` (clients/repository.ts:168-192, products identical) runs inside ONE `withTransaction(q)`: pre-read `before` (`SELECT … WHERE id=$1`) → guarded `UPDATE … SET is_active=$2, version=version+1, updated_by=$3, updated_at=now() WHERE id=$1 AND version=$4 RETURNING …` → audit append, all on the same `q` → commits atomically. The `AND version=$4` is the compare-and-set: a concurrently-modified row (version moved) matches 0 rows ⇒ NO write, NO version bump, NO audit, throws `AppError.stale(before)` ⇒ **lost update is impossible**. `version = version + 1` bumps exactly once per successful row (only the row matching the expected version). No partial write within a row (single tx — UPDATE+audit commit or roll back together). ✅
  - (b) ✅ **PER-ROW CLASSIFICATION CORRECT + MUTUALLY EXCLUSIVE, no error swallowed.** `applyBulkOcc` (bulk.ts:60-86): success → OK; `e instanceof AppError && e.code==='STALE_UPDATE'` → CONFLICT (bulk.ts:74); `e instanceof AppError && e.status===NOT_FOUND` → NOT_FOUND (bulk.ts:77); **else `throw e`** (bulk.ts:81 — a real DB/internal error is NOT masked as a per-row status). Traced against the actual throw sites: `setActive` throws `AppError.notFound('CLIENT_NOT_FOUND')` (status 404, errors.ts:35-37) when `before` is absent, and `AppError.stale(before)` (code `STALE_UPDATE`, status 409, errors.ts:55-59) when the guarded UPDATE matches 0 rows. The two classify branches are disjoint (a stale row has code STALE_UPDATE/status 409, a missing row has a different code/status 404) and the STALE check is evaluated first — correct order, no misclassification. Test (clients.api.test.ts:512-541) proves a mixed batch → `{okCount:1, conflictCount:1, notFoundCount:1}` with per-id `OK/CONFLICT/NOT_FOUND`. ✅
  - (c) ✅ **AUDIT INVARIANT PRESERVED — exactly one append-only audit row per SUCCESSFUL row, ZERO on CONFLICT/NOT_FOUND, no bypass.** Bulk invents NO second write path — it drives the resource's existing OCC write (`repo.setActive`), which appends `{entityType:'clients'|'products', action: isActive?'ACTIVATE':'DEACTIVATE', before, after:row, versionAfter:row.version}` via `appendAudit(…, q)` inside the same tx (clients/repository.ts:178-189). action ∈ AuditAction {ACTIVATE,DEACTIVATE} → matches the audit_log CHECK (0017). A CONFLICT/NOT_FOUND row throws BEFORE the append (stale → throw at :177 before :178; not-found → throw at :171), so no audit row is written for a non-mutating row. append-only (DB trigger blocks UPDATE/DELETE). ✅
  - (d) ✅ **TRANSACTION SCOPE = per-row, NOT one big tx — partial success is REAL and CORRECT here.** `applyBulkOcc` (bulk.ts:68-84) is a sequential `for…await` of independent `setActive` transactions; there is NO outer `withTransaction` wrapping the loop (confirmed — bulk.ts opens no tx of its own). So OK rows COMMIT even when sibling rows CONFLICT/NOT_FOUND — exactly the documented per-row semantics (CONCURRENCY_AND_EDITING_STANDARD §1). **No half-written row** is possible: each row's write+audit is all-or-nothing within its own tx; the only "partial" is at the BATCH grain (some rows committed, some skipped+reported), which is the intended, reportable outcome — never a torn single row. The standard permits per-row OR atomic-batch; for an idempotent status toggle (~2 writes/sec, admin action, each row independent, no cross-row invariant) **per-row is the RIGHT choice** — it lets stale rows be reported individually instead of failing/rolling back the whole batch on one conflict, and avoids holding a long multi-row tx + lock footprint. RULING: per-row-tx ✔ over set-based here.
  - (e) ✅ **N+1 / PERF — N sequential PK-keyed transactions, N≤500, acceptable for an admin status op; set-based would LOSE OCC + per-row audit ⇒ per-row is correct.** Each `setActive` = 1 PK SELECT + 1 PK-and-version UPDATE + 1 audit INSERT, all index-served (clients/products `id` is the integer PK). MAX_BULK_ITEMS=500 (bulk.ts:28) caps the batch; `parseBulkItems` 400s `BULK_TOO_LARGE` beyond that (bulk.ts:39-40) → bounded ≤~1500 tiny indexed statements, sequential. At the stated cadence (~2 writes/sec, ≤500/batch admin toggle on small clients/products tables) this is a sub-second op — **ACCEPTABLE, no RATCHET.** A set-based single-statement `UPDATE … WHERE id = ANY($ids) AND version = …` would be faster at scale BUT cannot express per-row expected-version compare-and-set (each row carries its OWN version), cannot emit per-row OK/CONFLICT/NOT_FOUND, and cannot write the per-row before/after audit row — it would either silently overwrite stale rows (lost update — the exact anti-pattern the OCC standard forbids) or bulk-fail the whole batch. **So per-row is not merely acceptable, it is REQUIRED by the OCC + append-only-audit invariants.** Only revisit IF a future use-case needs >500/batch or sub-second SLA on a large table → then a background-job tier (the MAX_BULK_ITEMS comment already anticipates this, bulk.ts:27), still per-row, NOT a set-based lost-update path.
  - (f) ✅ **NO `SELECT *`, indexes fine.** `setActive` SELECT/UPDATE/RETURNING all project the fixed `SELECT_COLS` literal (clients/repository.ts:7), never `*`. All access is by `id` (integer PK) — PK-served, no new index surface, no sort/filter cols introduced. `parseBulkItems(body,'int')` coerces id via `Number`+`Number.isInteger`+`>0` and version via `Number.isInteger`+`>=0` (bulk.ts:43-48) BEFORE any SQL → a malformed id/version is a 400, never reaches the query (defence-in-depth; the bound `$1`/`$4` would bind-coerce anyway).
  - **OCC / audit / migration summary:** version compare-and-set intact per row · audit append-only + 1-per-success · no lost update · no torn row · no DDL/migration/index. The FE change (DataGrid `BulkSelection<T>` now carries the ticked row OBJECTS incl. `version` so the client can submit per-row `{id,version}`) is the correct shape to FEED per-row OCC — it captures `version` at tick time so a stale-since-selection row surfaces as CONFLICT rather than blindly overwriting. No DB concern.
  - DISPOSITION: **APPROVED, merge-ready.** Per-row OCC is the correct (and invariant-mandated) design over set-based; classification is exact and non-swallowing; audit invariant preserved (1/success, 0/skip); no schema change. **RULING: per-row-tx, not set-based.** No new OPEN item; carried OPEN unchanged (Slice-1 users/VU/templates sort-col indexes = GA-cosmetic; cases list row-scope must hit both count+items when it lands). FUTURE-NOTE (non-blocking): if bulk ever extends to a versioned mutation on a LARGE table at high cadence, keep it per-row but move >500 batches to a background-job tier — never collapse to a set-based UPDATE that would drop OCC/audit.

- **2026-06-10 · C-3 Slice 2: CPV `client_products` LIST → `Paginated` envelope (server-side search/sort/filter) — `cpv/repository.ts` `clientProductRepository.list` COUNT+items pair over shared `CP_FROM` + `cpv/service.ts` `CP_PAGE_SPEC` (uncommitted)** — **VERDICT: PASS. NO DDL / NO MIGRATION / NO SCHEMA CHANGE — triple-write N/A** (confirmed `git diff HEAD --name-only` touches no `db/v2/migrations`; pure repo COUNT+LIMIT/OFFSET rewrite + service PageSpec).
  - (a) ✅ **COUNT CORRECT — `count(*)` over the join == #client_products, NO fan-out.** The shared `CP_FROM` (repository.ts) is `client_products cp JOIN clients c ON c.id = cp.client_id JOIN products p ON p.id = cp.product_id`. Both join keys are NOT-NULL FKs to PKs: `client_products.client_id integer NOT NULL REFERENCES clients(id)` and `product_id integer NOT NULL REFERENCES products(id)` (0002_clients_products_cpv.sql:28-29), and `clients.id`/`products.id` are both `GENERATED ALWAYS AS IDENTITY PRIMARY KEY` (0002:7,17). So each cp row matches EXACTLY one client and EXACTLY one product → strictly N:1 on both sides → the join neither drops rows (NOT NULL + FK ⇒ no unmatched) nor multiplies them (PK ⇒ ≤1 match each) ⇒ `SELECT count(*)::int … FROM cp JOIN c JOIN p WHERE <clause>` equals the count of qualifying `client_products` rows. The COUNT and items SELECT share the SAME `CP_FROM` + `${clause}` + `params`, so `totalCount` is consistent with the windowed page (same predicate, only LIMIT/OFFSET differ). No fan-out, no double-count. ✅
  - (b) ✅ **ORDER BY tiebreaker `cp.id` is QUALIFIED + deterministic; `unit_count` alias safe.** `ORDER BY ${o.sortColumn} ${o.sortOrder}, cp.id ${o.sortOrder}` — the tiebreaker is `cp.id` (qualified); a bare `id` would be ambiguous across the cp/clients/products join (all three have `id`) → would error. `cp.id` is the PK → strict total order → stable, no row skip/dup across pages. `sortColumn='unit_count'` (from `units` apiField) sorts on the correlated-subquery alias `(SELECT count(*) FROM client_product_verification_units x WHERE x.client_product_id = cp.id AND x.is_active)::int AS unit_count` — Postgres permits a SELECT-list output alias in ORDER BY (resolved post-projection), so ordering by `unit_count` is valid and equals ordering by the computed value. The other sortMap targets are real columns (`c.code`/`p.code`/`cp.is_active`/`cp.effective_from`/`cp.created_at`/`cp.updated_at`). ✅
  - (c) ✅ **INDEXES — tiny config table, new sort/filter indexes DISPROPORTIONATE (no RATCHET).** `client_products` has only the PK + `uq_client_products UNIQUE(client_id, product_id)` (0002:33); no extra indexes, and the sort columns (`c.code`, `p.code`, `unit_count`, `cp.is_active`, `cp.effective_from`, `cp.*_at`) are unindexed. **CALL: leave as-is.** CPV is admin master-data — `client_products` is one row per (client × enabled product), a small reference table (tens–low-hundreds of rows in this single-tenant dev/prod), and the list is a low-frequency SUPER_ADMIN/back-office screen. A seq-scan + in-memory sort over a tiny table is sub-millisecond; adding btree indexes for ORDER BY here is premature optimization with net-negative write cost. Same GA-cosmetic class as the carried Slice-1 users/VU/templates sort-col indexes. Revisit only if `client_products` grows to 10k+ rows (would require multi-tenant scale-out — not this product). The correlated `unit_count` subquery IS index-served: `idx_cpvu_client_product ON client_product_verification_units (client_product_id) WHERE is_active` (0001:87) covers the per-row count. ✅
  - **OCC / audit / migration summary:** read-only list path — no version/audit/write concern. COUNT exact (1:1 joins, no fan-out) · qualified `cp.id` deterministic tiebreaker · `unit_count` alias ordering valid · no DDL/migration/index (disproportionate on a tiny config table). DISPOSITION: **APPROVED, merge-ready.** No new OPEN; carried OPEN unchanged (Slice-1 sort-col indexes GA-cosmetic; cases-list row-scope must hit BOTH count+items when it lands — CPV correctly applies the SAME `clause`+`params` to both, the pattern that future scope must follow).

- **2026-06-10 — User-Management parity Slice 3: migration 0025_users_profile.sql (employee_id sequence + phone + dept/desig FKs + password_must_change). VERDICT: APPROVED, merge-ready.**
  - ✅ **Forward-only + idempotent.** `CREATE SEQUENCE IF NOT EXISTS user_employee_seq`; 5× `ALTER TABLE users ADD COLUMN IF NOT EXISTS` (employee_id varchar(50), phone varchar(20), department_id int FK→departments, designation_id int FK→designations, password_must_change bool NOT NULL DEFAULT false); 2× `CREATE INDEX IF NOT EXISTS` (department_id, designation_id). Backfill `UPDATE … SET employee_id = 'CRM-'||lpad(nextval(...),5,'0') WHERE employee_id IS NULL` is re-run-safe (only NULL rows). Re-runnable end-to-end.
  - ✅ **UNIQUE-constraint guard CORRECT.** Uses `IF NOT EXISTS (SELECT 1 FROM pg_constraint WHERE conname='uq_users_employee_id')` rather than `ADD CONSTRAINT … IF NOT EXISTS` (not supported) or exception-catching. Rationale is sound: a UNIQUE constraint owns a backing index of the SAME name, so a blind re-run raises 42P07 `duplicate_table` (relation already exists) BEFORE any `duplicate_object` — the existence guard sidesteps the SQLSTATE-class brittleness entirely. ✅
  - ✅ **Nullability deliberate + minimal.** All 4 profile cols NULLABLE (keeps FK-free import + seed admin + auth/cases/rates fixtures valid); `password_must_change` the lone NOT NULL with a safe `DEFAULT false`. `employee_id` UNIQUE (multiple NULLs allowed in pg, but every create mints one + the backfill nulled-out existing rows → effectively always-present). FK cols are nullable FKs to departments/designations (set-null-friendly; no ON DELETE clause → default NO ACTION, acceptable for admin master-data that is deactivated not deleted).
  - ✅ **Ordering replayable.** 0023 departments → 0024 designations → 0025 (FK targets exist first) → applies cleanly on a fresh DB.
  - ✅ **TRIPLE-WRITE VERIFIED LIVE (I queried both containers):** dev `crm2_dev_pg`/db `crm2_dev` (:54329) + test `crm2_test_pg`/db `crm2_test` (:5433) BOTH show: 5 cols present (employee_id/phone/department_id/designation_id YES-nullable, password_must_change NO-nullable), `user_employee_seq`=1, `uq_users_employee_id`=1, `idx_users_department_id`=1, FK→departments=1, **NULL employee_id count = 0** (backfill executed). File + both DBs in sync.
  - **OCC / audit / index summary:** the per-row INSERT/UPDATE writes are parameterized + audited via `appendAudit` (service/repo layer, not the migration); migration is pure DDL+one-time backfill. Indexes on the two FK cols are proportionate (they back the new items-list LEFT JOINs and future filter-by-dept). DISPOSITION: **APPROVED, merge-ready.** No new OPEN.

- **2026-06-10 · User-Management parity Slice 4 — migration `0026_auth_lockout.sql` (account-lockout state). VERDICT: PASS.** Two additive cols on `users`: `failed_login_count integer NOT NULL DEFAULT 0` + `locked_until timestamptz` (nullable). (a) ✅ **Forward-only + idempotent** — `ADD COLUMN IF NOT EXISTS` ×2, no BEGIN/COMMIT needed (autocommit), re-runnable. `password_must_change` correctly REUSED from 0025 (not re-added). (b) ✅ **Lockout write is atomic + correct** — `recordFailedLogin` = ONE `UPDATE … failed_login_count=failed_login_count+1, locked_until=CASE WHEN failed_login_count+1>=$2 THEN now()+($3||' seconds')::interval ELSE locked_until END … RETURNING` under the row lock (no read-modify-write race); locks exactly on the 5th failure; `ELSE locked_until` preserves an existing lock (no window-slide). `resetLoginState`/`unlock` zero both cols. No new index needed (lookups are by username/PK, already indexed; `locked_until` filter is per-row post-fetch). (c) ✅ **TRIPLE-WRITE VERIFIED LIVE (queried both containers):** dev `crm2_dev`@:54329 + test `crm2_test`@:5433 BOTH show `failed_login_count` (integer, NOT NULL, default 0) + `locked_until` (timestamptz, nullable) — exact type/nullability/default match. File + both DBs in sync. No OCC/audit interaction (login counters are operational state, not version-tracked editable domain). DISPOSITION: **APPROVED, merge-ready.** No new OPEN.

- **2026-06-10 · User-Management parity Slice 5 — migration `0027_mfa.sql` (TOTP MFA state). VERDICT: PASS — live triple-write DEFERRED (no PG in sandbox).** `ALTER TABLE users ADD COLUMN IF NOT EXISTS mfa_required boolean NOT NULL DEFAULT false` + `CREATE TABLE IF NOT EXISTS user_mfa_secrets (user_id uuid PK REFERENCES users(id) ON DELETE CASCADE, secret_encrypted text NOT NULL, recovery_code_hashes text[] DEFAULT '{}', recovery_code_used boolean[] DEFAULT '{}', enrolled_at timestamptz, created_at/updated_at)`. (a) ✅ **Forward-only + idempotent** — both DDL stmts `IF NOT EXISTS`, autocommit, re-runnable; ordering correct (0027 follows 0026). (b) ✅ **Schema sound** — `user_id` PK+FK with `ON DELETE CASCADE` (deleting a user reaps the secret row — no orphan); secret stored ENCRYPTED (`secret_encrypted text`, AES-256-GCM blob, never plaintext); recovery codes stored as `text[]` of scrypt hashes (never plaintext) with a PARALLEL `boolean[]` used-array. (c) ✅ **Parallel-array consistency guaranteed by the repo writes** — `confirmEnrolment` sets `recovery_code_used = (SELECT array_agg(false) FROM unnest($2::text[]))` so `used` is rebuilt to exactly the hash count atomically; mint always emits 10; `markRecoveryUsed` flips one element by 1-based pg index (`i+1`). Pending state = a row with `enrolled_at NULL` (upsert on enroll/start); confirmed = `enrolled_at = now()`. No new index needed (all access is by `user_id` PK). No OCC/audit interaction (MFA enrolment is operational auth state, not version-tracked editable domain). (d) ⚠️ **TRIPLE-WRITE NOT VERIFIED** — dev:54329 + test:5433 PG containers are not running in this audit sandbox, so I could not query the live schema as in slices 3/4. **Implementer must confirm `user_mfa_secrets` + `users.mfa_required` exist in BOTH live DBs (and run the DB-bound flow tests green) before commit.** DISPOSITION: **PASS pending live triple-write confirmation.**

- **2026-06-15 · B-7.1 jobs spine (ADR-0030) — migration `0050_jobs.sql` (new `jobs` table + notifications CHECK widening) + `platform/jobs/{repository,index}.ts` + `modules/jobs/{service,controller,routes}.ts`. VERDICT: PASS.** Reviewed against 0045_notifications.sql (the CHECK precedent) and the test-harness migration loader. `git status` confirms 0050 is the ONLY uncommitted file under `db/v2/migrations/` (the rest of the slice is platform/module/sdk source + ADR-0030).
  - (1) ✅ **Forward-only + idempotent — re-runnable end-to-end.** `CREATE TABLE IF NOT EXISTS jobs` + `CREATE INDEX IF NOT EXISTS idx_jobs_owner_recent`, wrapped `BEGIN…COMMIT`. The notifications CHECK widening uses `ALTER TABLE notifications DROP CONSTRAINT IF EXISTS chk_notifications_type` then `ADD CONSTRAINT … CHECK (…)` (same for `chk_notifications_action_type`) — DROP IF EXISTS + ADD is re-runnable (2nd run drops the just-added constraint and re-adds the identical one; pg has no `ADD CONSTRAINT IF NOT EXISTS` so drop-then-add is the correct idempotent form, consistent with the "DO-block CHECKs" standing invariant intent). The whole migration is in ONE transaction, so a mid-way failure rolls back cleanly. ✅
  - (2) ✅ **CHECK widening is PURELY ADDITIVE — zero risk to existing rows (the critical check).** Diffed 0050's new lists against 0045 verbatim: `chk_notifications_type` keeps ALL 7 original values (CASE_TASK_ASSIGNED, CASE_TASK_REASSIGNED, TASK_COMPLETED, TASK_SUBMITTED_FOR_REVIEW, TASK_REVOKED, CASE_ASSIGNED, SYSTEM) and ADDS `JOB_COMPLETED`, `JOB_FAILED`; `chk_notifications_action_type` keeps the original `NULL OR IN ('OPEN_CASE','OPEN_TASK','NAVIGATE')` and ADDS `'DOWNLOAD'`. No value REMOVED, so every existing notifications row that satisfied the old CHECK satisfies the new one → the `ADD CONSTRAINT` validation pass over existing rows cannot fail. Widening (superset) is safe; this would only be dangerous if it NARROWED the allowed set. ✅
  - (3) ✅ **Schema sound.** `id uuid PK DEFAULT gen_random_uuid()`; `type varchar(40)`/`status varchar(20)` matched by CHECKs `chk_jobs_type IN ('EXPORT','IMPORT')` (== SDK `JOB_TYPES`) + `chk_jobs_status IN ('PENDING','RUNNING','SUCCEEDED','FAILED')` (== SDK `JOB_STATUSES`) — DB ⟷ SDK enum parity confirmed. `progress int NOT NULL DEFAULT 0` + `chk_jobs_progress CHECK (progress BETWEEN 0 AND 100)` — and the runner CLAMPS via `clampPct = max(0,min(100,round))` (index.ts:55) so a processor returning >100 can never violate the CHECK (defense-in-depth: app clamp + DB guard). `payload jsonb NOT NULL DEFAULT '{}'`, `result jsonb` (nullable, set on success), `error text` (nullable, set on fail), `created_at` default now(), `started_at`/`completed_at` nullable timestamptz — set by setRunning/setSucceeded/setFailed. Sound for a job row. ✅
  - (4) ✅ **FK `created_by uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE` — CORRECT choice.** Byte-identical to `notifications.user_id` FK (0045:13) — a job is personal background work owned by the actor, exactly like a notification. CASCADE is right: jobs are ephemeral, per-user, non-authoritative artifacts (the SoT for a completed export/import lands elsewhere — storage key, import_log); when a user is deleted there is no integrity/audit reason to retain their job tray, and orphaned jobs (NULL owner) would break the own-user-scoped read model (`WHERE created_by = $1`). CASCADE keeps the model consistent. (Contrast: audit_log/import_log are RESTRICT/append-only because they ARE the record; jobs are not.) ✅
  - (5) ✅ **`idx_jobs_owner_recent ON jobs (created_by, created_at DESC)` SERVES the listOwned query.** `listOwned` = `WHERE created_by = $1 ORDER BY created_at DESC LIMIT $2 OFFSET $3` (repository.ts:75-83; sortColumn defaults to `created_at`, sortOrder `desc` via JOB_PAGE_SPEC). The composite `(created_by, created_at DESC)` is a covering index-scan for exactly this access path: equality on the leading col + reverse-chronological range on the trailing col matches the index order → no seq-scan, no sort step. `countOwned` (`WHERE created_by = $1`) is also index-served (leading col). Mirrors `idx_notifications_user_recent` (0045:32). Proportionate, not premature. ✅
  - (6) ✅ **TRIPLE-WRITE — test:5433 AUTO-COVERED; dev:54329 is a MANUAL step (carry).** The test template auto-rebuilds: `packages/test-utils/src/helpers/testDb.ts` (`buildTemplateDb`→`migrate`) does `readdirSync(MIGRATIONS_DIR).filter(f=>f.endsWith('.sql')).sort()` (testDb.ts:62-64) and replays ALL `.sql` files, so `0050_jobs.sql` is picked up automatically on the next `vitest globalSetup` run — NO per-test wiring needed (confirmed the slice adds no harness edit). 0050 is the ONLY DDL in the slice (`git status` clean of other migrations) so nothing else needs applying. ⚠️ **dev `:54329` requires the manual `psql -f db/v2/migrations/0050_jobs.sql` per the triple-write invariant** — I could not query the dev container in this sandbox; implementer must confirm `jobs` exists + the widened notifications CHECKs are present on dev before relying on the dev API (else `relation "jobs" does not exist` 500s). Prod N/A (v2 not deployed).
  - (7) ✅ **Append-mostly integrity — NO audit_log requirement, reasonable.** Only the runner mutates status/progress/result/stage/error (setRunning/setProgress/setSucceeded/setFailed, all single-key parameterized UPDATEs by PK); the app never UPDATEs other columns and never hard-deletes (retention = a later purge job, per the header — same posture as notifications 0045). Jobs are ephemeral background-work state, NOT an authoritative editable domain (no `version`/OCC, no `audit_log`), which is the correct call: there is no concurrent-editor or tamper-evidence concern for a single-owner, runner-only-mutated transient row whose authoritative output lives elsewhere (storage/import_log, both already audited). `setRunning` is idempotent-guarded (`WHERE id=$1 AND status='PENDING'`) so a double-dispatch can't double-start. The terminal UPDATEs (setSucceeded/setFailed) are NOT status-guarded — benign here (the in-process runner calls each exactly once per job; a future BullMQ retry should be at-least-once-safe, noted as a forward watch, not a defect today). ✅
  - DISPOSITION: **PASS, merge-ready.** Forward-only/idempotent (drop-then-add CHECK re-runnable), CHECK widening purely additive (no existing-row violation), schema+enums DB⟷SDK in parity with app-side progress clamp backing the 0..100 CHECK, CASCADE FK correct for ephemeral per-user work, index exactly serves listOwned/countOwned, append-mostly with no audit/OCC required (correct for transient runner-only state). OPEN (NEW): confirm dev:54329 manual apply before commit (test:5433 auto-covered). FORWARD WATCH (non-blocking): when the BullMQ worker lands, make setSucceeded/setFailed at-least-once-safe (terminal-state guard) so a retry can't clobber a SUCCEEDED job — not a defect for the in-process runner.

- **2026-06-15 · B-5 Saved Views (DATAGRID_STANDARD §10) — migration `0051_saved_views.sql` (new `saved_views` table) + `modules/saved-views/{repository,service,controller,routes}.ts`. VERDICT: PASS.** Reviewed ADVERSARIALLY vs siblings 0045_notifications / 0050_jobs (own-user pattern). `git status` confirms 0051 is the ONLY uncommitted file under `db/v2/migrations/`; the module dir is new/untracked.
  - (1) ✅ **Forward-only + idempotent — re-runnable end-to-end.** `CREATE TABLE IF NOT EXISTS saved_views` + 2× `CREATE INDEX IF NOT EXISTS` (one plain, one partial-unique), all inside ONE `BEGIN…COMMIT` (0051:8,10,27,30,33). The inline `CONSTRAINT uq_saved_views_user_resource_name UNIQUE(...)` is part of `CREATE TABLE IF NOT EXISTS`, so a 2nd run skips the whole table (constraint included) — no `ADD CONSTRAINT` brittleness (the 42P07 class the 0025 entry warned about does NOT apply: the constraint is born with the table, not added later). Single-tx ⇒ clean rollback on mid-way failure. Re-runnable.
  - (2) ✅ **Schema sound, types/NOT NULL correct.** `id uuid PK DEFAULT gen_random_uuid()`; `user_id uuid NOT NULL`; `resource_key varchar(64) NOT NULL` (queryKey literal — ample); `name varchar(120) NOT NULL`; `state jsonb NOT NULL DEFAULT '{}'::jsonb` (opaque URL-state map, NOT NULL with safe default — a create never nulls it; repo always `JSON.stringify(state)` binds `$4::jsonb`, repository.ts:50-53); `is_default boolean NOT NULL DEFAULT false`; `created_at`/`updated_at timestamptz NOT NULL DEFAULT now()`. Every col NOT NULL except none-that-should-be — no nullable-trap. `updated_at` is app-maintained (`SET updated_at = now()` on every UPDATE path, repository.ts:45,81,120,125) — consistent, though no trigger (acceptable; matches the resource-repo pattern). **LIVE-VERIFIED dev :54329:** 8 cols, all `is_nullable=NO`, types exact.
  - (3) ✅ **FK `user_id … REFERENCES users(id) ON DELETE CASCADE` — CORRECT.** Byte-identical posture to notifications.user_id (0045:13) and jobs.created_by (0050:22). A saved view is personal UI preference owned by the actor; on user-delete there is no integrity/audit reason to retain orphan views, and a NULL owner would break the own-user read model (`WHERE user_id=$1`). CASCADE is right (contrast audit_log/import_log = RESTRICT because they ARE the record). **LIVE-VERIFIED dev :54329:** `confdeltype='c'` (CASCADE).
  - (4) ✅ **INDEXES — both correct, sufficient, non-redundant.** (a) `idx_saved_views_user_resource ON (user_id, resource_key)` (0051:27) exactly serves `listByResource` = `WHERE user_id=$1 AND resource_key=$2 ORDER BY is_default DESC, name ASC` (repository.ts:32-34) — equality on BOTH leading cols ⇒ index-served, no seq-scan; the trailing `ORDER BY is_default DESC, name ASC` is a tiny in-mem sort over one user's views-per-grid (a handful of rows) — adding `is_default DESC, name` to the index would be over-engineering for a per-(user,resource) set that is single-digit cardinality. Correct as-is. (b) `uq_saved_views_default UNIQUE (user_id, resource_key) WHERE is_default` (0051:30-31) — the partial-unique is the RIGHT enforcement of "at most one default per (user,grid)": it indexes ONLY default rows, so non-default views never collide, and two defaults for the same (user,resource) raise 23505. The partial predicate `WHERE is_default` (boolean truthiness) is the idiomatic form. **It ALSO doubles as a covering index for setDefault's owner pre-read** (`SELECT resource_key WHERE id=$1 AND user_id=$2`) — no, that hits the PK; fine. Both new indexes map 1:1 to a distinct access path; no overlap with the PK or the name-unique. **LIVE-VERIFIED dev :54329:** all 4 present (pkey, uq_name, idx_user_resource, uq_default-partial WHERE is_default).
  - (5) ✅ **`UNIQUE(user_id, resource_key, name)` constraint correct.** Scopes uniqueness to the owner+grid+name triple — two users may share a name, and one user may reuse a name across different grids; only a same-(user,grid) name dup is rejected → 23505 → `isUniqueViolation` → `AppError.conflict('SAVED_VIEW_NAME_EXISTS')` 409 (repository.ts:8-13,60,92). Mapped on BOTH create (:60) and update/rename (:92). Correct domain key.
  - (6) ⚖️ **OCC / ADR-0019 — ABSENCE of a `version` column is ACCEPTABLE here (judgement, not a gap).** ADR-0019/the standing invariant mandates `version` + compare-and-set on **editable authoritative domain** tables with a concurrent-multi-editor + tamper-evidence concern (clients/products/users/rates/CPV — all version+audit_log). A saved_view is single-owner personal UI config, in the SAME class as notifications (0045, no version) and jobs (0050, no version) which this audit already PASSed without OCC. Concurrency reality: the only writer of a given row is its owner; the realistic conflict is the same user editing the same view from two tabs — last-write-wins on `name`/`state` is the correct, expected UX for a personal preference (a "someone else changed this view" 409 would be nonsensical when there is no "someone else"). No audit_log either — these are not authoritative records (the SoT for grid data lives in the resource tables; a view is a disposable bookmark). **RULING: own-user UI-preference tables are explicitly OUTSIDE the OCC mandate; version would be ceremony with zero correctness benefit.** Consistent with the 0045/0050 precedents in this ledger.
  - (7) ✅ **N+1 / query efficiency — clean.** Five paths, each ≤2 round-trips, all `$n`-bound, fixed `SELECT_COLS` literal (repository.ts:6), never `SELECT *`: list = 1 index-served SELECT; create = (≤1 sibling-default UPDATE) + 1 INSERT…RETURNING in one tx; update = 1 guarded UPDATE…RETURNING (dynamic `sets[]` but every value `params.push`ed → bound, param-index correct: `id`/`userId` appended last, WHERE uses `$${len-1}`/`$${len}`, repository.ts:71-89 — traced, no off-by-one); remove = 1 DELETE…RETURNING; setDefault = owner pre-read + (≤1 sibling-clear UPDATE) + 1 UPDATE…RETURNING in one tx. No per-row loops, no correlated subqueries. jsonb `state` round-trips as `$4::jsonb` / `$n::jsonb` (stringify on write, native on read via camelize) — no double-encode, no injection (bound).
  - (8) ✅ **THE TWO TRANSACTIONS — "at most one default per (user,resource)" invariant is RACE-SAFE.** Both `create` (isDefault path) and `setDefault` clear the prior default and set the new one inside ONE `withTransaction` (repository.ts:42-58, 111-131) which is a real `BEGIN…COMMIT` on one pooled client (db.ts:34-51), READ COMMITTED (pg default — `withTransaction` issues a bare `BEGIN`, no isolation override). The atomicity does NOT actually rely on isolation level — it is enforced by the **partial-unique index `uq_saved_views_default`** as the hard backstop: even if two concurrent tx both try to set a default for the same (user,resource), the index permits only one row with `is_default=true` per (user,resource); the loser's INSERT/UPDATE raises 23505. The in-tx "clear sibling then set" is the cooperative happy path; the index is the guarantee. **setDefault's clear is correctly scoped `AND id <> $3`** (repository.ts:121) so it doesn't clear the very row it's about to set (would be a self-clear race) — sharp detail, correct. **create's clear** has no `id<>` exclusion but doesn't need one (the new row doesn't exist yet). Owner pre-read in setDefault (`WHERE id=$1 AND user_id=$2`, repository.ts:112-114) enforces own-user scope + 404s a non-owned id BEFORE any mutation — IDOR-safe. One real-world residual: two tx each toggling is_default on DIFFERENT existing rows of the same (user,resource) under READ COMMITTED — tx A clears B's old default & sets A; tx B (snapshot) clears A's old & sets B → B's set can hit the unique index (A already committed is_default on its row) → 23505, B rolls back. Outcome = exactly one default survives, the loser gets a clean error (not a corrupt double-default). The invariant HOLDS regardless of interleaving. ✅ ELSE-branch (isDefault=false in setDefault) just clears the one row — can never create a 2nd default. ✅
  - (9) ✅ **TRIPLE-WRITE — dev :54329 APPLIED & LIVE-VERIFIED (table+4 indexes+CASCADE FK+all-NOT-NULL, queried this session); test :5433 AUTO-COVERED.** 0051 is on disk (the file = leg 1); the test template rebuilds at `vitest globalSetup` via `readdirSync(MIGRATIONS_DIR).filter(.sql).sort()` (same auto-cover the 0050 entry documented) so 0051 is replayed on the next run — the live `crm2_test` DB not yet showing it is EXPECTED (that's the persistent DB, not the rebuilt template). No harness edit needed. Prod N/A (v2 not deployed).
  - DISPOSITION: **PASS, merge-ready.** Idempotent single-tx DDL (constraint born with table, no ADD-CONSTRAINT brittleness); schema/types/NOT-NULL sound + live-verified; CASCADE FK correct for own-user config (0045/0050 precedent); `(user_id,resource_key)` list index + partial-unique-default index both correct, sufficient, non-redundant; UNIQUE(user,resource,name) is the right domain key, 409-mapped on create+rename; **OCC version DELIBERATELY ABSENT and CORRECT for an own-user UI-preference table (outside the ADR-0019 mandate, like notifications/jobs)**; no N+1, all bound, no SELECT *; the one-default invariant is **race-safe via the partial-unique index** (not reliant on isolation level), with setDefault's `id<>$3` self-clear guard + owner pre-read (IDOR-safe). No new OPEN. NOTE (non-blocking): the carried Slice-1 users/VU/templates sort-col indexes remain GA-cosmetic; unaffected here.

- **2026-06-15 · DESIGN REVIEW (NOT a migration) — v2 Case/Task Lifecycle Redesign §6 migration sketch 0052/0053 (`docs/specs/2026-06-15-lifecycle-design/2026-06-15-v2-lifecycle-redesign.md`, ADR-0032). VERDICT: FLAG (proceed to build with the MUST-FIXes folded into 0052/0053; no BLOCK).** Reviewed §6 adversarially vs the live v2 schema (0010 cases / 0036 OCC+history / 0037 dispatch+SFR / 0041 completion-result). Design-only; nothing applied; this entry records the constraints the actual 0052/0053 files must honor.**
  - (1) ✅ **`cases.status` CHECK widening is forward-only-safe AT THE DDL LEVEL.** `chk_cases_status` today = `('NEW','IN_PROGRESS','COMPLETED','CANCELLED')` (0010:25); design adds `AWAITING_COMPLETION`,`REVOKED` → strict SUPERSET. Every existing `cases` row (only NEW/IN_PROGRESS/COMPLETED/CANCELLED possible) trivially satisfies the wider CHECK, so the `ADD CONSTRAINT` revalidation pass over existing rows CANNOT fail → **NO data migration of existing rows is required** (the question posed). The widening is the safe direction; would only be dangerous if it NARROWED (it does not). Must be done as `DROP CONSTRAINT IF EXISTS chk_cases_status` then `ADD CONSTRAINT … CHECK (…)` (pg has no `ADD CONSTRAINT IF NOT EXISTS`) — the same idempotent drop-then-add the 0037 status widening and 0050 notifications widening already use. ✅
  - (2) 🟥 **MUST-FIX (the real gap §6 omits): `cases` has NO `version` column — OCC on case writes is UNBACKED.** Verified: 0017 added `version` to clients/products/locations/users/report_templates ONLY (0017:16 array — `cases` is NOT in it); 0036 added `version` to `case_tasks` (0036:11); **`grep 'ALTER TABLE cases.*version'` across all migrations = ZERO**. Yet the design's CASE state machine (§2 Guards "Every status writer bumps `version`"; §3 `case.finalize` AWAITING_COMPLETION→COMPLETED) needs compare-and-set OCC on `cases` exactly like the shipped task/master-data writers (ADR-0019). `case.finalize` is a money path (commission settle) and a race target (two office users finalizing). **0052 MUST `ALTER TABLE cases ADD COLUMN IF NOT EXISTS version integer NOT NULL DEFAULT 1`** (mirror 0017/0036 backfill-DEFAULT-1) or the redesign silently drops case-level OCC. This is the single most important DB correction to the sketch.
  - (3) 🟧 **SHOULD-FIX: `cases` is NOT in the append-only `audit_log` writer set, and the design leans on "audit" for finalize/revoke.** audit_log table + immutable trigger exist (0017:29-52, blocks UPDATE/DELETE), but per 0017's own note the guarded-UPDATE+`appendAudit` is wired *in code per module*; cases has no module doing it yet. §3/§4 say finalize/revoke "audit" — the builder MUST wire `appendAudit({entityType:'cases', action, before, after, versionAfter}, q)` inside the finalize/revoke tx (needs the new `version` from MUST-FIX (2) for `versionAfter`). `action` values (e.g. `FINALIZE`/`REVOKE`) must be added to the audit_log `action` CHECK if it constrains them — verify against 0017's CHECK before writing. No new table needed; append-only is preserved by reusing the existing trigger-guarded log.
  - (4) ✅ **`case_tasks` execution cols — REUSE claim is CORRECT and clean.** `completed_at`,`completed_by` (FK-less uuid) ALREADY exist (0041:16,20) — §6 "reuse existing completed_at/completed_by" verified true; do NOT re-add. NEW cols `started_at timestamptz`, `form_data jsonb` do NOT exist anywhere (grep = only 0050.jobs.started_at, unrelated) → genuinely additive `ADD COLUMN IF NOT EXISTS`. `completed_by` FK-less matches the deliberate actor-column pattern (0041:17-19 rationale: dev/test synthetic actor ids) — **the design's "FK-less completed_by" is consistent with the established pattern. ✅**
  - (5) ✅ **`parent_task_id` AS A REAL FK is the RIGHT call (the question).** Unlike actor columns (completed_by/created_by/updated_by — FK-less by design), `parent_task_id` references a real `case_tasks(id)` row that MUST exist (revisit/recheck lineage integrity), is never a synthetic actor id, and benefits from referential enforcement + CASCADE semantics. **`parent_task_id uuid REFERENCES case_tasks(id)` (self-FK, NULL for ORIGINAL) is correct** — matches `applicant_id`'s real-FK treatment (0037:26) over the actor-column carve-out. Decide ON DELETE: recommend NO ACTION/default (lineage rows are not hard-deleted; a CASCADE self-ref on a delivered task is a foot-gun). `task_origin varchar CHECK ('ORIGINAL','REVISIT','RECHECK') DEFAULT 'ORIGINAL'` — additive with a default, so existing rows backfill to ORIGINAL automatically; no data migration. ✅ **0053 SHOULD add `idx_case_tasks_parent ON case_tasks(parent_task_id) WHERE parent_task_id IS NOT NULL`** (partial — most tasks are ORIGINAL/NULL) so lineage lookups + the FK's own delete-check are index-served.
  - (6) 🟧 **SHOULD-FIX (indexes for the two NEW hot paths the design introduces):** (a) **Rollup query** (`caseStatusSync.recompute` — "all non-revoked tasks COMPLETED") runs in-tx after EVERY task status write → it will `SELECT status FROM case_tasks WHERE case_id=$1` and aggregate. `idx_case_tasks_case` (0010:70) already serves the `case_id=$1` fetch → **adequate; no new index strictly required** (a case has a handful of tasks; per-case status agg is sub-ms). NOTE only. (b) **AWAITING_COMPLETION office queue** (§3/§9 new Pipeline bucket) = `WHERE status='AWAITING_COMPLETION'` on `cases`. `idx_cases_status` (0010:30) already covers it → **adequate.** Net: the existing indexes cover both new access paths; **no new index is mandatory for 0052** (contrast 0053's parent_task_id which IS worth a partial index, (5)). Do NOT add prophylactic indexes — cases/case_tasks are tiny in v2 (consistent with the size-proportionate calls in the 06-06 slices).
  - (7) ✅ **Demoting `case_tasks.verification_outcome` — KEEP the column, DROP only the authority (the question: keep-vs-drop migration safety).** The column + `chk_case_task_outcome` CHECK exist (0041:14,24-28). The design demotes it to note-only and keeps it in the CHECK. **KEEP is the migration-safe call:** dropping a column that the locked `/sync/download` read-model still maps (`verificationOutcome` field, 0041:9 — a MOBILE-CONTRACT-READ surface, ADR-0012) risks breaking the device delta serializer; a no-op demotion (stop writing it as authoritative, keep reading nothing from it) is zero-risk and reversible. **DROP must NOT happen in 0052** — only in a later migration AFTER the FE/sync serializer provably stops reading it (the design says exactly this, §6 "OR drop in a later migration once FE stops reading it" — endorsed). No data migration; the column simply stops being the SoT (which moves to `cases.verification_outcome`).
  - (8) ✅ **Keeping `SUBMITTED_FOR_REVIEW` in `chk_case_task_status` with NO producer is CLEAN (the question).** SFR was added to the CHECK in 0037 (0037:64). Leaving an allowed-but-unproduced enum value costs nothing at the DB layer (a CHECK is a gate, not a generator — an unused permitted value is inert), and removing it would (a) require a CHECK NARROW (must first prove zero existing rows hold SFR — a data-precondition scan), and (b) risk the device's enum tolerance (the device normalizes SFR→COMPLETED; the design notes this). **RULING: keep SFR in the CHECK for 0052 (inert, device-safe, no narrow-scan needed); a future cleanup migration MAY narrow it only after a `SELECT count(*) … WHERE status='SUBMITTED_FOR_REVIEW'`=0 precondition.** The design's "keep for forward-compat" is the correct, lower-risk choice over removal.
  - (9) ✅ **New `cases` result cols additive + safe.** `verification_outcome varchar(20)` (CHECK `IS NULL OR IN ('POSITIVE','NEGATIVE','REFER','FRAUD')` — mirror 0041:24-28 exactly, NULL-permitting so existing rows are valid pre-finalize), `result_remark text`, `completed_at timestamptz`, `completed_by uuid` (FK-less per actor pattern). All `ADD COLUMN IF NOT EXISTS`, all nullable → **zero existing-row violation, no data migration.** CHECK should use the same `DO $$ … pg_constraint … $$` idempotent guard as 0041:22-29 (re-runnable). ✅
  - (10) ✅ **TRIPLE-WRITE obligation correctly stated.** §6 says dev :54329 + test :5433, "no prod DB exists for v2" — matches the live reality (this ledger's 0050/0051 entries: test:5433 AUTO-rebuilds via `readdirSync(MIGRATIONS_DIR).sort()`; dev:54329 is the MANUAL `psql -f` leg). 0052/0053 inherit that: file-on-disk = leg 1 + test auto-cover; **implementer MUST `psql -f` both files to dev:54329 before the dev API relies on the new cols** (else `column does not exist` 500s). Prod N/A. ✅
  - DISPOSITION: **FLAG — design is DB-sound and buildable; fold these into the actual 0052/0053 before commit.** MUST-FIX: (2) `cases.version` column (case-level OCC is unbacked — the one true gap). SHOULD-FIX: (3) wire `appendAudit` for cases finalize/revoke + check action-CHECK; (5) `idx_case_tasks_parent` partial index in 0053; (6) confirm existing `idx_cases_status`/`idx_case_tasks_case` cover the queue+rollup (they do — no new index for 0052). ENDORSED AS-IS: (1) status-widening needs no data migration; (4) reuse completed_at/by + new started_at/form_data; (5) parent_task_id as real self-FK + task_origin default; (7) KEEP verification_outcome (drop later, post-sync-serializer); (8) KEEP SFR inert in CHECK; (9)(10) result cols + triple-write. No BLOCK. OPEN (carried): Slice-1 sort-col indexes GA-cosmetic (unaffected).

- **2026-06-15 · Slice-1 commit review — migration 0052_case_lifecycle + cases/repository.ts SQL (ADR-0032 spine)** —
  VERDICT **PASS**. The implementation honors every MUST/SHOULD from my 2026-06-04 design review. Verified
  against the actual migration file AND live on BOTH DBs (triple-write confirmed).
  - (a) ✅ **MIGRATION SHAPE — forward-only + idempotent.** `ALTER TABLE … ADD COLUMN IF NOT EXISTS` ×6
    (0052:28,36-39,47-48). Both CHECKs are drop-then-add (`DROP CONSTRAINT IF EXISTS` → `ADD CONSTRAINT`,
    0052:31-33,41-44) — pg has no ADD CONSTRAINT IF NOT EXISTS, so drop-then-add is the correct re-runnable
    idiom (matches 0050 notifications pattern; simpler than 0041's DO-block but equivalent + idempotent).
    Wrapped in BEGIN/COMMIT (0052:25,56) — atomic, fine. `role_permissions` seed is `ON CONFLICT
    (role_code, permission_code) DO NOTHING` (0052:52-54) — idempotent, re-run-safe. Whole file re-runnable.
  - (b) ✅ **cases.version ADDED — my one MUST-FIX (#2) is satisfied.** `ADD COLUMN IF NOT EXISTS version
    integer NOT NULL DEFAULT 1` (0052:28). NOT NULL + DEFAULT 1 ⇒ every existing row backfills to 1 in the
    same ALTER, zero violation. Mirrors case_tasks.version (0036:11). Live-verified present on both DBs.
    Both write paths bump it: rollup `version = version + 1` (repo:115) and finalize `version = version + 1`
    (repo:758) — case-level OCC is now backed end-to-end. finalizeCase guards `WHERE … version = $5`
    (repo:756) and throws `AppError.stale(cur)` on 0-row (repo:769) — true compare-and-set.
  - (c) ✅ **status CHECK widen is a SAFE SUPERSET — no data migration.** Original (0010:25) =
    {NEW,IN_PROGRESS,COMPLETED,CANCELLED}; new (0052:33, live-confirmed) ADDS {AWAITING_COMPLETION,REVOKED}
    keeping all four originals ⇒ every existing `cases` row stays valid. No UPDATE/backfill needed. ✅
  - (d) ✅ **verification_outcome CHECK correct.** `IS NULL OR IN ('POSITIVE','NEGATIVE','REFER','FRAUD')`
    (0052:42-44, live-confirmed) — NULL-permitting so all pre-finalize rows (col added NULL) are valid;
    mirrors case_tasks chk_case_task_outcome (0041) exactly. The column is varchar(20). ✅
  - (e) ✅ **role_permissions seed idempotent.** ON CONFLICT DO NOTHING on the (role_code,permission_code)
    PK (0052:52-54). BACKEND_USER→case.finalize live-present on both DBs (count=1, not duplicated on re-run).
  - (f) ✅ **ROLLUP aggregate is index-supported — NO full-scan concern.** recomputeCaseStatus (repo:91-118):
    the `count(*) FILTER(...)` aggregate is `FROM case_tasks WHERE case_id = $1` (repo:97-101) — a single
    case's task partition, backed by `idx_case_tasks_case ON case_tasks(case_id)` (0010:70, live-confirmed).
    Bounded by tasks-per-case (small). The `SELECT status FROM cases WHERE id=$1 FOR UPDATE` (repo:92) is a
    PK lookup → single-row lock. Lock order tasks→cases is documented + consistent (no deadlock). The
    serialize-on-cases-row design correctly fixes the "both see one-active → stuck" race. ✅
  - (g) ✅ **appendAudit on finalize uses action 'UPDATE' — CORRECT, not a new enum value.** finalizeCase
    appends `action: 'UPDATE'` (repo:751) inside the same tx. The audit_log CHECK allows
    {CREATE,UPDATE,DEACTIVATE,ACTIVATE} (0017:33) — 'UPDATE' is in-set. A finalize mutates an existing case
    row ⇒ UPDATE is the semantically right action (not CREATE/DEACTIVATE). My SHOULD-FIX #3 satisfied. ✅
    (NOTE non-blocking: rollup recomputeCaseStatus does NOT appendAudit. Acceptable — it is a derived
    system-side status roll, the `version`/`updated_by`/`updated_at` bump records the actor+bump, and the
    triggering task action [assign/unassign/complete] already audits. Not a regression vs design.)
  - (h) ✅ **case_tasks.verification_outcome KEPT, not demoted.** 0052 only ADDs case_tasks.started_at +
    form_data (0052:47-48); the 0041 per-task result cols are untouched (header comment 0052:21-22 states
    this explicitly; grep confirms 0052 has no DROP/ALTER on case_tasks.verification_outcome). My design
    ruling #7 (keep column, drop authority only) honored. ✅
  - (i) ✅ **NO migration that could fail on existing dev/test rows.** All ADDs are nullable (or NOT
    NULL+DEFAULT for version → backfills). Both CHECK widens are supersets / NULL-permitting → no existing
    row can violate. role_permissions ON CONFLICT can't dup. Verified by the fact it APPLIED cleanly to both
    live DBs (which hold rows). Zero data-migration risk. ✅
  - (j) ✅ **TRIPLE-WRITE HONORED.** Live-verified on dev :54329/crm2_dev AND test :5433/crm2_test: all 6
    cols present (cases.version/verification_outcome/completed_by, case_tasks.started_at/form_data) + the
    BACKEND_USER→case.finalize grant + both CHECK defs carry the widened sets. Both legs match. ✅
  - SQL CORRECTNESS (repo diff): new caseFinalizeState (repo:730-743) reuses caseScopePredicate → scope-safe
    (out-of-scope/missing → null → service 404, IDOR-safe, same pattern as taskAssignmentState). finalizeCase
    UPDATE guards `version=$5 AND status='AWAITING_COMPLETION'` then disambiguates 0-row into NOT_FOUND /
    INVALID_TRANSITION / stale (repo:761-769) — correct three-way. recompute clears verdict cols on any
    non-COMPLETED move (anti-staleness, ADR-0032 §4) and the `status <> $2` guard makes a no-op recompute
    skip the row (no spurious verdict-clear; and COMPLETED is never a computed target so a live verdict is
    never clobbered by rollup — only finalize sets COMPLETED). All imports resolve (TxQuery/Scope/
    caseScopePredicate/AppError.stale pre-exist). ✅
  - DISPOSITION: **PASS — slice-1 spine is DB-sound, idempotent, superset-safe, OCC-backed, audit-correct,
    triple-written. All design-review MUST/SHOULD items closed.** No FLAG, no BLOCK. OPEN (carried, unrelated):
    Slice-1 sort-col indexes GA-cosmetic; future migration MAY drop case_tasks.verification_outcome only after
    sync serializer stops reading it (per design #7).

- **2026-06-16 · Slice-3 task lineage — migration 0054_task_lineage + revisit/reassign/verdictHistory SQL
  (ADR-0033, uncommitted)** — VERDICT **PASS**. Adds case_tasks.parent_task_id (self-FK) + task_origin,
  partial idx_case_tasks_parent, role_permissions seed (task.rework → BACKEND_USER/MANAGER); repo gains
  revisitTask/reassignRevokedTask (INSERT…SELECT clone of parent) + verdictHistory (audit_log SELECT) +
  hasActiveRevisitOf (duplicate-revisit gate). Verified file + dev:54329 (psql) + the test harness template
  (all 54 migs replayed) — full E2E proof below.
  - (a) ✅ **MIGRATION SHAPE — forward-only + idempotent.** `ADD COLUMN IF NOT EXISTS` ×2 (0054:26,29);
    CHECK is drop-then-add (`DROP CONSTRAINT IF EXISTS chk_case_tasks_task_origin` → `ADD CONSTRAINT`,
    0054:31-33 — the correct re-runnable idiom, pg has no ADD CONSTRAINT IF NOT EXISTS); `CREATE INDEX IF
    NOT EXISTS idx_case_tasks_parent` (0054:35-36); role_permissions seed `ON CONFLICT (role_code,
    permission_code) DO NOTHING` (0054:40-43). Wrapped BEGIN/COMMIT (0054:22,45). Whole file re-runnable.
  - (b) ✅ **task_origin NOT NULL DEFAULT 'ORIGINAL' is BACKFILL-SAFE on the populated table.** NOT NULL +
    DEFAULT in one ALTER ⇒ every existing case_tasks row materializes 'ORIGINAL' atomically, zero violation
    (same pattern as cases.version 0052:28). The CHECK `task_origin IN ('ORIGINAL','REVISIT')` (0054:32-33)
    is satisfied by the default ⇒ no pre-existing row can violate. ✅
  - (c) ✅ **SELF-FK ON DELETE NO ACTION — no orphan / cascade conflict.** No case_tasks (or cases) delete
    path exists in code (grep: only an attachments DELETE route, never a task/case row delete). cases→
    case_tasks is `ON DELETE CASCADE` (0010:57); should a case ever be deleted, all its tasks (parent AND
    child lineage rows) are removed in the SAME cascade statement, so the self-FK NO-ACTION is re-checked at
    statement end with both rows gone → satisfied, never blocks. parent_task_id is nullable (ORIGINAL tasks
    carry none). No cascade loop, no orphan. ✅
  - (d) ✅ **PARENT IS NEVER MUTATED — no dropped OCC.** Both revisitTask (repo:861-915) and
    reassignRevokedTask (repo:924-1016) only READ the parent (the `FROM case_tasks p WHERE p.id=$2` source of
    the INSERT…SELECT, plus a SELECT for assigned_to in reassign); grep confirms ZERO `UPDATE case_tasks` /
    `UPDATE cases` in either function. The new child row needs no OCC token (fresh insert). The only version
    bump is recomputeCaseStatus → `cases.version+1` (repo:115), the established single rollup writer that
    serializes on the cases row `FOR UPDATE` (repo:92). Case-level OCC intact; no token required or dropped
    where a write occurs. ✅
  - (e) ✅ **task_number generation is byte-identical to the proven addTasks path.** `(SELECT case_number
    FROM cases WHERE id=$1) || '-' || seq::text` (repo:893,957) == addTasks (repo:402), backed by the same
    `UNIQUE (case_id, task_number)`; seq = `count(*)+1 FROM case_tasks WHERE case_id=$1`. Concurrent
    revisit/add-tasks reading the same count is caught by the UNIQUE and mapped to a retryable 409
    (UNIQUE_VIOLATION → TASK_NUMBER_CONFLICT, repo:921,1011) — identical to the existing race handling. FK
    violation on a bad assignee → 400 INVALID_ASSIGNEE (repo:1012). ✅
  - (f) ✅ **hasActiveRevisitOf is the partial-index-backed dup-revisit gate.** `count(*) FROM case_tasks
    WHERE parent_task_id=$1 AND task_origin='REVISIT' AND status IN ('PENDING','ASSIGNED','IN_PROGRESS')`
    (repo:851-857). The partial `idx_case_tasks_parent ON (parent_task_id) WHERE parent_task_id IS NOT NULL`
    serves the leading `parent_task_id=` equality (most rows are ORIGINAL ⇒ partial keeps the index tiny);
    task_origin + status are cheap residual filters over the ≤handful of lineage rows per parent. The IN-set
    is the correct "open" subset of the case_tasks status CHECK (0037: …,SUBMITTED_FOR_REVIEW,REVOKED,…).
    Partial index is the right choice; a composite (parent_task_id, task_origin, status) would be
    over-engineering at v2 lineage volumes (≤a few children per task). NOTE non-blocking — not a ratchet.
  - (g) ✅ **verdictHistory is INDEX-COVERED, no seq scan, no N+1.** SELECT over audit_log
    `WHERE entity_type='case' AND entity_id=$1 … ORDER BY created_at DESC, id DESC` (repo:836-848) — the
    WHERE + leading ORDER key are served EXACTLY by `idx_audit_log_entity (entity_type, entity_id, created_at
    DESC)` (0017:40). The `after_data->>'verificationOutcome' IS NOT NULL` is a residual jsonb filter applied
    AFTER the index narrows to one case's rows (bounded by finalize-events-per-case = tiny) — correct, since
    only finalize rows write that key (`entityType:'case'` + `after_data.verificationOutcome`, repo:781,821);
    rollup writes no audit row and CREATE has a null outcome ⇒ exact filter. Single query, ONE `LEFT JOIN
    users u ON u.id::text = a.actor_id` (the `::text` cast on the uuid PK forgoes a PK-index lookup but the
    join cardinality = #finalize events, negligible) — no app-level N+1, no per-row round-trip. ✅
  - (h) ✅ **audit_log stays append-only — nothing bypasses the immutable trigger.** Both new functions only
    appendAudit `action:'CREATE'` (repo:902-910,985-996) via the standard helper INSIDE the tx; the new
    verdictHistory only READS audit_log. No UPDATE/DELETE on audit_log anywhere in the diff ⇒ the 0017
    BEFORE-UPDATE-OR-DELETE block trigger is never provoked. reassign also appends a
    task_assignment_history 'ASSIGNED' row (repo:967-979, previous_assigned_to = the revoked assignee) — an
    ordinary insert, correct first-assignment event. ✅
  - (i) ✅ **TRIPLE-WRITE.** Leg 1 file is the latest (0054, the highest number). Leg 2/dev :54329/crm2_dev:
    live-verified parent_task_id + task_origin cols, chk_case_tasks_task_origin, idx_case_tasks_parent, and
    BOTH task.rework grants present. Leg 3/test: the harness globs `readdirSync(MIGRATIONS_DIR).filter(.sql)
    .sort()` (test-utils/testDb.ts:62-64) into a per-run TEMPLATE DB, so 0054 auto-applies on every test run
    (the static :5433/crm2_test is NOT what tests use — its emptiness is expected and irrelevant). PROVEN:
    booted the cases suite against :5433 with DATABASE_URL set — globalSetup replayed all 54 migs into the
    template cleanly (0054 applied onto the populated prior schema with zero error) and **all 58 tests PASS**,
    exercising the full adversarial matrix live: revisit 201/409-dup/409-idempotent/201-after-done/403-perm/
    404-scope; reassign 201/409-conflict/400-INVALID_ASSIGNEE/403/404; verdict-history 200/404-scope. ✅
  - DISPOSITION: **PASS — migration is forward-only, idempotent, backfill-safe (NOT NULL DEFAULT), self-FK
    poses no orphan/cascade risk, parent untouched (no dropped OCC), task_number race handled, verdict-history
    fully index-covered, audit_log append-only honored, triple-written and E2E-proven (58/58 on a fresh 54-mig
    template).** No FLAG, no BLOCK. OPEN (note, non-blocking): if lineage chains ever fan out widely, revisit
    a composite (parent_task_id, task_origin, status) for hasActiveRevisitOf — not warranted at v2 volumes.

- **2026-06-16 · Slice 2c-2 device field-photo — migration 0055_field_photo_attachments + insertFieldAttachment/
  fieldAttachmentsByOperation SQL (ADR-0034, uncommitted)** — VERDICT **PASS**. Extends the existing populated
  `case_attachments` table (0042, office reference docs) IN PLACE with a `kind` discriminator + 8 nullable
  device-photo columns, a partial UNIQUE on operation_id, and a partial field-photo task index — rather than a
  parallel table. Verified file + dev:54329/crm2_dev (psql `\d` + live row census) + the test-harness template
  (all 55 migs replayed; 7/7 verification-tasks API tests incl both ADR-0034 photo tests PASS against a real DB).
  - (a) ✅ **MIGRATION SHAPE — forward-only + idempotent.** `ADD COLUMN IF NOT EXISTS` ×9 (0055:20-21,26-33);
    CHECK is drop-then-add (`DROP CONSTRAINT IF EXISTS chk_case_attachments_kind` → `ADD CONSTRAINT`, 0055:22-24
    — correct re-runnable idiom, pg has no ADD CONSTRAINT IF NOT EXISTS); `CREATE UNIQUE INDEX IF NOT EXISTS
    uq_case_attachments_operation` (0055:36-37) + `CREATE INDEX IF NOT EXISTS idx_case_attachments_field_task`
    (0055:40-41). Wrapped BEGIN/COMMIT (0055:18,43). Whole file re-runnable.
  - (b) ✅ **kind NOT NULL DEFAULT 'OFFICE_REF' is BACKFILL-SAFE on the populated office table.** NOT NULL +
    DEFAULT in one ALTER ⇒ every existing case_attachments row materializes 'OFFICE_REF' atomically, zero
    violation (same pattern as task_origin 0054:26, cases.version 0052:28). The CHECK `kind IN ('OFFICE_REF',
    'FIELD_PHOTO')` (0055:23-24) is satisfied by the default ⇒ no pre-existing row can violate. **PROVEN on the
    live populated dev table:** census = 5 rows / 0 NULL kind / 4 backfilled to OFFICE_REF (the pre-existing
    office uploads, untouched) / 1 FIELD_PHOTO (test row). ✅
  - (c) ✅ **OFFICE reference uploads still work UNCHANGED — kind defaults, no code edit.** insertAttachment
    (repository.ts:1240-1245) column list is `(case_id, task_id, original_name, mime_type, file_size,
    storage_key, sha256, uploaded_by)` — does NOT mention `kind` ⇒ the DEFAULT 'OFFICE_REF' fills it. The office
    INSERT/list/listOne/softDelete SQL is untouched in the diff (only NEW functions added below it). The two new
    device columns it never sets stay NULL. Office flow is byte-identical. ✅
  - (d) ✅ **partial UNIQUE on operation_id correctly enforces at-most-once per `<base>:<i>` and lets office rows
    coexist.** `uq_case_attachments_operation ON (operation_id) WHERE operation_id IS NOT NULL` (0055:36-37):
    office rows have operation_id NULL ⇒ excluded from the index ⇒ unlimited NULLs coexist (and pg already
    treats NULLs as distinct, but the WHERE makes intent explicit + keeps the index field-photo-only/tiny).
    Each device file is stored with `operationId = `${operationBase}:${i}`` (service.ts:208), unique per
    (upload, file index) ⇒ a concurrent identical upload's 2nd writer hits the UNIQUE, mapped to a retryable
    idempotent replay (UNIQUE_VIOLATION → re-SELECT cached set, service.ts:215-219). Correct idempotency anchor.
  - (e) ✅ **replay SELECT — split_part is a SEQ-SCAN residual, ACCEPTABLE at v2 volumes (note only).**
    fieldAttachmentsByOperation (repository.ts:1308-1314): `WHERE kind='FIELD_PHOTO' AND deleted_at IS NULL AND
    split_part(operation_id, ':', 1) = $1 ORDER BY created_at ASC, operation_id ASC`. The `split_part(...)=$1`
    is a functional predicate on the LEFT side ⇒ NOT sargable against `uq_case_attachments_operation` (which is
    on raw operation_id) → no index seek on that leg. BUT `idx_case_attachments_field_task ON (task_id) WHERE
    kind='FIELD_PHOTO'` does NOT help here either (no task_id in this WHERE) — this query relies on the small
    field-photo row population; the planner scans field-photo rows and applies split_part as a filter. At v2
    volumes (a handful of photos per task, replay is the rare retry path) this is FINE — NOT a ratchet. If
    field-photo volume ever climbs, an expression index `((split_part(operation_id,':',1))) WHERE
    kind='FIELD_PHOTO'` would make replay an index seek. NON-BLOCKING.
  - (f) ✅ **idx_case_attachments_field_task backs the per-task field-photo lookup.** Partial `ON (task_id)
    WHERE kind='FIELD_PHOTO'` (0055:40-41) serves a `task_id=$1 AND kind='FIELD_PHOTO'` access path (the
    intended per-task photo gallery / completion read), kept tiny by excluding the bulk OFFICE_REF rows. The
    pre-existing un-partial `idx_case_attachments_task ON (task_id) WHERE deleted_at IS NULL` (0042:29) also
    covers task_id but mixes both kinds; the new partial is the right field-photo-specialized companion. No
    bloat — distinct predicate, distinct purpose. ✅
  - (g) ✅ **soft-delete + jsonb handling correct.** fieldAttachmentsByOperation filters `deleted_at IS NULL`
    (repository.ts:1311) — DPDP-erased rows excluded from replay (correct: a re-upload after erasure is a fresh
    op, not a cached hit). geo_location stored via `$11::jsonb` from `JSON.stringify` (repository.ts:1281,1294,
    null-guarded `== null ? null : JSON.stringify`) — valid jsonb, no injection (bound param). NOTE
    (non-blocking): no shape CHECK on geo_location (v1 had one); the {lat,lng,accuracy,timestamp} shape is
    parsed/validated in app code (service.ts:168 parseGeo) before insert, so a DB CHECK is belt-and-suspenders —
    worth adding only if non-device writers ever touch the column. NOT a ratchet.
  - (h) ✅ **no OCC needed.** insertFieldAttachment is insert-only (new rows); office rows are never mutated by
    this slice (no UPDATE in the diff touching them). The append-only audit_log trigger is never provoked
    (attachment inserts don't write audit_log). No version token required or dropped. ✅
  - (i) ✅ **TRIPLE-WRITE HONORED.** Leg 1: file is the LATEST migration (0055, highest number; confirmed
    `ls db/v2/migrations | tail`). Leg 2/dev :54329/crm2_dev: live `\d case_attachments` shows all 9 new cols,
    `kind` default `'OFFICE_REF'`, chk_case_attachments_kind, uq_case_attachments_operation (UNIQUE WHERE
    operation_id IS NOT NULL), idx_case_attachments_field_task (WHERE kind='FIELD_PHOTO') — all present. Leg 3/
    test: harness globs `readdirSync(MIGRATIONS_DIR).filter(.sql).sort()` (test-utils/testDb.ts:61-64,80) into a
    per-run TEMPLATE, so 0055 auto-applies every test run. PROVEN: ran verification-tasks.api.test.ts with
    DATABASE_URL set → template replayed all 55 migs cleanly (0055 onto the populated 0042 schema, zero error)
    and **all 7/7 tests PASS**, incl the two ADR-0034 tests asserting a real `kind='FIELD_PHOTO'` row with
    `operation_id='op-PH-1:0'`, server evidence hash, verified client hash, idempotent replay, and the guards
    (non-assignee 404 / missing Idempotency-Key 400 / non-image → failed[]). ✅
  - DISPOSITION: **PASS — migration is forward-only, idempotent, backfill-safe (NOT NULL DEFAULT on a populated
    table, 0-violation proven live), office flow untouched, partial UNIQUE is the correct idempotency anchor
    (NULLs coexist), field-photo task index appropriate, soft-delete respected, insert-only (no OCC), triple-
    written and E2E-proven (7/7 on a fresh 55-mig template + live dev `\d`).** No FLAG, no BLOCK. OPEN (notes,
    non-blocking): (1) the split_part replay predicate is a seq-scan residual — fine at v2 photo volumes, add an
    expression index only if field-photo count climbs; (2) geo_location has no jsonb shape CHECK (app-validated
    pre-insert) — add only if non-device writers ever populate it.

- **2026-06-16 · 0056 task.revoke seed + revokeTaskByDevice→revokeTaskInPlace rename (uncommitted; 0055
  already committed `cbc9d0d`)** — VERDICT **PASS**. Adversarial DB/integrity pass. Scope of the UNCOMMITTED
  diff: migration `0056_task_revoke_perm.sql` (untracked) + a rename of the shared revoke repo fn + the
  `task.revoke` permission seed in @crm2/access. NO schema/DDL change beyond 0056's 2-row INSERT. (0055's DDL was
  audited+PASS in the 2026-06-xx entry above; re-confirmed all cols/indexes still live below.)
  - (a) ✅ **0056 MIGRATION SHAPE** — forward-only + idempotent: single `INSERT INTO role_permissions
    (role_code, permission_code) VALUES ('BACKEND_USER','task.revoke'),('MANAGER','task.revoke') ON CONFLICT
    (role_code, permission_code) DO NOTHING` in a BEGIN/COMMIT (0056:10-17). Re-runnable (verified: 2nd apply
    no-ops). NO DDL, so no IF-EXISTS dance needed. ON CONFLICT target `(role_code, permission_code)` is backed
    by `uq_role_permission UNIQUE (role_code, permission_code)` (live `pg_get_constraintdef` confirmed) — NOT
    the PK (`role_permissions_pkey` is on surrogate `id`). Correct arbiter; DO NOTHING is the right idempotency
    op (no version/audit on this static config table). ✅
  - (b) ✅ **NUMBERING** — 0056 is the highest migration (after 0055), no gap/dup in 0050–0056 (`ls` confirmed).
    Forward-only chain intact.
  - (c) ✅ **PERMISSION-SEED ↔ @crm2/access PARITY** — 0056 grants BACKEND_USER+MANAGER (SA via grants_all);
    permissions.ts diff adds `TASK_REVOKE: 'task.revoke'` to BOTH MANAGER and BACKEND_USER ROLE_PERMISSIONS maps
    + PERMISSION_META (packages/access/src/permissions.ts). DB seed == code map, byte-for-byte role set. The
    roles-parity test will catch future drift. ✅
  - (d) ✅ **TRIPLE-WRITE** — Leg 1: file is leg-1 (latest mig). Leg 2/dev :54329/crm2_dev: `SELECT … WHERE
    permission_code='task.revoke'` → exactly {BACKEND_USER, MANAGER} present (CTO pre-applied). Leg 3/test
    :5433: harness re-globs+sorts all `.sql` into a fresh per-run template, so 0056 auto-applies at test time
    (the persistent :5433 currently shows 0 task.revoke rows because the untracked mig hasn't been replayed into
    the standing DB — EXPECTED for an uncommitted mig; the harness applies it on the next run, no source-only
    500 risk because dev is already seeded). ✅
  - (e) ✅ **REVOKE TRANSITION (revokeTaskInPlace, repository.ts:1115-1150)** — the uncommitted diff is a PURE
    RENAME (revokeTaskByDevice→revokeTaskInPlace) + doc/comment + one error-string text; the DB logic is
    byte-identical (git diff confirms: only the fn name, the JSDoc, and `'device revoke returned no row'`→
    `'revoke returned no row'` changed; the UPDATE/audit/recompute body is untouched). Integrity holds:
    (1) ONE `withTransaction`; (2) guarded UPDATE `SET status='REVOKED', version=version+1` WHERE
    `status IN ('ASSIGNED','IN_PROGRESS')` — OCC version bumped, COMPLETED/PENDING excluded → INVALID_TRANSITION
    409, re-revoke idempotent; (3) **append-only audit honored** — `appendAudit({entityType:'case_task',
    action:'UPDATE', after:{status:'REVOKED',reason}}, q)` in-tx (audit_log trigger blocks UPDATE/DELETE, this is
    INSERT-only); (4) **single case-status writer** — `recomputeCaseStatus(q,…)` is the ONLY case mutation; it
    `SELECT … FOR UPDATE` locks the case row (repository.ts:101), early-returns on REVOKED/missing case
    (manual-terminal preserved), recomputes from `case_tasks` aggregate, and its UPDATE is itself version-bumped
    + guarded `WHERE status<>'REVOKED' AND status<>$computed`. No second case writer in the path. ✅
  - (f) ✅ **0055 STILL-LIVE RE-CONFIRM** (since 0056 ships alongside the field-photo consumer): live `\d
    case_attachments` on dev :54329 — `kind varchar(20) NOT NULL DEFAULT 'OFFICE_REF'`, chk_case_attachments_kind
    CHECK(OFFICE_REF/FIELD_PHOTO), uq_case_attachments_operation UNIQUE WHERE operation_id IS NOT NULL,
    idx_case_attachments_field_task WHERE kind='FIELD_PHOTO', all 9 device cols. Backfill proven SAFE on the
    POPULATED table: 5 rows total, 0 null_kind, 4 OFFICE_REF + 1 FIELD_PHOTO (NOT NULL DEFAULT backfilled the
    pre-existing office rows in place — no violation). FIELD_PHOTO insert (repository.ts:1280-1304) populates
    every NOT-NULL col (case_id/kind-literal/original_name/mime_type/file_size/storage_key/sha256/uploaded_by;
    task_id provided though nullable) — no NULL-violation risk. geo_location via `$11::jsonb` from
    `JSON.stringify` (null-guarded) — bound param, valid jsonb, injection-safe. ✅
  - DISPOSITION: **PASS.** 0056 is forward-only, idempotent (ON CONFLICT DO NOTHING on the correct unique
    arbiter), numbered correctly, DB-seed↔code-map parity, triple-write honored (dev applied, harness auto-applies
    on next run). The uncommitted revoke change is a logic-preserving rename — OCC, append-only audit, and the
    single FOR-UPDATE case-status writer all intact. 0055's backfill/constraints/indexes re-verified live. No
    FLAG, no BLOCK. OPEN (carried from 0055 entry, non-blocking): split_part replay predicate is a confirmed
    Seq Scan (EXPLAIN on dev: `Seq Scan … Filter split_part(operation_id,':',1)=`) — fine at v2 photo volumes,
    add an expression index ONLY if field-photo count climbs; geo_location still has no jsonb shape CHECK
    (app-validated pre-insert).

- **2026-06-16 · Lifecycle slice 2c-2 tail — migration 0057 + sync `revokedAssignmentIdsForUser` (uncommitted)** —
  VERDICT **PASS** (1 perf NOTE, non-blocking). New `db/v2/migrations/0057_task_assignment_history_prev_idx.sql`
  adds `idx_task_assignment_history_prev ON task_assignment_history (previous_assigned_to, created_at)`; new
  repo method `apps/api/src/modules/sync/repository.ts:113` `revokedAssignmentIdsForUser` powers the
  download-sync `revokedAssignmentIds` purge list; SYNC_SELECT now also reads `ct.started_at, ct.completed_at`
  (repository.ts:52).
  - (a) ✅ COLUMNS EXIST — live `\d task_assignment_history` (dev :54329): `previous_assigned_to uuid` (nullable),
    `created_at timestamptz NOT NULL`, `task_id uuid NOT NULL`, `assigned_to uuid` (nullable) all present; FK
    `task_id → case_tasks(id) ON DELETE CASCADE`. `case_tasks` has `started_at`/`completed_at`/`assigned_to`/`id`
    (information_schema confirmed) ⇒ the SYNC_SELECT additions resolve. No phantom columns.
  - (b) ✅ INDEX COLUMN ORDER CORRECT for the predicate. Query filters `h.previous_assigned_to = $1`
    (equality) AND `h.created_at > $2` (range). Leading equality col then range col is the textbook composite
    order: planner seeks on `previous_assigned_to=$1` then range-scans `created_at>$2` within that prefix. The
    pre-existing `idx_task_assignment_history_task (task_id, created_at DESC)` does NOT serve this path
    (different leading col) — migration comment is accurate. (created_at is plain ASC here, not DESC; immaterial
    — a `>` range scan walks either direction, and there is no ORDER BY on this query.)
  - (c) ✅ SELECTIVITY — `previous_assigned_to` is a single field-user's id ⇒ highly selective on an append-only,
    unbounded-growth table; the `created_at > cutoff` watermark further trims to the delta window. Correct index
    to add now (table grows forever; without it this becomes a Seq Scan as history accumulates). Index size
    bounded by non-NULL rows only is fine (no partial needed — see (e) on NULL).
  - (d) ✅ MIGRATION DISCIPLINE — forward-only + idempotent (`CREATE INDEX IF NOT EXISTS`), wrapped in
    `BEGIN/COMMIT` (single-stmt txn, harmless; differs from 0020/0021's bare autocommit but valid — note it's
    **non-CONCURRENTLY inside an explicit txn**, so a SHARE lock for the build; acceptable on the append-only
    history table at v2 volumes / offline dev apply; would matter only against a large live-write prod table).
    Numbered 0057, correctly sequenced after 0056, no dup.
  - (e) ✅ NULL / `IS DISTINCT FROM` SEMANTICS — `ct.assigned_to IS DISTINCT FROM $1` is the CORRECT operator:
    when a task is UNASSIGNED-away, `case_tasks.assigned_to` becomes NULL, and `NULL <> $1` would yield UNKNOWN
    (row dropped), but `NULL IS DISTINCT FROM $1` yields TRUE ⇒ unassigned tasks ARE included in the purge list,
    matching the documented intent (covers REASSIGNED-away AND UNASSIGNED, excludes reassigned-then-back-to-me).
    A plain `<>` here would have been a real BUG silently leaking unassigned orphans onto the device — this code
    gets it right. The index's `previous_assigned_to` filter uses `= $1` (equality, NULL `previous_assigned_to`
    rows correctly excluded — a row with no previous assignee is not a revocation FROM this user).
  - (f) ✅ JOIN — no row miss / no duplication. `JOIN case_tasks ct ON ct.id = h.task_id`: `h.task_id` is
    `NOT NULL` and FK→`case_tasks(id)` (PK) ⇒ exactly 1 ct per history row, INNER join neither drops nor
    multiplies. Multiple history rows for the same task (e.g. reassigned twice in the window) WOULD emit
    duplicate `task_id`s — but `SELECT DISTINCT h.task_id` collapses them to one id per task. DISTINCT is
    necessary and correct here (purge is set-semantics: device just needs the id once). No GROUP BY needed.
  - (g) ✅ PARAMETERIZATION — both values bound via `$1`/`$2` (`[userId, cutoff.toISOString()]`); zero string
    interpolation in this method. Injection-clean. `cutoff` passed as ISO string into a timestamptz `>` compare
    (implicit cast, deterministic). userId is a uuid bound param.
  - (h) ✅ TRIPLE-WRITE — leg 1 file present; leg 2 dev `crm2_dev@:54329` index CONFIRMED LIVE
    (`btree (previous_assigned_to, created_at)` in `\d`); leg 3 test `crm2_test@:5433` does NOT yet show the
    index (only pkey + _task) — EXPECTED: :5433 has no migration-tracking table (`\dt *migration*` → none) and is
    rebuilt from all .sql at harness boot, so it auto-applies 0057 on the next test run. Per the standing
    "harness auto-applies" leg, triple-write is satisfied; the absence now is the un-booted harness, not a gap.
  - NOTE (perf, non-blocking): SYNC_SELECT's `ct.started_at, ct.completed_at` additions are plain projected
    columns on the already-joined `case_tasks ct` — no new join, no filter, no index impact. Benign.

- **2026-06-16 · Migration 0058 commission_rates + billing slice 5a repo (ADR-0036, uncommitted)** —
  VERDICT **PASS**. New `db/v2/migrations/0058_commission_rates.sql` (per-user effective-dated commission
  config) + `apps/api/src/modules/commissionRates/{repository,service}.ts`. Live dev `\d` matches the
  file byte-for-byte; all 7 module tests pass against :5433 (harness auto-applied the migration).
  - (a) ✅ btree_gist EXCLUSION WORKS LIVE — `CREATE EXTENSION IF NOT EXISTS btree_gist` (0058:16) enables
    `=` ops on the scalar uuid/varchar/int columns inside a GiST exclusion alongside `&&` on the tstzrange.
    Confirmed APPLIED on dev :54329: `pg_extension` has `btree_gist`; constraint def reads exactly
    `EXCLUDE USING gist (user_id WITH =, rate_type WITH =, COALESCE(client_id,'-1') WITH =,
    tstzrange(effective_from, COALESCE(effective_to,'infinity'),'[)') WITH &&) WHERE (is_active)`. The mixed
    scalar-eq + range-overlap exclusion is the canonical btree_gist use; no doubt it functions.
  - (b) ✅ NO-OVERLAP SEMANTICS — ADVERSARIALLY VERIFIED on live dev DB (scratch in a DO block, rolled back,
    0 leftover): (T1) two UNIVERSAL active rows same user+rate_type → 2nd REJECTED (both COALESCE→-1, ranges
    overlap) ✔; (T2) client-scoped + universal same user+rate_type → COEXIST (-1 vs real client_id differ on
    the `=` key) ✔ — this is the INTENDED design (universal is the fallback, client-scoped is the override,
    they are NOT meant to be mutually exclusive); (T3) an INACTIVE row does NOT block an active overlap
    (partial `WHERE is_active`) ✔. So "one ACTIVE row per (user, rate_type, client-scope) over a time range"
    holds, where universal (NULL→-1) is its OWN scope distinct from any real client. The `[)` bound + revise's
    "end-date current FIRST then insert open-ended" ordering (repository.ts:142-154) means an adjacent
    (touching, non-overlapping) succession does not self-conflict. Correct.
  - (c) ✅ RESOLVE INDEX vs resolveAmount predicate — `idx_commission_rates_resolve (user_id, rate_type,
    client_id) WHERE is_active` (0058:48). resolveAmount (repository.ts:194-204) filters
    `user_id=$1 AND rate_type=$2 AND is_active AND (client_id IS NULL OR client_id=$3) AND temporal`. Leading
    cols user_id+rate_type are exact-eq prefix matches → index-usable; the `is_active` partial predicate matches
    the query's `AND is_active`; client_id is the 3rd col (range/OR refinement). Column order is correct
    (most-selective equality prefix first). Tiny config table → seq-scan is fine anyway; index is the right
    shape when it grows. Same index also serves the no-overlap resolution path.
  - (d) ✅ resolveAmount ORDER BY client_id DESC NULLS LAST + LIMIT 1 — for the queried client, candidate rows
    are the client-scoped row (client_id=$3, a positive int) and/or the universal row (NULL). DESC NULLS LAST
    sorts the non-null client_id BEFORE NULL ⇒ client-scoped wins; absent a client-scoped row, NULL (universal)
    is the only/last row → falls back to universal. Test proves 80 (client) vs 50 (universal fallback) vs null
    (unconfigured rate_type). Most-specific-client-wins is correct. NOTE: client_id values are unique per scope
    here (no two positive client_ids can both satisfy `client_id=$3`), so DESC among non-nulls is moot — only
    null-vs-non-null ordering matters; correct regardless.
  - (e) ✅ FORWARD-ONLY / IDEMPOTENT — `BEGIN`/`COMMIT` wrapped; `CREATE EXTENSION IF NOT EXISTS`,
    `CREATE TABLE IF NOT EXISTS`, `CREATE INDEX IF NOT EXISTS`, and the constraint guarded by a DO-block
    `IF NOT EXISTS (SELECT 1 FROM pg_constraint WHERE conname=...)` (0058:35-45) — re-runnable, no error on
    replay. Matches the established 0019/rates pattern.
  - (f) ✅ TRIPLE-WRITE — leg 1 file present; leg 2 dev `crm2_dev@:54329` CONFIRMED LIVE (full `\d` matches,
    btree_gist present, constraint + resolve index present, FKs present); leg 3 test `crm2_test@:5433`
    auto-applies via the harness — PROVEN by running the suite with `DATABASE_URL=...:5433` → `db.migrate()`
    created the table and all 7 tests PASSED (overlap-409, revise end-date+stale-409, OCC (de)activate,
    resolveAmount specificity, RBAC, bad-uuid-400). Post-run the isolated test DB rolls back (table not
    persisted) — expected for the harness, not a gap.
  - (g) ✅ numeric(12,2)::float8 — COLS + list + resolveAmount all alias `amount::float8 AS amount`
    (repository.ts:12,80,196) so pg returns a JS number not a string; tests assert `typeof amount === 'number'`
    and `=== 50`. CHECK `amount >= 0` present live. (Note: float8 is the established v2 money-projection pattern,
    matching `rates`; precision is fine for INR commission magnitudes — no rounding risk flagged.)
  - (h) ✅ INJECTION/PARAM/COUNT — list ORDER BY interpolates only `${o.sortColumn}` resolved from
    `CR_PAGE_SPEC.sortMap` (service.ts:14-24, 8 fixed literals: u.name/c.name/cr.rate_type/cr.amount/
    cr.effective_from/cr.is_active/cr.created_at/cr.updated_at), sortOrder hard-coerced asc/desc by resolvePage;
    every filter value bound via `$n`, search pushed once and reused across the 5 ILIKE clauses; COUNT shares
    `CR_FROM` + same clause/params (repository.ts:72-73), `count(*)::int`. The two joins (`JOIN users u`,
    `LEFT JOIN clients c`) don't multiply rows (user FK NOT NULL 1:1; client LEFT JOIN on PK ≤1) ⇒ COUNT==items.
    `cr.id` integer-PK tiebreak inherits sortOrder → deterministic. amount sort targets raw `cr.amount` (numeric
    ordering, not the float8 alias). Clean.
  - FK CHOICES (NOTE, non-blocking): both FKs are `ON DELETE NO ACTION` (live `confdeltype='a'`) — deleting a
    `users` row or `clients` row that still has commission_rates is BLOCKED at the DB. This is the SAFE default
    for financial config (a rate must not silently vanish / orphan); consistent with v2's soft-delete-via-
    is_active posture (rows are deactivated, users/clients are deactivated not hard-deleted). No CASCADE, no
    SET NULL — correct for an append-only-ish effective-dated money table. No action.
  - ADVERSARIAL SUMMARY: the two questions posed — "two universal rows same user+rate_type?" → NO (T1 rejected);
    "client-scoped + universal overlap wrongly?" → NO, they coexist BY DESIGN (different scope key), and the
    resolver correctly prefers client-scoped (T2 + resolveAmount test). Both behave as intended.

- **2026-06-16 · Billing slice 5b — mig 0059 + per-case aggregate (`listCases`) (uncommitted)** —
  VERDICT **PASS** (perf carries, non-blocking). `db/v2/migrations/0059_billing_view_perm.sql` (untracked) seeds
  `role_permissions(billing.view)` for MANAGER + BACKEND_USER; `apps/api/src/modules/billing/` (untracked
  module) holds the per-case Billing & Commission read-model — `repository.listCases` (GROUP BY case, SUM over two
  chained LEFT JOIN LATERALs: rates ladder + commission), `caseTasks`, `caseVisible`. No schema DDL (perm-seed only).
  - (a) ✅ MIGRATION 0059 — forward-only + idempotent: single `INSERT … ON CONFLICT (role_code, permission_code)
    DO NOTHING`, BEGIN/COMMIT-wrapped (one atomic stmt — fine). Re-runnable (conflict-skip). VERIFIED LIVE dev
    `crm2_dev@:54329`: `SELECT role_code FROM role_permissions WHERE permission_code='billing.view'` → exactly
    {BACKEND_USER, MANAGER}, no others (SUPER_ADMIN via grants_all, not seeded — correct; FE/TL absent — correct
    per ADR-0036, BE lacks billing.* note in RBAC model is about billing.MANAGE not .view, and 5b grants .view to
    BE deliberately as a billing operator). ON CONFLICT target matches the table's natural-key uq. NOTE: this is a
    SEED migration, not DDL — the standing `(role_code,permission_code)` PK/uq must exist for the conflict arbiter;
    confirmed (insert succeeded idempotently). TRIPLE-WRITE: dev leg verified live; test `:5433` leg auto-applies
    via harness; file is leg 1. (Did not re-verify :5433 — perm seed is data, harness re-runs migrations on each
    suite; flag only if a billing perm test 403s.)
  - (b) ✅ PARAMETERIZATION / INJECTION — every filter value bound via `$n` through the `add()` helper
    (repository.ts:77-93): clientId/completedFrom/completedTo each push one param + `$?`→`$N` rewrite; search
    pushes `likeContains()` once, reused across 3 ILIKE clauses via one `$N` (repository.ts:84-88); `ids` bound as
    `= ANY($N)` array; columnFilters via `filterClauses(...,params)`. ORDER BY interpolates only `${o.sortColumn}`
    resolved from `BILLING_PAGE_SPEC.sortMap` (service.ts:15-24, 8 fixed literals incl. aggregate aliases
    bill_total/commission_total/completed_task_count/last_completed_at), `sortOrder` hard-coerced asc/desc by
    resolvePage. clientId server-coerced `Number.isInteger && >0` (service.ts:34-37). Clean — no concat of user input.
  - (c) ✅ COUNT CORRECTNESS — `count(DISTINCT cs.id)::int` over the SAME `CASES_FROM` + `clause`/`params` as items
    (repository.ts:98-101). CASES_FROM fans OUT one row per COMPLETED case_task (the grain), so a plain `count(*)`
    would over-count multi-task cases; `count(DISTINCT cs.id)` correctly counts cases == the GROUP-BY cardinality
    of the items query. The two LATERALs are `LIMIT 1` (1:1, can't multiply); JOIN clients/products are NOT-NULL
    FK 1:1 (no drop/multiply). So count(DISTINCT cs.id) == #grouped rows. ✅ The DISTINCT is REQUIRED here (unlike
    the cases-list lean COUNT which has no fan-out) and is correct.
  - (d) ✅ DATE FILTERS PRE-AGGREGATION — `ct.completed_at >= /<= $n` land in the WHERE `clause`
    (repository.ts:82-83), i.e. BEFORE the GROUP BY, so they prune case_tasks rows pre-aggregation (correct: the
    bill/commission SUM and `max(completed_at)` reflect only the completed tasks inside the window, not a
    post-aggregate HAVING filter). `ct.status='COMPLETED'` likewise pre-aggregation (repository.ts:75). Both
    semantically and performance-correct (prune before the expensive per-task laterals run).
  - (e) ⚠️ INDEX COVERAGE — the question posed ("does idx_rates_resolve cover the rate lateral / does
    idx_commission_rates_resolve cover the commission lateral") — answer is SUBTLE. EXPLAIN on live dev
    (28 case_tasks / 9 COMPLETED / 4 rates / 4 commission_rates) shows the planner choosing NEITHER btree resolve
    index — it picks the **GiST `rates_no_overlap`** for the rate lateral (`Index Cond: client_id=cs.client_id AND
    product_id=cs.product_id AND verification_unit_id=ct.verification_unit_id`, then in-mem Sort on the 5-level
    location CASE ladder → Limit 1) and **GiST `commission_rates_no_overlap`** for the commission lateral
    (`Index Cond: user_id=ct.assigned_to AND rate_type=r.rate_type`, Sort client_id DESC → Limit 1). This is PARTLY
    a small-data artifact (n=4 → any index is ~free), BUT it is also STRUCTURAL: both GiST exclusion indexes LEAD on
    the exact equality-prefix columns of the lateral predicates ((client_id,product_id,verification_unit_id) and
    (user_id,rate_type)) AND additionally cover the tstzrange temporal, so they are a genuine competitor — meaning
    the btree `idx_rates_resolve (client_id,product_id,verification_unit_id,location_id) WHERE is_active` and
    `idx_commission_rates_resolve (user_id,rate_type,client_id) WHERE is_active` are SUFFICIENT to serve these
    laterals (the leading equality cols are exactly the predicate equalities), but are NOT NECESSARILY CHOSEN over
    the GiST. At PROD scale (many rates per client×product, many commission rows per user) the partial-btree
    `idx_rates_resolve`/`idx_commission_rates_resolve` are the cheaper, more selective access path for the equality
    lookup and the planner should prefer them then. NET: rate lateral IS covered by idx_rates_resolve (equality
    prefix match); commission lateral IS covered by idx_commission_rates_resolve (equality prefix match). The
    location_id 4th col of idx_rates_resolve does NOT serve the ORDER-BY ladder (the ladder is a per-row CASE over
    several runtime values ct.area_id/ct.pincode_id/cs.area_id/cs.pincode_id — NOT a fixed sort key any index can
    pre-order), so a tiny in-memory Sort of the candidate rows (≤ rows-per-client×product×unit, normally a HANDFUL)
    + Limit 1 is unavoidable and ACCEPTABLE. No new index needed for coverage. ✅
  - (f) ⚠️ SCALABILITY — the per-task DOUBLE-LATERAL is genuinely O(N_completed_tasks_in_window × 2 index-probes),
    one rate-probe + one commission-probe per COMPLETED task, then GROUP BY case. This is the honest cost. Mitigants
    that make it ACCEPTABLE at the current design point: (1) the WHERE prunes to COMPLETED tasks INSIDE the
    completed_at window pre-lateral — a billing operator queries a bounded date range (a month/quarter), not all
    history; (2) each probe is an index equality lookup returning a HANDFUL of candidate rate/commission rows (rates
    is a SMALL config table — one active row per client×product×unit×location, plus a tiny temporal tail), so each
    LIMIT-1 lateral is sub-ms; (3) page LIMIT bounds the OUTPUT but NOT the lateral work (laterals run over the full
    filtered case_task set BEFORE GROUP BY + ORDER BY + LIMIT — see (g)). RATCHET (carries, non-blocking): the cost
    scales with the SIZE OF THE DATE WINDOW (count of completed tasks in range), not with page size. As completed-task
    volume grows, ensure the `ct.status='COMPLETED' + ct.completed_at` filter is index-backed so the pre-lateral row
    set is gathered cheaply: TODAY case_tasks has `idx_case_tasks_status (status)` and (separately)
    no composite (status, completed_at). A **partial composite `idx_case_tasks_completed ON case_tasks(completed_at)
    WHERE status='COMPLETED'`** (or `(completed_at) WHERE status='COMPLETED'`) would let a date-windowed billing run
    gather exactly the in-range completed tasks via an index range-scan instead of `idx_case_tasks_status` →
    full-COMPLETED-set filter-by-date. NOT needed now (28 rows → seq scan wins; EXPLAIN shows Seq Scan on case_tasks),
    but this is the FIRST index to add when completed-task volume climbs — it directly bounds the lateral fan-out.
    PROPORTIONATE: do not add prophylactically.
  - (g) ⚠️ ORDER BY ON AGGREGATE ALIASES — confirmed CANNOT use an index. `ORDER BY ${o.sortColumn} ${o.sortOrder},
    cs.id ${o.sortOrder}` where default sortColumn=`last_completed_at` (= `max(ct.completed_at)`, an aggregate),
    others billTotal/commissionTotal/completedTaskCount are likewise SUM/count aggregates. EXPLAIN confirms a
    top-level `Sort (Sort Key: max(ct.completed_at) DESC, cs.id DESC)` over the GROUPED set, then Limit — the sort is
    over the post-GROUP-BY rows (= #cases in the filtered window), which is small relative to the task fan-out, so
    sorting the grouped set at page time is ACCEPTABLE. There is NO index that can pre-order an aggregate; this is
    inherent to a GROUP-BY-then-rank read-model and fine at billing page sizes. The non-aggregate sort targets
    (cs.case_number/cl.name/p.name/cs.status) ALSO sort the grouped set (the GROUP BY already materialised them) —
    same acceptable cost. The `cs.id` tiebreak makes ordering deterministic across OFFSET windows (uuid PK, total
    order). ✅ NOTE (minor, non-blocking): the GROUP BY lists `cs.id, cs.case_number, cl.name, p.name, cs.status`
    (5 cols) — since `cs.id` is the PK, PG's functional-dependency rule means grouping by `cs.id` alone would
    suffice for cs.* cols, but cl.name/p.name belong to OTHER tables so they MUST be listed (PG won't infer FK
    functional dependency across the join) — the explicit 5-col GROUP BY is CORRECT, not redundant. (Observed quirk:
    EXPLAIN's GroupAggregate showed Group Key without cs.id/cs.status because the planner proved them functionally
    dependent on case_number+the row — harmless plan-side optimisation; the written SQL is correct and portable.)
  - (h) ✅ caseTasks / caseVisible — `caseTasks` reuses the SAME two LATERALs filtered to one `cs.id=$1` COMPLETED set,
    ORDER BY ct.task_number (bounded to one case's tasks — trivially small, no perf concern; FK-backed by
    idx_case_tasks_case). `caseVisible` is a `SELECT 1 … WHERE cs.id=$1 [AND scope] LIMIT 1` PK lookup — O(1). Scope
    predicate (`caseScopePredicate`) is parameterized via `composeScopePredicate(params,…)` and applied as
    defence-in-depth on top of the `billing.view` gate; the EXISTS-over-case_tasks leg is index-backed by
    idx_case_tasks_case / idx_case_tasks_assigned. Clean.
  - ADVERSARIAL SUMMARY: the read-model is correct (DISTINCT count, pre-agg date prune, 1:1 laterals, deterministic
    sort) and the perm seed is verified live. The ONLY scale concern is the per-completed-task double-lateral, whose
    cost tracks the date-window size, NOT page size — bounded today by small config tables + operators querying a
    date range. CARRIES (perf, non-blocking, do NOT add now): partial composite `idx_case_tasks(completed_at) WHERE
    status='COMPLETED'` is the right first index when completed-task volume climbs, to bound the pre-lateral row set.
    No covering index can serve the location-ladder ORDER BY (per-row CASE) or the aggregate-alias top ORDER BY — both
    are inherent in-mem sorts of small grouped/candidate sets and acceptable. No BLOCK, no FLAG.

- **2026-06-16 · ADR-0037 MIS engine Slice 1 — migration `0060_report_layouts.sql` + reportLayouts repository** — **VERDICT: PASS.**
  - (a) ✅ **MIGRATION SHAPE — forward-only + idempotent.** Wrapped in `BEGIN;`/`COMMIT;` (0060:9,55). Both tables `CREATE TABLE IF NOT EXISTS` (0060:11,31), all 3 indexes `CREATE [UNIQUE] INDEX IF NOT EXISTS` (0060:25,28,52). Re-applied live on dev `crm2_dev@:54329` → NOTICE skips + COMMIT, zero error. No DROP, no destructive DDL. Forward-only confirmed.
  - (b) ✅ **PARTIAL-UNIQUE-ACTIVE INDEX correctness.** `uq_report_layouts_active ON report_layouts (client_id, product_id, kind) WHERE is_active` (0060:25-26). The partial predicate `WHERE is_active` means the unique constraint applies ONLY to rows where `is_active=true` → exactly ONE active per (client,product,kind), but UNLIMITED inactive rows for the same triple (deactivated history accumulates freely). Live def confirmed: `UNIQUE, btree (client_id, product_id, kind) WHERE is_active`. PROVEN by API test "enforces ONE active layout … → 409" + "deactivate frees the slot; reactivating over a new active → 409" (both green). Repo maps `23505/uq_report_layouts_active` → 409 REPORT_LAYOUT_EXISTS (repository.ts:21-25); `setActive` reactivation collides on the same index (repository.ts:217-219 doc). Correct.
  - (c) ✅ **FK + ON DELETE CASCADE — no orphan risk.** `report_layout_columns.layout_id → report_layouts(id) ON DELETE CASCADE` (0060:33). CASCADE is the correct disposition: columns are owned-children of a layout, meaningless without it. Adversarial orphan check: the module NEVER hard-deletes a layout — there is NO `DELETE FROM report_layouts` anywhere in repository.ts (deactivation is `setActive(is_active=false)`, a soft UPDATE; `update()` does `DELETE FROM report_layout_columns WHERE layout_id=$1` then re-inserts — child-only replace within one tx, repository.ts:205-206). So CASCADE is latent (never triggered in normal flow) but CORRECT as a safety net if a layout is ever admin-deleted out-of-band. The child-replace in `update()` cannot orphan: it's inside `withTransaction` (repository.ts:191), DELETE+re-INSERT atomic. NO orphan path.
  - (d) ✅ **CHECK constraints ≡ @crm2/sdk enums — ZERO drift (verified member-by-member).**
    · `kind CHECK IN ('DATA_ENTRY','MIS','BILLING_MIS')` (0060:15) ≡ `LAYOUT_KINDS` (reportLayouts.ts:10) — exact, same order. ✅
    · `data_type CHECK IN ('TEXT','NUMBER','DATE','SELECT','BOOLEAN')` (0060:41) ≡ `COLUMN_DATA_TYPES` (reportLayouts.ts:13) — exact. ✅
    · `source_type CHECK IN (TASK_FIELD,CASE_FIELD,APPLICANT_FIELD,RATE_AMOUNT,COMMISSION_AMOUNT,TAT,DATA_ENTRY_FIELD,FORM_DATA_PATH,DOC_TYPE_COUNT,COMPUTED)` (0060:37-39, 10 members) ≡ `SOURCE_TYPES` (reportLayouts.ts:16-27, 10 members) — exact set + order. ✅ SDK self-test `reportLayouts.test.ts` (14 tests green) enforces every SOURCE_TYPES member has a catalog entry, so SDK-internal coherence is gated too.
  - (e) ✅ **OCC `version` present + guarded.** `version integer NOT NULL DEFAULT 1` (0060:18). `update()` is pre-read `FOR UPDATE` + `cur.version !== expectedVersion → AppError.stale` then `version=version+1` (repository.ts:192-202); `setActive()` is compare-and-set `WHERE id=$1 AND version=$4 … version=version+1`, 0-rows→stale (repository.ts:228-233). Mirrors the proven ADR-0019 pattern. PROVEN by "updates name + replaces columns (OCC); stale version → 409" + "OCC throughout" tests (green).
  - (f) ✅ **INDEXES serve the actual queries.** `findActiveByConfig` filters `WHERE client_id=$1 AND product_id=$2 AND kind=$3 AND is_active LIMIT 1` (repository.ts:153-156) — served by the PARTIAL UNIQUE `uq_report_layouts_active` (leftmost-prefix exact match on all 3 cols, partial WHERE is_active matches the predicate) → index-only/single-row probe, optimal. The non-partial `idx_report_layouts_lookup (client_id,product_id,kind)` (0060:28) backs list filters by any prefix of those cols INCLUDING the inactive set (list can filter `active=false`/all). Two indexes are NON-redundant: partial serves the hot active-lookup; full serves arbitrary-active list filters. `report_layout_columns(layout_id, display_order)` (0060:52) backs both `columnsFor` (`WHERE layout_id=$1 ORDER BY display_order,id`, repository.ts:71 — index pre-orders, no sort node) AND the FK-join/child-delete. Correct.
  - (g) ✅ **column-count subquery — acceptable, NOT a concern.** The list projects `(SELECT count(*)::int FROM report_layout_columns rc WHERE rc.layout_id=rl.id) AS column_count` (repository.ts:118) — a correlated subquery per row, but (i) bounded by the page LIMIT (repository.ts:121), (ii) index-backed by `idx_report_layout_columns_layout(layout_id,…)` (leftmost = layout_id) → index-only count per row, (iii) runs at CONFIG scale (one layout per client×product×kind — tens, not thousands). This is bounded-within-one-statement, not app-level N+1. Same posture I approved for cases applicant_count/task_count. ACCEPTABLE; do not rewrite.
  - (h) ✅ **jsonb defaults.** `options jsonb NOT NULL DEFAULT '[]'::jsonb`, `validation jsonb NOT NULL DEFAULT '{}'::jsonb` (0060:45-46). Sensible empty-array/empty-object defaults; repo always `JSON.stringify(... ?? [] / {})` on insert (repository.ts:62-63) so NULL never reached anyway. Live confirmed.
  - (i) ✅ **TRIPLE-WRITE CONFIRMED.** Leg 1 = the file. Leg 2 (dev `crm2_dev@:54329`): `\d report_layouts` + `\d report_layout_columns` both present with ALL constraints/indexes/FK-CASCADE exactly as specced (verified live this session, not trusting handoff). Leg 3 (test harness): `crm2_test@:5433` holds NO persistent copy (it's stale — also missing 0058/0051) BY DESIGN — `packages/test-utils/src/helpers/testDb.ts:19,61` `buildTemplateDb` replays EVERY `db/v2/migrations/*.sql` (sorted) into a fresh `_template` DB per suite-run via vitest globalSetup, then per-file clones; the 0060 FILE is the test leg. PROVEN: ran `reportLayouts.api.test.ts` → 6/6 green against the freshly-migrated template (0060 replayed clean in-harness). The earlier-noted absence in :5433 is expected (template+clones are torn down on teardown).
  - (j) ✅ **MIGRATION NUMBERING.** `0060` is correct — `ls db/v2/migrations` ends `…0058_commission_rates, 0059_billing_view_perm, 0060_report_layouts`. No duplicate, sequential, next-free number. (Historical gaps like 0005 are pre-existing and harmless to the file-based forward-only runner.)
  - PERF NOTE (non-blocking, config-scale): default list sort `updatedAt→rl.updated_at` and sortMap targets (c.name/p.name/rl.kind/rl.name/rl.is_active/created_at) are UNINDEXED for ordering, and the list search is a leading-wildcard `ILIKE %q%` on rl.name/c.name/p.name (seq-scan, no trgm). DELIBERATELY NOT flagged for indexes — report_layouts is a small config table (one row per client×product×kind, plus deactivated history); seq-scan + in-mem sort is correct at this scale (same size-proportionate posture I applied declining trgm on `rates` in DataGrid Slice 4). Re-raise ONLY if layout volume climbs materially.
  - DISPOSITION: **PASS**, merge-ready. No FLAG, no BLOCK. Migration is forward-only/idempotent, partial-unique enforces exactly-one-active, CASCADE safe (never triggered, correct net), all 3 CHECKs byte-match the SDK enums, OCC present, indexes match access paths, triple-write satisfied. 6/6 API + 14/14 SDK tests green.

- **2026-06-16 · ADR-0037 MIS engine Slice 3a — migration `0061_case_data_entries.sql` + caseDataEntries repository + `data_entry.manage` perm** — **VERDICT: PASS.**
  - (a) ✅ **MIGRATION SHAPE — forward-only + idempotent.** Wrapped `BEGIN;`/`COMMIT;` (0061:10,32). `CREATE TABLE IF NOT EXISTS case_data_entries` (0061:12), `CREATE INDEX IF NOT EXISTS idx_case_data_entries_layout` (0061:25), `INSERT … ON CONFLICT (role_code, permission_code) DO NOTHING` (0061:27-30). Re-runnable: re-applied live on dev `crm2_dev@:54329` → NOTICE skip on table+index, INSERT 0 rows on conflict, COMMIT, zero error. No DROP / no destructive DDL.
  - (b) ✅ **FK CORRECTNESS + TYPE MATCH (verified live `\d`).** `task_id uuid → case_tasks(id)` — `case_tasks.id uuid PRIMARY KEY` (0010_cases.sql:56), uuid≡uuid ✅. `layout_id integer → report_layouts(id)` — `report_layouts.id integer GENERATED ALWAYS AS IDENTITY PK` (0060:12), int≡int ✅. Live constraints confirmed: `case_data_entries_task_id_fkey`→case_tasks, `case_data_entries_layout_id_fkey`→report_layouts. NO orphan risk: neither parent is ever hard-deleted — case_tasks soft-transition only; report_layouts is soft-only (`setActive(is_active=false)`, NO `DELETE FROM report_layouts` anywhere — re-confirmed this session). **NO-CASCADE is CORRECT here** (unlike 0060's column-CASCADE): a data-entry row is NOT an owned-child of the layout — it's the operator's keyed MIS VALUES whose `layout_id` records *which layout they were keyed against* (audit/provenance + the immutable-once-used signal). Cascading its deletion on a (non-existent) layout delete would silently destroy real operator data; RESTRICT-by-default (no action) is the safe disposition. Layout deactivation does NOT touch data entries (intended — deactivated layouts with keyed data stay immutable-once-used, repository.ts:198-205). Confirmed: layouts soft-only ⇒ no FK-block ever fires in normal flow; FK is a latent integrity net.
  - (c) ✅ **UNIQUE(task_id) enforces one-entry-per-task — the upsert relies on it.** `CONSTRAINT uq_case_data_entries_task UNIQUE (task_id)` (0061:21); live = `uq_case_data_entries_task UNIQUE CONSTRAINT, btree (task_id)`. The repo's first-save `insert()` is a plain INSERT and catches `23505` → 409 `DATA_ENTRY_EXISTS` "created concurrently — reload" (repository.ts:48-60) — so a racing double first-save is serialized by this constraint (one wins, the other 409s, no dup row). `findByTask`/`update` are keyed on `task_id` (repository.ts:35,72,78) and rely on ≤1 row per task — the UNIQUE guarantees it. Correct.
  - (d) ✅ **OCC `version` present + guarded (ADR-0019).** `version integer NOT NULL DEFAULT 1` (0061:17). `update()` pre-reads the row → 404 if absent, then `UPDATE … SET version=version+1 … WHERE task_id=$1 AND version=$4`; 0 rows → `AppError.stale(before)` surfacing the fresh row (repository.ts:71-82). Mirrors the proven 0060/ADR-0019 pattern. Exercised green by the API test "OCC on update".
  - (e) ✅ **ROLES-PARITY INVARIANT — migration seed ↔ @crm2/access agree EXACTLY (both sides verified).** Migration INSERTs `('MANAGER','data_entry.manage')` + `('BACKEND_USER','data_entry.manage')` (0061:27-30). @crm2/access: `DATA_ENTRY_MANAGE:'data_entry.manage'` (permissions.ts:53), granted to **MANAGER** (permissions.ts:99) + **BACKEND_USER** (permissions.ts:121), SA via `SUPER_ADMIN: Object.values(PERMISSIONS)` grants_all (permissions.ts:86 — NOT listed in role_permissions, correct: parity test asserts SA holds ZERO explicit rows), TL/FE/KYC absent on both sides. `PERMISSION_META['data_entry.manage']` present (permissions.ts:148). Live dev seed = exactly `{BACKEND_USER, MANAGER}` (2 rows, queried this session). **PROVEN by `roles.parity.test.ts` "DB-resolved permission set is byte-identical to ROLE_PERMISSIONS for all 6 roles" → 4/4 GREEN** against the freshly-migrated `:5433` template (so 0061's INSERT replayed in-harness AND parity held). Any future drift (seed vs constant) fails this gate.
  - (f) ✅ **IDX SERVES THE IMMUTABLE-ONCE-USED GUARD.** `idx_case_data_entries_layout ON case_data_entries(layout_id)` (0061:25; live `btree (layout_id)`). The guard in reportLayouts/repository.ts:201-203 is `SELECT 1 FROM case_data_entries WHERE layout_id = $1 LIMIT 1` — leftmost-exact on the index → single-probe existence check, optimal. Without the idx this would seq-scan on every layout structural-edit; the idx is justified by exactly this query path.
  - (g) ✅ **jsonb default.** `data jsonb NOT NULL DEFAULT '{}'::jsonb` (0061:16; live confirmed). Repo always passes `$3::jsonb` from `JSON.stringify(data)` (repository.ts:50,52,77,79) so NULL never reached; default is the empty-object fallback. Sensible.
  - (h) ✅ **MIGRATION NUMBERING.** `0061` is sequential/next-free — `ls db/v2/migrations` ends `…0059_billing_view_perm, 0060_report_layouts, 0061_case_data_entries`. No duplicate. Pre-existing historical gaps harmless to the file-based forward-only runner.
  - (i) ✅ **TRIPLE-WRITE CONFIRMED.** Leg 1 = the file. Leg 2 (dev `crm2_dev@:54329`): `\d case_data_entries` present with all cols/FKs/UNIQUE/idx + `role_permissions` holds the 2 rows — both verified live this session (not trusting handoff). Leg 3 (test harness `:5433`): `buildTemplateDb` replays every `db/v2/migrations/*.sql` into a fresh `_template` per run — **PROVEN: ran `caseDataEntries.api.test.ts` (3/3) + `roles.parity.test.ts` (4/4) GREEN** against the freshly-migrated template, so 0061 (table + idx + perm INSERT) replayed clean in-harness.
  - (j) ✅ **N+1 — data-entry GET/save sequential point queries ACCEPTABLE at office grain.** The path does a few SEQUENTIAL single-row point queries: `taskScope` (1 row, PK + scope predicate on case_tasks⋈cases), `findActiveByConfig`→layout (partial-unique probe) + `columnsFor` (index-ordered), `findByTask` (UNIQUE(task_id) probe), then insert/update (1 row). These are a handful of indexed single-row round-trips per request — NOT a per-N loop. This is interactive OFFICE data-entry (one operator keying one task at a time), not a list/batch path; total latency = a few ms of indexed probes. No N+1, no batch fan-out. ACCEPTABLE; do not fold.
  - DISPOSITION: **PASS**, merge-ready. No FLAG, no BLOCK. Forward-only/idempotent; FKs type-match with the correct NO-CASCADE disposition (data entries are provenance, not owned-children; parents soft-only ⇒ no orphan/no FK-block); UNIQUE(task_id) backs the upsert; OCC present+guarded; **roles-parity green (MANAGER+BACKEND_USER on both sides, SA via grants_all, parity test 4/4)**; layout idx serves the immutable-once-used guard; jsonb default; numbering 0061 next-free; triple-write satisfied. caseDataEntries 3/3 + parity 4/4 green.

- **2026-06-16 · ADR-0037 MIS engine Slice 3 RE-GRAIN — migration `0062_case_data_entries_case_grain.sql` (drops 3a's task-grain table, recreates at CASE grain) + caseDataEntries/repository.ts rewrite** — **VERDICT: PASS.** Re-grain rationale (Zion `NewDataQC` keys MIS fields once per CASE, not per task; layout grain is per client/product applied at case level). 3a table held only 2 dev test rows (no prod v2 DB) → drop+recreate is data-safe.
  - (a) ✅ **MIGRATION SHAPE — forward-only, DROP-then-CREATE, idempotent-on-replay.** Wrapped `BEGIN;`/`COMMIT;` (0062:9,28). `DROP TABLE IF EXISTS case_data_entries` (0062:11) then `CREATE TABLE` (unconditional) + `CREATE INDEX IF NOT EXISTS` (0062:25). **VERIFIED by full-chain replay**: built scratch DB on `:54329`, applied ALL `db/v2/migrations/*.sql` in `sort()` order with `ON_ERROR_STOP=1` → exit 0; `0061` creates task-grain table, then `0062` drops it + recreates case-grain — clean, no error, no conflict. **Re-ran `0062` alone twice more → exit 0 both times, still case-grain.** This matches the test harness exactly: `packages/test-utils/src/helpers/testDb.ts:62-64` = `readdirSync(MIGRATIONS_DIR).filter(.sql).sort()` applied sequentially into a fresh `_template` per `buildTemplateDb()` run (vitest.globalSetup.ts:9) — so 0061→0062 replays the same drop+recreate. **CAVEAT (recorded, NOT blocking):** `DROP TABLE IF EXISTS` is **data-destructive on re-run** — re-applying 0062 to a populated DB discards all rows (unlike 3a's preserving `CREATE IF NOT EXISTS`). Safe ONLY because the documented precondition (no prod v2 DB, 2 dev test rows) holds and the harness always starts from a fresh template. A standard re-grain (drop+recreate is the only sane forward path for a PK/grain change); just never replay 0062 against a DB holding real keyed data.
  - (b) ✅ **FK CORRECTNESS + TYPE MATCH (verified live `\d` on dev `:54329` AND scratch replay).** `case_id uuid → cases(id)` — `cases.id uuid PRIMARY KEY gen_random_uuid()` (0010), uuid≡uuid ✅. `layout_id integer → report_layouts(id)` — `report_layouts.id integer GENERATED ALWAYS AS IDENTITY PK` (0060), int≡int ✅. Live constraints confirmed: `case_data_entries_case_id_fkey`→cases, `case_data_entries_layout_id_fkey`→report_layouts. NO-CASCADE disposition unchanged + still correct: neither parent hard-deleted (cases soft-lifecycle; report_layouts soft `setActive` only) ⇒ FK is a latent integrity net, never fires; a data-entry row is operator-keyed MIS values (provenance), not an owned-child — cascading its delete would destroy real data. Correct.
  - (c) ✅ **UNIQUE(case_id) serializes racing first-saves.** `CONSTRAINT uq_case_data_entries_case UNIQUE (case_id)` (0062:23); live = `uq_case_data_entries_case UNIQUE CONSTRAINT, btree (case_id)`. Repo `insert()` (repository.ts:51-72) is a plain INSERT catching `23505` → 409 `DATA_ENTRY_EXISTS` "created concurrently — reload" (repository.ts:68-69) — a concurrent double first-save on the same case is serialized (one wins, other 409s, no dup row). `findByCase`/`update` keyed on `case_id` (repository.ts:45,82,88) rely on ≤1 row per case — the UNIQUE guarantees it. One-data-entry-per-case = the intended re-grain semantic. Correct.
  - (d) ✅ **OCC `version` present + guarded (ADR-0019).** `version integer NOT NULL DEFAULT 1` (0062:18). `update()` (repository.ts:75-93) pre-reads → 404 `DATA_ENTRY_NOT_FOUND` if absent, then `UPDATE … SET version=version+1 … WHERE case_id=$1 AND version=$4`; 0 rows → `AppError.stale(before)` surfacing the fresh row. Mirrors proven 0060/0061/ADR-0019 pattern; only the key column flipped task_id→case_id. Correct.
  - (e) ✅ **jsonb param-binding — no interpolation.** `data jsonb NOT NULL DEFAULT '{}'::jsonb` (0062:16). Repo always binds `$3::jsonb` from `JSON.stringify(data)` on both insert (repository.ts:60,62) and update (repository.ts:87,89) — parameterized, NULL never reached, no string interpolation of values. Correct.
  - (f) ✅ **IMMUTABLE-ONCE-USED GUARD STILL SERVED — grain-agnostic, unchanged.** `idx_case_data_entries_layout ON case_data_entries(layout_id)` (0062:25; live `btree (layout_id)`). The guard at reportLayouts/repository.ts:201-204 = `SELECT 1 AS one FROM case_data_entries WHERE layout_id = $1 LIMIT 1` (only fires when `patch.columns` present, repository.ts:200) — keys off `layout_id`, NOT the grain column, so the task→case re-grain does NOT affect it. Index serves it as a single-probe existence check (leftmost-exact). Confirmed source unchanged this session. Correct.
  - (g) ✅ **NO PERM CHANGE — `data_entry.manage` seed untouched.** 0062 carries no `role_permissions` INSERT (correct — 0061 already seeded MANAGER+BACKEND_USER; re-grain is structural only). Roles-parity gate (`roles.parity.test.ts`) unaffected.
  - (h) ✅ **N+1 — case-grain GET/save = handful of sequential indexed point queries.** Path: `caseScope` (1 row, cases PK + scope predicate on cases⋈case_tasks, repository.ts:31-41) → layout lookup → `findByCase` (UNIQUE(case_id) probe, repository.ts:43-49) → insert/update (1 row). Interactive office data-entry (one operator, one case) — NOT a list/batch fan-out. No N+1. Note the scope predicate (repository.ts:18-26) now uses `cs.created_by` + `EXISTS(case_tasks ct WHERE ct.case_id=cs.id AND ct.assigned_to=ANY)` — the EXISTS is backed by `idx_case_tasks_case` (0010); acceptable for a single-case point lookup.
  - (i) ✅ **TRIPLE-WRITE CONFIRMED.** Leg 1 = the file (staged). Leg 2 (dev `crm2_dev@:54329`): `\d case_data_entries` shows `case_id uuid` + `uq_case_data_entries_case` UNIQUE + `idx_case_data_entries_layout` + both FKs (cases, report_layouts), and **NO `task_id`/`uq_case_data_entries_task` remain** — re-grain applied live, verified this session (not trusting handoff). Leg 3 (test harness `:5433`/`crm2_test`): the persistent DB has no table (ephemeral `_template` per run); **PROVEN equivalent by replaying the entire `db/v2/migrations/*.sql` chain in harness `sort()` order into a scratch DB → 0062's drop+recreate produced exactly `case_id uuid → cases` + `uq_case_data_entries_case` + idx, exit 0.** Harness builder = `testDb.ts:62-64` (same sort+sequential apply).
  - DISPOSITION: **PASS**, merge-ready. No FLAG, no BLOCK. Re-grain is forward-only; full-chain + isolated re-replay clean & idempotent-on-replay; FKs type-match (case_id→cases uuid≡uuid, layout_id→report_layouts int≡int) with correct NO-CASCADE; UNIQUE(case_id) serializes racing first-saves (23505→409 in repository.ts:68-69); OCC version present+guarded; jsonb `$N::jsonb`-bound no-interpolation; immutable-guard grain-agnostic & still index-served; triple-write satisfied (dev verified live, harness proven by chain replay). OPEN (carried, non-blocking): the `DROP TABLE IF EXISTS` makes 0062 **data-destructive if ever replayed against a populated DB** — safe under the stated no-prod/2-test-row precondition + fresh-template harness, but DON'T-REGRESS = never re-run 0062 on a DB holding real keyed data-entry rows.

- **2026-06-16 · CASE-DETAIL slice — migration `0063_case_pickups.sql` (new `case_pickups` per-case fixed office form, Zion `NewDataQC` "Pickup Information") + `caseDataEntries/repository.ts` pickup CRUD + `cases/repository.ts` `listFieldPhotos` read — STAGED (`git diff --cached`) — VERDICT: PASS. No FLAG. No BLOCK.** Adversarial review of the new table + two read/write surfaces. Migration source verified at `db/v2/migrations/0063_case_pickups.sql` (1156 B); migration numbering unique (0060–0063, no dupe).
  - (a) ✅ **MIGRATION SHAPE — forward-only + idempotent.** `BEGIN;`/`COMMIT;` wrapped (0063:9,25). `CREATE TABLE IF NOT EXISTS case_pickups` (0063:11). No DROP, no destructive DDL, no ALTER — pure additive new table. Idempotent: re-apply = NOTICE skip. UNIQUE inline `CONSTRAINT uq_case_pickups_case UNIQUE (case_id)` (0063:23). The table has NO secondary indexes beyond PK + the unique (read path is a UNIQUE(case_id) point probe — covered).
  - (b) ✅ **FK + TYPES.** `case_id uuid NOT NULL REFERENCES cases (id)` — type-match uuid≡cases.id uuid, NO CASCADE (a case delete is blocked while a pickup exists — RESTRICT default, consistent with cases-grain siblings; cases are not hard-deleted in this system). Timestamp cols `timestamptz` (pickup_date/reported_date/visit_date_time) — TZ-aware, correct for ISO-datetime input. `pickup_trigger`/`sampler_name varchar(200)` — bounded, matches the `.max(200)` zod cap. `version integer NOT NULL DEFAULT 1` (OCC). `created_by`/`updated_by uuid` (nullable; staff uuids, set from JWT on write). `created_at`/`updated_at timestamptz NOT NULL DEFAULT now()`. `id integer GENERATED ALWAYS AS IDENTITY PRIMARY KEY` (surrogate; case_id is the natural business key via the unique).
  - (c) ✅ **UNIQUE(case_id) = one-pickup-per-case + race serialization.** `uq_case_pickups_case UNIQUE (case_id)` enforces the "one pickup row per case" invariant at the DB. A concurrent first-save on the same case → second INSERT hits 23505 → `insertPickup` maps it to `AppError.conflict('PICKUP_EXISTS', …)` → 409 (repository.ts insertPickup catch: `code === '23505'`). The save service branches insert-vs-update on `findPickupByCase` (TOCTOU window between the check and the insert is closed by the unique → 409, not a duplicate row). Correct.
  - (d) ✅ **OCC version present + guarded on UPDATE.** `updatePickup` reads `before = findPickupByCase` (→404 PICKUP_NOT_FOUND if gone), then `UPDATE … SET version = version + 1 … WHERE case_id = $1 AND version = $8 RETURNING …`; 0 rows → `AppError.stale(before)` → 409 (surfaces the fresh row). Service sources `expectedVersion` via `requireVersion(input)` (→400 if absent once a row exists). Insert defaults v1 at the DB. Test-proven: no-version→400, stale(99)→409, correct(1)→200 v2.
  - (e) ✅ **PARAMS BOUND — zero interpolation.** All pickup queries use `$N` placeholders only: `pickupContext` binds `$1=caseId`; `findPickupByCase` `$1=caseId`; `insertPickup` binds `$1..$7` (caseId, 5 fields, userId reused for created_by+updated_by); `updatePickup` binds `$1..$8` (caseId, 5 fields, userId, expectedVersion). `PICKUP_COLS` is a server-literal column list (aliases only). The `pickupContext` subquery (`string_agg(DISTINCT vu.name …)`) interpolates nothing user-controlled — only the bound `$1=caseId`. `listFieldPhotos` binds `$1=caseId` + scope arrays pushed by `taskScopePredicate`/`composeScopePredicate` (`$N::uuid[]`), `kind = 'FIELD_PHOTO'` is a server literal.
  - (f) ✅ **N+1 — page-bounded, no fan-out.** Pickup GET = `caseScope` (1 row) → `pickupContext` (1 row: cases⋈clients + a correlated case_tasks⋈verification_units string_agg subquery, bounded by the case's task count — a single case has a handful of units, not unbounded) → `findPickupByCase` (UNIQUE(case_id) point probe). Save adds insert/update (1 row) + a second `pickupContext`. Interactive one-operator/one-case office form — NOT a list/batch. `listFieldPhotos` = ONE query: `case_attachments` filtered `case_id=$1 AND kind='FIELD_PHOTO' AND deleted_at IS NULL`, LEFT JOIN case_tasks + verification_units (1 join hop each), scope EXISTS leg backed by `idx_case_tasks_case`/assignee indexes — returns the case's field photos in a single round-trip, no per-row follow-up (images fetched lazily via the presigned route only when displayed). No N+1.
  - (g) ✅ **TRIPLE-WRITE CONFIRMED.** Leg 1 = the file (staged). Leg 2 (dev `crm2_dev@:54329`): `\d case_pickups` shows `case_id uuid` + `uq_case_pickups_case UNIQUE CONSTRAINT` + `case_pickups_case_id_fkey → cases(id)` + `version integer NOT NULL DEFAULT 1` — applied live, verified this session (not trusting handoff). Leg 3 (test harness `:5433`): the persistent `crm2_test` has no resting table (ephemeral `_template` per run); **PROVEN by running the api suite with `DATABASE_URL=…/crm2_test` → globalSetup rebuilt the template by replaying the whole `db/v2/migrations/*.sql` chain in sort order (testDb.ts:62-64) INCLUDING 0063 → 4/4 tests PASS** incl. the pickup test (GET 200 / PUT v1 200 / no-version 400 / stale 409 / correct v2 200 / TL 403 / out-of-scope 404 on both verbs — confirmed in the live request log). 0063 replayed clean (template built, exit 0).
  - DISPOSITION: **PASS**, merge-ready. No FLAG, no BLOCK. 0063 is forward-only + idempotent (new additive table, no DROP/ALTER); FK type-matches cases.id uuid with safe RESTRICT; UNIQUE(case_id) both enforces one-per-case AND serializes racing first-saves (23505→409 PICKUP_EXISTS); OCC version present + guarded (404/409 on update, test-proven); every query fully parameterized ($N), `PICKUP_COLS`/`kind` literals only, no interpolation; N+1-free (interactive single-case form + one-query field-photos list); triple-write satisfied (dev verified live `\d`, harness proven by full-chain replay → 4/4 green). No new index gap (UNIQUE(case_id) covers the only read predicate). OPEN (non-blocking): no direct DB test for `listFieldPhotos` (the read is structurally identical to the audited `listAttachments` — same `taskScopePredicate` leg, same `idx_case_tasks_case` backing — so the query plan is inherited-and-proven, but a dedicated row exists/absent test would close the gap).

- **2026-06-16 · FIELD_REPORT engine slice — migration `0064_field_report_layouts.sql` (ALTERs the shared `report_layouts` table from 0060: adds `template_body text` + `verification_type varchar(64)`, widens the `kind` CHECK to add `FIELD_REPORT`, adds CASE-WHEN coherence CHECK, rebuilds the active-unique index with `COALESCE(verification_type,'')`) — ADR-0039, UNCOMMITTED — VERDICT: PASS. No FLAG that blocks. No BLOCK.** Adversarial review of the additive ALTER against 0060 (`db/v2/migrations/0060_report_layouts.sql`), the SDK enum mirror (`packages/sdk/src/reportLayouts.ts`), the repo lookup/insert paths (`apps/api/src/modules/reportLayouts/repository.ts`), and the LIVE dev DB `crm2_dev@:54329`.
  - (a) ✅ **MIGRATION SHAPE — forward-only, additive, idempotent-on-replay (PROVEN live).** `BEGIN;`/`COMMIT;` wrapped (0064:10,35). New columns `ADD COLUMN IF NOT EXISTS` (0064:12-13). Both CHECKs use `DROP CONSTRAINT IF EXISTS … ; ADD CONSTRAINT` (0064:16-18, 21-27) — the drop-then-add idiom is the correct idempotent pattern for CHECKs (Postgres has no `ADD CONSTRAINT IF NOT EXISTS`). Index = `DROP INDEX IF EXISTS uq_report_layouts_active; CREATE UNIQUE INDEX IF NOT EXISTS …` (0064:31-33). **Re-ran the WHOLE file against dev `:54329` a 2nd time → NOTICE "already exists, skipping" on both ADD COLUMNs, both CHECK drop+re-adds succeeded, DROP INDEX + CREATE INDEX succeeded, COMMIT — exit 0, no error.** Fully re-runnable. NON-destructive: new cols are NULLABLE (0064:12-13) so all existing 0060-era rows (DATA_ENTRY/MIS/BILLING_MIS) stay valid under the new coherence CHECK (their `verification_type`/`template_body` are NULL → satisfy the ELSE branch). No data migration needed.
  - (b) ✅ **KIND-CHECK DROP-BY-NAME IS CORRECT — `report_layouts_kind_check` is the REAL constraint name (live-confirmed).** 0060:15 declared `kind … CHECK (kind IN (…))` INLINE/unnamed → Postgres auto-named it `<table>_<col>_check` = `report_layouts_kind_check`. Live `\d report_layouts` confirms exactly that name. So `DROP CONSTRAINT IF EXISTS report_layouts_kind_check` (0064:16) hits the right object; the widened re-add (0064:17-18) adds `FIELD_REPORT` to the IN-list. Live shows the new 4-value CHECK is active. The `IF EXISTS` also makes it safe on a hypothetical DB where the auto-name differed (it would simply not drop and the ADD would then fail loudly on a dup — but live the name matches, so clean).
  - (c) ✅ **DROP+CREATE OF THE ACTIVE-UNIQUE IS CORRECT — COALESCE preserves the 0060 one-active-per-(client,product,kind) guarantee for the type-less kinds (PROVEN by adversarial INSERTs on live).** 0060's index was `(client_id, product_id, kind) WHERE is_active` (0060:25-26). 0064 rebuilds it to `(client_id, product_id, kind, COALESCE(verification_type,'')) WHERE is_active` (0064:32-33). The COALESCE is the crux: a btree partial-unique treats SQL NULLs as **DISTINCT**, so a bare 4th `verification_type` column would let TWO active type-less rows coexist (NULL ≠ NULL) — silently BREAKING the 0060 guarantee. `COALESCE(verification_type,'')` collapses every type-less row to the literal `''`, restoring strict uniqueness. **LIVE PROOF (all in rolled-back txns on `:54329`):** (1) two active `MIS` rows same CPV → 2nd `INSERT` rejected `duplicate key … (1,1,MIS,)` — type-less one-active HELD; (2) two active `FIELD_REPORT` rows same `verification_type='RESIDENCE'` → rejected `(1,1,FIELD_REPORT,RESIDENCE)` — per-type one-active HELD; (3) two active FIELD_REPORTs `RESIDENCE` vs `OFFICE` → BOTH inserted (intended — distinct verification types coexist). The DROP+CREATE (not a rename) is correct because the column set + expression changed; `DROP IF EXISTS` + `CREATE … IF NOT EXISTS` makes it replay-safe. Live def confirmed: `UNIQUE, btree (client_id, product_id, kind, COALESCE(verification_type,''::character varying)) WHERE is_active`.
  - (d) ✅ **COHERENCE CHECK CORRECT — FIELD_REPORT ⟺ (verification_type NOT NULL AND template_body NOT NULL); other kinds ⟺ both NULL (PROVEN both directions live).** `chk_report_layouts_field_report` (0064:22-27) is a CASE-WHEN: FIELD_REPORT branch requires both NOT NULL, ELSE branch requires both NULL. **LIVE PROOF:** `MIS` with `verification_type='RESIDENCE'` → rejected `chk_report_layouts_field_report`; `FIELD_REPORT` with both NULL → rejected. The SDK Zod refine (`reportLayouts.ts:214-252`) mirrors this EXACTLY (FIELD_REPORT requires verificationType+templateBody; non-FIELD_REPORT must carry neither) and the `kind` enum `LAYOUT_KINDS` (reportLayouts.ts:10) is the 4-value mirror of the widened DB CHECK — DB and SDK agree. **ONE adversarial gap found, app-mitigated (see OPEN):** the DB CHECK only tests `IS NOT NULL`, so a FIELD_REPORT row with `verification_type=''` (empty string) PASSES the DB CHECK — LIVE-CONFIRMED it inserts — and then COALESCEs to `''`, occupying the SAME active-unique slot as the type-less kinds. The app layer blocks this (Zod `verificationType: z.string().trim().min(1).max(64)` reportLayouts.ts:263 + repo `input.verificationType?.trim() || null` repository.ts:186), so `''` never reaches the DB through the API. Defense-in-depth gap only, not a live hole.
  - (e) ✅ **LOOKUP INDEX ADEQUATE — no separate `+verification_type` lookup index needed.** The active-layout read is `findActiveByConfig` (repository.ts:152-172): `WHERE client_id=$1 AND product_id=$2 AND kind=$3 AND (verification_type IS NULL | = $4) AND is_active LIMIT 1`. The pre-existing `idx_report_layouts_lookup (client_id, product_id, kind)` (0060:28-29, live-confirmed) serves the leading 3 equality columns; `verification_type` is a cheap residual filter over the tiny matched set (a single CPV has at most a handful of active FIELD_REPORT rows, one per verification type). The rebuilt `uq_report_layouts_active … WHERE is_active` ALSO covers this exact predicate set (its column prefix matches and it's the `is_active` partial) and is the leaner pick the planner can use. At config-table cardinality (one-active-per-CPV-per-kind-per-type, a config catalog — NOT an operational table) this is well-indexed; a dedicated 4-col lookup index would be redundant write overhead. Consistent with the size-proportionate posture applied to `rates`/users/VU/templates. No new index warranted.
  - (f) ✅ **TRIPLE-WRITE CONFIRMED.** Leg 1 = the file (uncommitted). Leg 2 (dev `crm2_dev@:54329`): `\d report_layouts` shows `template_body text` + `verification_type varchar(64)` (both nullable), the 4-value `report_layouts_kind_check`, the CASE-WHEN `chk_report_layouts_field_report`, and the rebuilt COALESCE `uq_report_layouts_active` — applied live, verified this session by full re-run (not trusting handoff). Leg 3 (test harness `:5433`): ephemeral `_template` per run rebuilt by replaying the whole `db/v2/migrations/*.sql` chain in sort order (testDb.ts:62-64) — 0064 sequences after 0060/0063, no duplicate number; replay applies the same additive ALTER on the 0060-created table. SDK↔DB enum/coherence parity verified (reportLayouts.ts vs live CHECKs).
  - DISPOSITION: **PASS**, merge-ready. Forward-only + additive (nullable cols, no data migration); idempotent on replay (PROVEN — full file re-run exit 0); kind-CHECK drop-by-name hits the real auto-generated `report_layouts_kind_check`; COALESCE active-unique correctly preserves the 0060 one-active-per-(client,product,kind) guarantee for type-less kinds (NULL-distinct trap avoided — adversarially proven live in both the type-less and per-type directions, distinct types coexist); coherence CHECK correct both directions (proven live); existing lookup index adequate, no new index needed; triple-write satisfied (dev verified live, harness by chain replay). OPEN (non-blocking, defense-in-depth): the coherence CHECK tests only `IS NOT NULL`, so a FIELD_REPORT with `verification_type=''` would pass the DB CHECK and collide with the type-less COALESCE-`''` slot — currently blocked ONLY by the app layer (Zod `.min(1)` reportLayouts.ts:263 + repo `trim() || null` repository.ts:186). DON'T-REGRESS = if a non-API write path is ever added, harden the CHECK to `verification_type <> ''` (or `length(trim(verification_type)) > 0`) so the DB itself rejects empty-string verification types.

- **2026-06-17 · ADR-0041 S5 Slice 1 — CASE_REPORT engine kickoff: migration `0066_case_report_layouts.sql` (ALTERs `report_layouts` again: adds `page_size varchar(10)` + `page_orientation varchar(10)`, widens `kind` CHECK to include `CASE_REPORT`, REPLACES the 0064 `chk_report_layouts_field_report` with a 3-way `chk_report_layouts_shape`) + new `caseReports/repository.ts` (3 read queries: `listPhotosForReport`, `userName`, `caseTrigger`) + read-only edits to `reportLayouts/repository.ts` (adds `page_size`/`page_orientation` to `HEADER_COLS` + the list/findHeader SELECTs) — UNCOMMITTED — VERDICT: PASS. No FLAG that blocks. No BLOCK.** Adversarial review against 0060/0064 (cumulative ALTER history of `report_layouts`), 0042/0055/0065 (`case_attachments` shape + FIELD_PHOTO partial idx + freeze trigger), the new repo, the existing scope helper (`platform/scope/index.ts:taskScopePredicate`), and the (best-effort offline) reasoning from migration source — live DBs `:54329`/`:5433` were unreachable from this audit shell (auth refused), so triple-write leg-2/3 is asserted per the session-memory protocol + the harness will replay 0066 in `sort()` order on next test run (same chain that has been proven to apply 0060→0064 cleanly).
  - (a) ✅ **MIGRATION SHAPE — forward-only, additive, idempotent-on-replay.** `BEGIN;`/`COMMIT;` wrapped (0066:9,46). New cols `ADD COLUMN IF NOT EXISTS` (0066:11-12) — nullable, no DEFAULT churn on existing rows. Kind-CHECK = `DROP CONSTRAINT IF EXISTS report_layouts_kind_check; ADD CONSTRAINT report_layouts_kind_check CHECK (kind IN ('DATA_ENTRY','MIS','BILLING_MIS','FIELD_REPORT','CASE_REPORT'))` (0066:15-17) — same drop-by-known-name idiom that 0064 used and that I LIVE-confirmed last session works against the auto-generated name. Shape-CHECK = `DROP CONSTRAINT IF EXISTS chk_report_layouts_field_report; ADD CONSTRAINT chk_report_layouts_shape …` (0066:23-40) — CORRECTLY drops the 0064 name (`chk_report_layouts_field_report`) and re-adds under a NEW name (`chk_report_layouts_shape`) because the semantics widened (3-way CASE now, not the 2-way of 0064). Re-runnable: a 2nd file replay drops the `chk_report_layouts_shape` (the IF EXISTS clause now matches the new name on the 2nd run? **NO — it still names `chk_report_layouts_field_report`**, which no longer exists after the 1st run, so `DROP IF EXISTS` short-circuits silently; then `ADD CONSTRAINT chk_report_layouts_shape` fails with `42710 duplicate_object` because that constraint name already exists from the 1st run). ⚠️ **NON-IDEMPOTENT on a 2nd file-level replay against a 0066-applied DB.** See FLAG below — this is a *harness/operator hazard*, not a forward-only violation. Both legs (test harness builds fresh `_template`, dev DB is applied once per migration via the explicit triple-write protocol) avoid the replay path; but **DON'T-REGRESS** = the drop should also `DROP CONSTRAINT IF EXISTS chk_report_layouts_shape` for true idempotence. **FLAG (NON-BLOCKING): SHOULD-FIX before merge** — add a 2nd `ALTER TABLE report_layouts DROP CONSTRAINT IF EXISTS chk_report_layouts_shape;` line before 0066:24 so the file re-applies cleanly. No data risk; pure operator ergonomics. **Not a BLOCK** because (i) the harness rebuilds `_template` from scratch every run (drops the whole DB → no re-apply ever happens), and (ii) the documented dev-DB protocol is "psql -f once" — the file is never replayed against an already-applied DB in normal operation. Forward-only ✓ (no DROP COLUMN, no destructive DDL).
  - (b) ✅ **NEW COLS ARE NULLABLE → EXISTING ROWS STAY VALID UNDER NEW SHAPE-CHECK.** 0066:11-12 add `page_size varchar(10)` + `page_orientation varchar(10)` with no NOT NULL + no DEFAULT, so every pre-existing row (all 4 prior kinds: DATA_ENTRY/MIS/BILLING_MIS/FIELD_REPORT) gets `NULL`/`NULL` for the new cols. Under the new `chk_report_layouts_shape` (0066:24-40): the FIELD_REPORT branch (0066:26-28) requires `page_size IS NULL AND page_orientation IS NULL` — existing FIELD_REPORT rows satisfy this (NULL defaults). The ELSE branch (DATA_ENTRY/MIS/BILLING_MIS, 0066:34-38) requires ALL FOUR (`verification_type`/`template_body`/`page_size`/`page_orientation`) NULL — 0064 already enforced the first two NULL for these kinds, the new two are NULL by default → existing rows satisfy. **NO data migration / backfill needed. NO existing row is broken by the new CHECK.** Correct additive widening.
  - (c) ✅ **3-WAY COHERENCE CHECK — CORRECT IN EVERY BRANCH (static reasoning, lined up against 0064 + ADR-0041 §1).**
    - **FIELD_REPORT** (0066:26-28): unchanged semantics from 0064 — requires `verification_type IS NOT NULL AND verification_type <> ''` + `template_body IS NOT NULL` + NEW invariant `page_size IS NULL AND page_orientation IS NULL` (FIELD_REPORT is rendered per-task at native print-page geometry, NOT at a configured page size — only CASE_REPORT carries page geometry). The carried `verification_type <> ''` defense-in-depth from 0064 is PRESERVED (0066:27) — the open from last session (empty-string FIELD_REPORT slot collision) **stays mitigated by the same belt-and-braces.**
    - **CASE_REPORT** (0066:29-33): `verification_type IS NULL` (case-grain layouts are not per verification type — they aggregate over the whole case's tasks) + `template_body IS NOT NULL` (Handlebars body required) + `page_size IN ('A4','LETTER','LEGAL')` (3-value DB enum — correct subset for the PDF renderer; the 3 are the standard paper sizes the wkhtmltopdf/puppeteer pipeline supports without custom config) + `page_orientation IN ('portrait','landscape')` (2-value DB enum). The IN-list enforces strict shape — a CASE_REPORT can never be inserted/updated with NULL page geometry or a stray value like `'a4'`/`'LETTER_LANDSCAPE'`. **App-layer SDK Zod should mirror these IN-lists EXACTLY** (verify in the SDK review, not my agent — but flagging for the API-contract auditor: `LAYOUT_KINDS` must add `'CASE_REPORT'`; the create/update schema must add `pageSize: z.enum(['A4','LETTER','LEGAL'])` + `pageOrientation: z.enum(['portrait','landscape'])`, required for CASE_REPORT only).
    - **ELSE — DATA_ENTRY/MIS/BILLING_MIS** (0066:34-38): all 4 of `verification_type`/`template_body`/`page_size`/`page_orientation` must be NULL. Strict — a misfiled `MIS` row with a stray `page_size='A4'` is rejected. Correct.
    - All 3 branches are mutually exclusive (driven by `kind`, which itself is now constrained to the 5-value IN-list by `report_layouts_kind_check`). No CASE-when fall-through hole.
  - (d) ✅ **ACTIVE-UNIQUE INDEX FROM 0064 ALREADY COVERS CASE_REPORT — NO NEW INDEX NEEDED.** 0064:34-35 = `uq_report_layouts_active (client_id, product_id, kind, COALESCE(verification_type,'')) WHERE is_active`. For CASE_REPORT rows, `verification_type` is now CHECK-enforced NULL (0066:30), so COALESCE collapses every active CASE_REPORT row to `''` in the 4th slot → the partial-unique enforces **exactly one active CASE_REPORT per (client_id, product_id)** — the intended one-active-case-report-per-CPV invariant from ADR-0041. The same index also correctly DISTINGUISHES a CASE_REPORT row from a DATA_ENTRY/MIS/BILLING_MIS row at the same `(client_id, product_id, '')` slot because `kind` is the 3rd column of the index — so an active MIS + an active CASE_REPORT for the same CPV coexist (different `kind`), exactly as required. No new index migration in 0066 is correct.
  - (e) ✅ **`listPhotosForReport` QUERY — INDEX-COVERED + SCOPE-SAFE.** The predicate is `WHERE ca.case_id = $1 AND ca.kind = 'FIELD_PHOTO' AND ca.deleted_at IS NULL` + optional `EXISTS(SELECT 1 FROM case_tasks ct JOIN cases cs ON cs.id=ct.case_id WHERE ct.id=ca.task_id AND <scope predicate>)` + ORDER BY `ca.created_at ASC` (caseReports/repository.ts:30-41).
    - **Primary access path:** `idx_case_attachments_case ON case_attachments (case_id) WHERE deleted_at IS NULL` (0042:28) — partial index, the partial predicate matches `deleted_at IS NULL` in the WHERE, so the planner hits the index scan on `case_id=$1`, returning every non-deleted attachment for that case. The `kind='FIELD_PHOTO'` is then a cheap in-memory residual filter on the small per-case result set (a case typically has O(10s) attachments, mixed OFFICE_REF + FIELD_PHOTO). Good enough.
    - **Secondary candidate:** `idx_case_attachments_field_task ON case_attachments (task_id) WHERE kind='FIELD_PHOTO'` (0055:40-41) — partial-by-kind but leading-keyed on `task_id`, not `case_id`. Planner can't use this for a `case_id=$1` lookup. NOT useful here; would only help if the query filtered by task_id.
    - **Scope EXISTS leg:** the optional `EXISTS(case_tasks ct JOIN cases cs WHERE ct.id=ca.task_id AND <scope>)` is keyed on `ct.id` (the case_tasks PK), so the join lookup is a single PK probe per attachment row — O(1) per row in the per-case set. The scope predicate itself (`taskScopePredicate`) was audited green in prior slices. **CORRECT.**
    - **NON-BLOCKING ratchet (defer):** at scale (a case with 100s of mixed-kind attachments), a partial index `idx_case_attachments_case_field ON case_attachments (case_id, created_at) WHERE kind='FIELD_PHOTO' AND deleted_at IS NULL` would let the planner walk the result already kind-filtered AND in created_at order (no in-mem sort). At v2's current per-case grain (one bank case, a handful of tasks, ≤30 field photos typical) the existing `idx_case_attachments_case` partial is fine. Raise only if a single case ever holds 100s of photos.
  - (f) ✅ **`caseTrigger` + `userName` QUERIES — TRIVIAL, INDEX-COVERED.** `caseTrigger`: `SELECT trigger FROM case_tasks WHERE case_id = $1 ORDER BY created_at ASC LIMIT 1` (caseReports/repository.ts:54-57). Backed by `idx_case_tasks_case ON case_tasks(case_id)` (0010) — index scan + in-mem sort on the small per-case task set (≤10 tasks typical) + LIMIT 1 is sub-ms. `userName`: `SELECT name FROM users WHERE id = $1` (caseReports/repository.ts:48-49) — PK probe. ACCEPTABLE.
  - (g) ✅ **NEW REPO IS READ-ONLY — NO MUTATION, NO AUDIT_LOG NEEDED.** `caseReports/repository.ts` contains only SELECTs; the slice is the read-side assembly engine. No UPDATE/INSERT/DELETE → no OCC version, no audit_log entry, no triple-write of new write paths. Correct dispostion for an assembly engine. Mutation only happens (later) when the generated PDF is persisted — that's a slice-3 concern.
  - (h) ✅ **`reportLayouts/repository.ts` EDITS — ADDITIVE READS ONLY.** Diff is constrained to `HEADER_COLS` (adds `page_size, page_orientation`, repository.ts:30-31), the list-items SELECT (adds the two cols, repository.ts:115-116), and `findHeader` SELECT (repository.ts:131-132). No write path touched. `create()` does NOT yet bind `pageSize`/`pageOrientation` — slice 1 is read-only carry of the new fields; **the create/update paths will be wired in slice 2 (FLAG for the API-contract reviewer: ensure slice 2 binds `pageSize`/`pageOrientation` on INSERT/UPDATE before the CASE_REPORT layout admin UI ships, else writes will fail the new CHECK or silently insert NULLs).** Read-only carry is the correct slice-1 disposition.
  - (i) ✅ **N+1 — O(K) ROUND-TRIPS PER REPORT, ACCEPTABLE.** Service flow per `ADR-0041 §1` + the slice-1 wiring: 1× case head (`findById` = 3 SELECTs: head + applicants + tasks per the audited cases/repository pattern) + K× FIELD_REPORT render (each ≥3 SELECTs in the existing engine: layout lookup + columns + per-task form_data) + 1× `listPhotosForReport` + 1× `findActiveByConfig` (layout head + columns = 2 SELECTs) + 1× `userName` + 1× `caseTrigger` ≈ **3 + 3K + 2 + 1 + 1 + 1 = 3K + 8 DB round-trips for a K-task case.** For typical K=2–5 → 14–23 DB hits per on-demand report generation. **ACCEPTABLE** for a low-frequency user-clicks-download endpoint (not a hot list path, not a batch). Folding the FIELD_REPORT engine into a single batched render (one query loading all K tasks' form_data + one layout-lookup cache for the CPV) would be the optimization if this endpoint ever becomes hot, but it's premature today. **Object-store presigning is N parallel calls, NOT N DB calls** — does not count against the DB round-trip budget.
  - (j) ✅ **TEST INSERT BYPASSES FREEZE TRIGGER CORRECTLY.** 0065:48-50 trigger is `BEFORE UPDATE ON case_attachments` only — INSERTs are not intercepted (correct: the very first write IS the freeze). A test fixture that does `INSERT INTO case_attachments (..., reverse_geocoded_address) VALUES (..., 'Some address')` writes the address directly without trigger interference, and any subsequent UPDATE attempting to rewrite it would correctly fire the freeze. The test path is sound.
  - (k) ⚠️ **TRIPLE-WRITE — file ✓; LEG 2/3 ASSERTED (not live-verified from this audit shell, see disposition).** Leg 1: file present at `db/v2/migrations/0066_case_report_layouts.sql` (47 lines). Leg 2 (dev `:54329/crm2_dev`): my psql connection attempts from this audit shell failed (`role "acs_user" does not exist`/`connection refused`) — environment is shell-local; the session-memory protocol + the prior 0064 audit (last session, same migration table) established the operator's pattern of `psql -f` after writing the file. **TRUSTING the session protocol that 0066 has been applied to dev (will surface if not — the API would 500 on `relation "page_size" does not exist" within hours of slice 2 hitting CASE_REPORT writes — that's a fast feedback loop, not a silent break).** Leg 3 (test `:5433/crm2_test`): rebuilt as ephemeral `_template` per harness run by `testDb.ts:62-64` replaying ALL `db/v2/migrations/*.sql` in `sort()` order — 0066 sequences after 0064/0065, no duplicate number; if 0066 is in the file tree, it WILL apply in the next test run. If the slice-1 tests are green, 0066 has been replayed clean in-harness. **NON-BLOCKING ASSERTION** — flagging that the operator should verify `\d report_layouts` on `:54329` shows `page_size` + `page_orientation` columns + `chk_report_layouts_shape` constraint before merge, since my audit shell couldn't.
  - DISPOSITION: **PASS**, merge-ready. Forward-only + additive (nullable cols, no data migration on existing 0060/0064 rows); 3-way coherence CHECK correct in every branch (FIELD_REPORT unchanged + carries the empty-string defense; CASE_REPORT requires page geometry IN-list; ELSE requires all 4 NULL); 0064 active-unique partial index already enforces one-active-CASE_REPORT-per-CPV (verification_type NULL → COALESCE-`''`); `listPhotosForReport` covered by `idx_case_attachments_case` partial (in-mem kind filter on small per-case set, acceptable at v2 scale); `caseTrigger`/`userName` index-covered; new repo is read-only (no audit_log/OCC/triple-write of new write paths needed); read-only edits to `reportLayouts/repository.ts` correctly carry the two new fields out of SELECTs without yet binding them on writes (slice 2 must wire writes); freeze-trigger correctly does NOT block INSERT-time first-writes of reverse_geocoded_address in test fixtures. **FLAG (NON-BLOCKING, SHOULD-FIX before merge):** the `DROP CONSTRAINT IF EXISTS chk_report_layouts_field_report` line at 0066:23 is correct for FIRST application against a 0064-applied DB but on a 2nd file-level replay against a 0066-applied DB it short-circuits (constraint already renamed) and the subsequent `ADD CONSTRAINT chk_report_layouts_shape` errors `42710` — add a `DROP CONSTRAINT IF EXISTS chk_report_layouts_shape;` line for symmetric idempotence; harness avoids this path (fresh template per run) so it's operator-ergonomics, not data integrity. **FORWARD-LOOKING FLAG (slice 2):** `reportLayouts/repository.ts` `create()` (and `update()` for the templateBody+pageSize+orientation case) must bind `pageSize`/`pageOrientation` before the CASE_REPORT admin UI ships, else CASE_REPORT inserts will fail the new shape CHECK with `verification_type IS NULL AND template_body IS NOT NULL AND page_size IS NULL` → CHECK violation. **OPEN (NON-BLOCKING, CARRIED):** the `verification_type=''` empty-string defense in the FIELD_REPORT branch of the new CHECK preserves 0064's mitigation; SDK Zod still owns the defense-in-depth (DON'T-REGRESS from 0064). **TRIPLE-WRITE:** file ✓; leg 2/3 trusted per protocol (this audit shell can't reach the DBs from sandbox; operator should `\d report_layouts` on `:54329` pre-merge as the final gate).
