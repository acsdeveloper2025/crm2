# Frozen Decisions Registry (CRM2)

> **Single source of truth for every frozen decision.** Build phase is active with multiple
> agents in parallel — the primary risk is **architecture drift**, not design. Any decision below
> is **LOCKED**: changing it requires the full process in `docs/ARCHITECTURE_GOVERNANCE.md`
> (ADR + Impact Analysis + Alternatives + Migration Plan + CTO approval). Enforcement detail per
> decision is in `FREEZE_LOCK_REPORT.md`. Default **Owner = CTO** (domain-specific owners in
> `DOMAIN_OWNERSHIP.md`). Dates are when the decision was frozen.

| # | Decision | Status | ADR | Enforced By | Owner | Date Frozen |
|---|---|---|---|---|---|---|
| 1 | **Case → Task → Verification Unit** model | LOCKED | ADR-0002 | DDL CHECKs (mig 0001) + MASTER_MEMORY §2 + dependency-cruiser + code review | CTO | 2026-06-04 |
| 2 | **Verification Unit Registry** (one catalog, 9 field ∪ 59 KYC = 68) | LOCKED | ADR-0001 | migration 0001 + seed + `db/v2/REGISTRY_SPEC.md` + `BUILD_GATE_REGISTRY_LOCK.md` | CTO | 2026-06-04 |
| 3 | **PostgreSQL 17** (PG18 deferred) | LOCKED | ADR-0003 | DESIGN_AND_STACK_FREEZE + migrations + DATABASE_CHANGE_PROCESS | CTO | 2026-06-04 |
| 4 | **No Prisma / no ORM** — raw `pg` | LOCKED | ADR-0004 | ENGINEERING_STANDARDS + dependency-cruiser (`db-access-only-in-repositories`) + review | CTO | 2026-06-04 |
| 5 | **Repository pattern** (controller→service→repository) | LOCKED | ADR-0005 | dependency-cruiser (`controller-not-to-repository`, `db-access-only-in-repositories`) + review | CTO | 2026-06-04 |
| 6 | **Data-access strategy** — raw SQL only in repositories + migrations | LOCKED | ADR-0005 | ENGINEERING_STANDARDS §5 + dependency-cruiser + review | CTO | 2026-06-04 |
| 7 | **API versioning** — single `/api/v2` for web + mobile | LOCKED | ADR-0011 | route prefix + `API_VERSIONING_POLICY.md` + review | CTO | 2026-06-04 |
| 8 | **Mobile compatibility** — `crm-mobile-native` is a first-class `/api/v2` consumer | LOCKED | ADR-0012 | `MOBILE_API_COMPATIBILITY_MATRIX.md` + contract tests (pending-activation) | CTO | 2026-06-04 |
| 9 | **Verification Workspace** (one status-adaptive workspace; KYC = a mode) | LOCKED | ADR-0006 | MASTER_MEMORY §1 + review (build pending) | CTO | 2026-06-04 |
| 10 | **Naming convention** (snake SQL · camel TS · kebab routes · UPPER_SNAKE env/codes) | LOCKED | ADR-0007 | ENGINEERING_STANDARDS §6 + `camelize()` edge + review | CTO | 2026-06-04 |
| 11 | **Design system** (80% Twenty / 10% Linear / 10% Salesforce; light-first) | LOCKED | ADR-0008 | DESIGN_AND_STACK_FREEZE + `COLOR_SYSTEM_FREEZE.md` + review | CTO | 2026-06-04 |
| 12 | **UI theme** — `@crm2/ui-theme` tokens are the only color source | LOCKED | ADR-0008 | `COLOR_SYSTEM_FREEZE.md` + no-hardcoded-color review gate (0 in features/components) | CTO | 2026-06-04 |
| 13 | **Feature-flag strategy** | LOCKED | ADR-0009 | ADR-0009 + review (`useFeatureFlag` pending) | CTO | 2026-06-04 |
| 14 | **Reporting strategy** (`v_` / `mv_` via raw-SQL reporting repositories) | LOCKED | ADR-0010 | ENGINEERING_STANDARDS §5 + repository pattern | CTO | 2026-06-04 |
| 15 | **TypeScript strict** (all strict flags) | LOCKED | ADR-0013 | `tsconfig.base.json` (6 flags) + CI `typecheck` | CTO | 2026-06-04 |
| 16 | **Logger standard** — `@crm2/logger` (6 levels); `console.*` banned | LOCKED | ADR-0013 | `@crm2/logger` + ESLint `no-console` + `check-suppressions` + review | CTO | 2026-06-04 |
| 17 | **Quality gates** (ESLint strict · Prettier · coverage · gitleaks · boundaries · no-circular) | LOCKED | ADR-0013 | `docs/CI_CD_STANDARDS.md` matrix + `pnpm verify` + CI | CTO | 2026-06-04 |
| 18 | **Security standards** (6-role RBAC, default-deny scope, secret scanning) | LOCKED | ADR-0013 | `SECURITY_STANDARDS.md` + `@crm2/access` + gitleaks + review | CTO / Security owner | 2026-06-04 |
| 19 | **Universal DataGrid** (TanStack Table; the only data table) | LOCKED | — (standards freeze; ADR-0013 umbrella) | `docs/DATAGRID_STANDARD.md` + CI gates 45–48 + review | CTO | 2026-06-05 |
| 20 | **Pagination standard** (server-side; 25/50/100/200/500; envelope) | LOCKED | — (standards freeze) | `docs/PAGINATION_AND_LOADING_STANDARDS.md` + CI gates 40–44 | CTO | 2026-06-05 |
| 21 | **Search & filtering standard** (global/column/Excel/multi-col/saved views) | LOCKED | — (standards freeze) | `docs/DATAGRID_STANDARD.md` + CI gates 46–47 | CTO | 2026-06-05 |
| 22 | **Loading standard** (time-bands · Hexagon loader · real % · skeletons) | LOCKED | — (standards freeze) | `docs/PAGINATION_AND_LOADING_STANDARDS.md` + `UI_STANDARDS.md` | CTO | 2026-06-05 |
| 23 | **Background-job standard** (`>8s`/exports = jobs; bell/toast/in-app) | LOCKED | — (standards freeze) | `docs/PAGINATION_AND_LOADING_STANDARDS.md` §10–11 + CI gate 44 | CTO | 2026-06-05 |
| 24 | **Package architecture** (5 pkgs + `@crm2/logger`; **no `@crm2/ui`** — components in-app) | LOCKED | ADR-0013 | MASTER_MEMORY §4 + `ALLOWED_DEPENDENCIES.md` + review | CTO | 2026-06-04 |
| 25 | **Technology stack** (Node 22 · TS · PG17 · Valkey 8 · React 19 · Tailwind 4 · shadcn · TanStack Query/Table · Vitest · Playwright · pnpm · Turbo) | LOCKED | ADR-0003/0008/0013 | DESIGN_AND_STACK_FREEZE + `ALLOWED_DEPENDENCIES.md` + lockfile review | CTO | 2026-06-04 |
| 26 | **Universal Import/Export** (DataGrid = sole export entry, 3 modes, XLSX/CSV/PDF, `≥10k`=job; one `@crm2/import-engine`, app-internal; standard import flow + validation report + import audit) | LOCKED | — (standards freeze; ADR-0013 umbrella) | `docs/IMPORT_EXPORT_STANDARD.md` + `docs/DATAGRID_STANDARD.md` + review (build DEFERRED) | CTO | 2026-06-05 |
| 27 | **Platform-capability locations & ownership** (DataGrid → web `components/ui/data-grid/`; import → api `platform/import/`+worker+web flow; export → api `platform/export/`+report-worker; all app-internal, package **extraction DEFERRED**; scope of ui-theme/logger/sdk/access/config clarified) | LOCKED | — (governance; ADR-0013 umbrella) | `docs/PLATFORM_CAPABILITIES_OWNERSHIP.md` + `ALLOWED_DEPENDENCIES.md` + review | CTO | 2026-06-05 |
| 28 | **Case Workspace (single page) & per-client+product Reporting Engine** (one save-gated page: per-task data-entry/MIS · assignment · FE-mobile images+data · report entry · auto-gen · per-task PDF/Word/Excel · Final Status+Case Report; reporting = config-driven per (client,product[,type]) templates of two kinds **MIS_EXCEL** + **CASE_REPORT**, formats PDF/WORD/EXCEL, field/column mapping incl FE-mobile data+images+seal; admin Template Designer; 200+ formats = config not code) | LOCKED | **ADR-0015** (supersedes type-only 0008 Report Templates; extends ADR-0010/0012) | `docs/CASE_WORKSPACE_AND_REPORTING_FREEZE.md` + review (build DEFERRED) | CTO+owner | 2026-06-05 |
| 29 | **Rate Management** — one unified **Verification-Unit** rate axis (no separate KYC table); resolution chain `(client,product,VU,location)→SZR→rate_type→rates→amount` (KYC VUs skip SZR/rate_type); **strict no-fallback** + eligibility-gates-pricing (DB trigger); **real effective-dated versioning** (revise=new dated row, point-in-time, no-overlap exclusion); **freeze-by-copy preservation UNCHANGED from V1**; single-page Workspace on the DataGrid standard (one line per rate) | **SUPERSEDED → ADR-0018** | **ADR-0016** (4-table model abandoned mid-build) | superseded by #31 (flat one-table model); ADR-0016 + RATE_MANAGEMENT_FREEZE banner-stamped | CTO+owner | 2026-06-05 |
| 30 | **Effective-From temporal usability gating (master data)** — user-settable `effective_from timestamptz NOT NULL DEFAULT now()` on the 7 master-data tables (verification_units, clients, products, rate_types, locations, users, report_templates) **+ the 2 CPV join tables** (client_products, client_product_verification_units; mig 0016); **USABLE ⇔ `is_active AND effective_from <= now()`**; `?active=true` means USABLE; operational reads (login · assignable-users · CPV available-units/allUnitsEnabled gating vu+cp+cpvu · rate dropdowns · pincode cascade · rate-type lookup) gate on it; CPV admin list shows active unit-count per link (discoverability); admin lists show all + ACTIVE/SCHEDULED/INACTIVE; **no `effective_to`** on master data (is_active ends a row) | LOCKED | **ADR-0017** (generalises ADR-0016 effective-dating; does not import the rates revision/overlap model) | `docs/EFFECTIVE_FROM_STANDARD.md` + `0015_effective_from.sql` + `0016_cpv_effective_from.sql` + repo gates + tests (api 113 green) | CTO+owner | 2026-06-05 |
| 31 | **Rate Management — flat one-table model** (supersedes #29) — a rate is ONE flat effective-dated row `(client, product, verification_unit, location_id, rate_type) → amount`; `rate_type` = varchar snapshot from a read-only managed `rate_types` lookup (18 codes); **NO eligibility table, NO service_zone_rules, NO eligibility trigger, NO rate_type_id FK** (all dropped in mig 0013); revise = new dated row + end-date prior (no overwrite); KYC units null location/rate_type; freeze-by-copy preservation unchanged; single searchable table UI (pincode→area cascade, Kind-gating) | LOCKED | **ADR-0018** (supersedes ADR-0016) | migrations `0013_rate_management_flatten.sql` + `0014_rate_types_lookup.sql` + `rates`/`rateTypes` modules + review | CTO+owner | 2026-06-05 |

| 32 | **Responsive-First Web Design** — the web app is responsive-first / device-agnostic; every screen designed mobile-up (320 → 768 → 1024 → 1440), no desktop-only design, no horizontal overflow; responsive nav (sidebar → hamburger/Sheet `<lg`), responsive grids (mobile-up `grid-cols-1 md:…`), responsive dialogs (`w-full`+scroll/Sheet), responsive filters (`flex-wrap`); **table strategy** desktop DataGrid → tablet condensed → mobile card/list; Playwright viewport testing required (320/768/1024/1440) | LOCKED | — (standards freeze; ADR-0008 design-system + ADR-0013 umbrella) | `docs/RESPONSIVE_DESIGN_STANDARD.md` + CI gates 49–50 + review | CTO+owner | 2026-06-05 |

| 33 | **Concurrency & editing standard** — **Optimistic Concurrency Control**: every editable table has `version integer NOT NULL DEFAULT 1`; every update (incl. activate/deactivate) is guarded `… SET …, version=version+1, updated_at=now(), updated_by=$actor WHERE id=$id AND version=$expected RETURNING …`; 0 rows → 404 or **409 `STALE_UPDATE`** (with current row); updates **require** the expected version (body `version`/`If-Match`, else 400 VERSION_REQUIRED); reads return version; multi-stmt = `withTransaction`; every change appends an **immutable** audit/history row; bulk = **per-row OCC** partial-success (large=job); FE shows a **Conflict dialog** (reload & re-apply / discard) — **no silent overwrite**. No pessimistic locks across user think-time. | LOCKED | **ADR-0019** (+ADR-0005 repo, depends on the frozen append-only audit) | `docs/CONCURRENCY_AND_EDITING_STANDARD.md` + CI gates 51–53 + review | CTO+owner | 2026-06-05 |

| 34 | **Access Control 2.0** — configuration is DATA, semantics are CODE: roles are rows (`roles`, custom roles allowed; 6 system roles delete/deactivate-locked, SUPER_ADMIN fully locked via `grants_all`); role→permission mapping DB-editable (`role_permissions`; permission CATALOG stays code-owned); per-role hierarchy visibility mode (ALL/SUBTREE/DIRECT_TEAM/SELF); scope-dimension wiring per role (`role_scope_dimensions`, EXPAND/RESTRICT) over a code-owned dimension predicate registry (`platform/scope/dimensions.ts` — new dimension = one reviewed code addition); ONE generic `user_scope_assignments`; visibility = `(hierarchy OR expand) AND restrict`, RESTRICT fail-closed at every layer; **zero role-name literals in api business logic** (CI-tested); FE gates on permissions from login//me, never role names. Amends row 18's "6-role code-defined RBAC" (sanctioned reopen). | LOCKED | **ADR-0022** (owner-signed 2026-06-10) | migrations 0033–0035 + `noRoleLiterals` drift-gate test + roles-seed parity test + audit panel ledger | CTO+owner | 2026-06-11 |

| 35 | **Login policy acceptance** — admin-managed, versioned, all-users **server-driven gate**: every user must accept all `is_active AND effective_from <= now()` policies before the app loads; enforced server-side on **login** (`mustAcceptPolicies` + `pendingPolicies` on `LoginResponse`) and re-checked on **refresh** (mirrors the `mustChangePassword` gate, strictly more robust than v1's FE-only guard); **two version columns on purpose** — `content_version` drives re-acceptance (bumps only on content publish → fleet-wide re-accept), `version` is OCC (ADR-0019, bumps every save); self-service `POST /api/v2/auth/accept-policies` (authenticated, no permission → IDOR-safe, server snapshots `content_version`, idempotent upsert); **immutable** `policy_acceptances` audit (one row per `(user, policy, content_version)` + ip/UA/source); admin CRUD module `/api/v2/policies` (`policy.manage` writes, `page.policies` view) + ADMINISTRATION nav; starter = v1 Field-Exec Acknowledgement seeded ACTIVE (decline = logout); built v2-API mobile-compatible (`source='MOBILE'` reserved). | LOCKED | **ADR-0043** | migration `0068_policy_acceptance.sql` + `@crm2/access` (`policy.manage`/`page.policies`) + auth gate/policies-module tests + `MOBILE_API_COMPATIBILITY_MATRIX.md` | CTO+owner | 2026-06-17 |

**No row may be changed in place.** A superseded decision gets a NEW row + a superseding ADR; the
old row is marked `SUPERSEDED → ADR-NNNN` (never deleted). See `docs/ARCHITECTURE_GOVERNANCE.md`.
