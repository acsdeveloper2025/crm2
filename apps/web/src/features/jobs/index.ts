// Public surface of the jobs feature. Other features import from here (never the
// internal modules directly) — enforced by the no-cross-feature-internals boundary.
export { JOBS_KEY } from './api.js';
