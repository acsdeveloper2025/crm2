// Public surface of the rateTypeAssignments feature. Other features import from here (never the
// internal modules directly) — enforced by the no-cross-feature-internals boundary.
export { RateTypeAssignmentsPage } from './RateTypeAssignmentsPage.js';
