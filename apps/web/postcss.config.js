// Tailwind v4: the framework ships its own PostCSS plugin (vendor-prefixing
// included), replacing the v3 `tailwindcss` + `autoprefixer` pair.
export default { plugins: { '@tailwindcss/postcss': {} } };
